# 会话交接

最后更新：2026-09-21（17:25）

> 只放**会变的**：当前进度、环境现状、下一步、未提交清单。
> 稳定知识在 `项目知识库/`（入口 `00-index.md`；**跑命令 / 查环境 → `运行手册.md`，写断言前 → `探壳结论.md`**）。
> 已结的历史条目在 `交接归档/`（最近切出：`会话交接-20260921.md`，含 15:40 这一批；更早：`会话交接-20260917.md`）。
> ⚠️ **该目录只在本机** —— 已进 `.gitignore`，**clone / 整包拷贝之后不存在**，别照着找。

## 状态锚点（新对话先读这块）

- **焦点**：华硕大厅 1.6.11.4 客户端 UI 自动化（pywinauto + Win32 + pytest）。P0 / P1-A / P1-B / P1-C 全结，
  P2 安全验证 + WorkBuddy 入口 + 证据留痕已落地。**多机跑批地基已落地，第一台真测试机正在上线。**
- ✅ **首台真机 DESKTOP-DOHED68（Win10 19045 / Python 3.13.14）12 步 bootstrap 全跑过（16:00）**：
  第 1–11 步全 `[OK]`，**唯一红的第 12 步是杀软误杀**（见下），与代码无关。**可以继续发第二台。**
- ✅ **`--loop`「卡住不动」已定位并修（本轮，17:20）**：新机跑 `farm_agent.py --loop` 打印两行后
  5 分钟无输出，被当成卡死。**实际是空闲分支一个字都不打** —— 30 秒一次静默轮询，机器好得很。
  **更糟的是它与真故障同形**：UNC 一断 `Path.is_file()` 把 `OSError` 吞成 `False`，
  `take_task` 一路走到"无任务"，节点在报假平安。**修**：每次空闲轮询打心跳（含"在等哪个任务文件"，
  顺手解决"节点名对不上"的核对）+ 农场探不到时换成中文排查指引 + 关键 `print` 全加 `flush=True`
  （重定向到文件时是块缓冲，不 flush 又变回"看起来卡住"）；`--once` 不可达**返回 3**，
  不再打「无任务，退出」+ 0（那是假绿）。8 条回归锁在 `tests/unit/test_farm_agent_policy.py`。
  详见 `项目知识库/运行手册.md`「节点侧的四道防呆」。
- ⚠️ **未验项**：`tools/install_git.ps1` 的**安装程序本身仍未被执行过**（真跑会在这台机器上装一份 Git）；
  `tools/add_av_exclusions.ps1` **也没真跑过**（本机非管理员 + 会话内子 `powershell.exe` 被安全策略挡；
  只做了 PowerShell 解析器语法检查=通过，另有源码守卫用例）。第一次真加排除项时盯一眼输出。
  （待用户确认：新机上 Git 是脚本装的，还是那台机器本来就有。）
- **环境级 flake = 杀软误杀（根因已坐实，不再是"疑似驱动"）**：Windows Defender 的 ML 判定把进程
  **刚写出**的临时文件判成 `Trojan:Win32/Bearfoos.A!ml`，此后读它返回 `GetLastError=225`
  （Python 显示成 `[Errno 22] Invalid argument`），约 14 秒后文件被隔离删除。
  **它挡的不只是第 12 步** —— 从共享盘取包进 `_cache` 同样会被拦，直接卡 P0/P1。
  16:45 真机**关掉实时保护后同一批用例立刻转绿**（反向坐实根因是杀软）。
  ⚠️ **关杀软只是权宜**：篡改保护/组策略会把实时保护自动开回来，且**第三方杀软不在脚本管辖内**。
  **正规做法是恢复防护 + `tools/add_av_exclusions.ps1`**（默认不跑、bootstrap 也不调，有守卫用例锁着）。
  **要不要给 5 台统一加 —— 待你拍板，未落地。** 确认命令 `tools\probe_av_quarantine.py`（只读）。
  细节见 `运行手册.md` 坑 6。
- ⚠️ **git 身份**：global 无 `user.name/email`；本仓库已设 **repo-local** `wwd <alven3330859445@gmail.com>`。
- ⚠️ **本机拿不到 `origin/main` 远端跟踪引用**（环境层：`git fetch origin` 打印新建分支但
  `.git/refs/remotes/origin/` 始终空、`git status` 显示 `[gone]`；手工 `update-ref` rc=0 也不落地，
  对照组正常 → 不是仓库问题）。**别拿它判断"没 push"** —— 权威口径是 `git ls-remote origin` / `share`。
- **机器规模（组长 2026-09-18 15:47）**：一批 5 台、跑完再换下一批。**批次调度代码暂不实现。**
- **安装包分发**：组长明确**没有官方下载地址** → 走自建共享盘，已启用并端到端验证。
  **前提：包源机（你这台笔记本）要常开**，长期跑建议换常开测试机。
- **7-Zip 自动装钉 26.02**（不是最新版）：它同时是 P1-A「更新夹具」的起点，装最新版会让
  `test_update_fixture_via_hall` 起点断言失配。**只有 Python 仍须人工**（引导悖论，唯一真正绕不开的第 0 层）。
- **共享盘侧两条遗留**：① `HallShare-SMB` 防火墙规则（445 入站，Private+Public）跑批结束记得关；
  ② `hallshare` 账号密码已重置且永不过期（**密码值不进任何文件**），节点 bootstrap 必须用同一个密码。
  建共享是**唯一需要提权的动作**，已用完 —— 5 台共用，不必每台再来。
- **发给测试同事两份**：`项目知识库/测试机操作手册.md`（完整版，讲为什么、报错往哪查）+
  `新机操作清单.md`（**纯步骤版，不带解释**）。别让人看《运行手册.md》现学。
  **桌面副本由 `tools/sync_handbook.py` 同步**（`DOCS` 列表；`--check` 旧了 rc=1）——
  手工同步靠记性一定会漏（09-21 真发生过：桌面停在三天前的 10 步版，差三轮修复）。

### 下一步第一件事

1. **第一台真测试机继续往下走**：重拉代码（先 `safe.directory`、clone 带目标目录名，见清单 2A）→
   管理员窗口里设 `HALL_SHARE_USER/PASSWORD` + `HALL_FARM_ROOT` → 跑 `tools\bootstrap_machine.ps1`。
   **顺序不能反：先开管理员窗口 → 在窗口内设变量 → 同一窗口跑**（反了变量全丢）。
   提权是必须的：第 4 步装 7-Zip、第 11 步建计划任务。完整步骤见手册。
   **无外网的机器**：代码走**共享盘 clone**，离线依赖共享盘 `hall-packages\wheelhouse\` 上已备好
   （bootstrap 自动取），别再拷 `.venv`。
   ⚠️ **发新机之前先 `git ls-remote share` 核一眼** —— 新机 clone 到旧文档**不报错**，
   这是本轮**真正挡住"发第二台机器"**的那一件（09-21 共享盘曾落后 2 个 commit）。
2. 每台机器**手工填 `farm_node.env`**（bootstrap 只生成空模板）—— `HALL_TEST_USER/PASSWORD/MS_USER`。
   不填 → 登录套件静默跳过（回执 `credentials` 会标出，不假绿但拿不到结论）。
   **刻意保持人工**：密码不落盘是红线。自检命令见手册步骤 3（**必须带 `node_env_path=Path('farm_node.env')`**）。

## 当前状态

- 本机 `C:\Users\admin`（= `LAPTOP-VS5F7HF4`）是当前唯一执行机 + 包源机 + 控制机，已装大厅 1.6.11.4。
- **本机真实是 150% 缩放 / 物理 2560×1600（DPI 144）**。报告或日志里见到 `96 DPI` / `1707x1067` 是未设 DPI 感知的**假值**，别当结论。
- 登录态：跑完停在**未登录**（登录套件最后一条会退出登录）；要复现已登录界面得先跑密码登录用例。
- 夹具现状：机器上**没装网易云音乐**；`C:\AsusMCenterDownload` 留 165MB 缓存（下轮复用）。
  7-Zip 更新夹具钉 26.02，`reset_fixture.py` 每轮自动压回起点。**起点干净可直接复跑。**
- 覆盖对照（组长表 sheet「1.6.11.2S」26 点）：补 11 / 合并 1 / 偏弱 1 / 漏或受阻 13。
  xlsx 在桌面 `华硕大厅-1.6.11.2S-覆盖对照.xlsx`（本机无 D 盘归档目录=受阻，不进 git）。

## 下一步 / 待办

1. **证据机制收尾**：①你回看确认可行后 `--evidence` 默认改 `failure-only`（只改 conftest 一处）；
   ②要「关键断言」列有内容得上步骤级 `step("...")` 显式记录（目前通过用例该列为空）；
   ③嫌 unit 纯逻辑用例截图吵，可在 conftest 按 marker 排除；④样例 run `reports/evidence/2026-09-17_1052/`(89MB)、
   `2026-09-17_1108/` 看过可删；⑤改 docstring 后要重跑一轮报告才刷新说明行。
2. **待报组长两处表前提差异**（非脚本缺陷，转发前自己先过一眼）：①行 29「原入口 GIF」——本机 1.6.11.4
   原入口图标加载期/稳态/hover 全程零帧变化，是静态图不是 GIF；②行 24「未同意协议 WB 入口正常」——
   未同意时产品把全部内容（含 WB 入口）门控在协议页之后，WB 入口根本不渲染，「正常」只在同意后才谈得上。
3. 密码登录那条仍要你自己终端设 `HALL_TEST_USER/PASSWORD` 补验（凭据不进对话，代理侧非 tty 自动 skip）。
   注册「真成功」happy path 用你那个一次性备用号，跑不跑由你定；假验证码无反馈那条是 `xfail`，
   跟开发确认后转正式缺陷或删用例。
4. **P2 后续**：正式安装包到了改 `config.local.yaml` 的 `security.package` 重跑（结论只代表本地 1.6.11.4S 包）。
   行 7-13 的**卡点已探针实测确认在服务端**（`reports/probe/update_link_probe.py`：点「立即检查更新」60s 零反馈、
   `C:\ProgramData\AppStore` 不创建、无包落地），等服务端对本机 SN 下发更高版本包后再补，行 12/13 最易。
   行 14 清单对比骨架已落地（`security.manifest` 一填即跑，没填自动 skip，oracle 已用真实包端到端验过）。
   WB 行 19/20/21/22/30 缺后台开关/多机型设备受阻。
5. 可选：`VersionLabel` 等无障碍缺陷（清单见 `项目知识库/探壳结论.md` 末尾）当产品问题提一条。
6. **多机跑批**：组长七问已全回，**口径表已进 `项目知识库/运行手册.md`「验证目标与组长口径」**（写断言前先看它）。
   控制通道走共享盘 `hall-farm` 已拍板并实跑通。`download.url_template` 仍留空（官方无稳定地址，填了只会 404）。
   待问话术见 `待问组长-话术.md`。
7. **新机拿代码走共享盘裸仓库**（2026-09-21 已落地）：搭建命令、push/pull 口径、**已验 / 未验清单**、
   第一台真机实测对照表，全在 `项目知识库/多机代码分发.md`。**发新机前重跑一遍那里的清单。**
8. **「发放版」清单的自动重生成（待你拍板）**：`交接归档/新机操作清单-发放版.md`（**仅本机**、已 gitignore）
   是把主文档 4 处密码占位换成真值的快照，**主文档一改它就旧** —— 本项目已有"手工同步靠记性必漏"的前科。
   两条路选一条：进 `tools/sync_handbook.py`（顺带管发布），或单写 `make_release_doc.py` 一键重生成。
   **在你拍板前，每次改完主文档记得手工同步它一次。**
9. ⚠️ **落点撞车（2026-09-21 17:01 发现，未处理）**：`C:\Users\admin\Desktop\新机操作清单.md`
   现在放的是**发放版**（含明文共享盘密码、banner 与归档那份略有出入），而它**正是 `sync_handbook.py` 的目标路径**。
   **下次跑不带 `--check` 的 `sync_handbook.py` 会把它覆盖成占位版**（旧版会自动留档为
   `新机操作清单-旧版备份.md`），发出去就成了一份密码是 `<共享盘密码>` 的废文档。
   **动手前先决定**：桌面那份改名/挪目录，还是让脚本直接生成发放版。
