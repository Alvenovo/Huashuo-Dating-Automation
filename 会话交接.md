# 会话交接

最后更新：2026-09-18

## 状态锚点（新对话先读这块）

- **焦点**：华硕大厅 1.6.11.4 客户端 UI 自动化（pywinauto + Win32 + pytest）。P0 / P1-A / P1-B / P1-C 全结，P2 安全验证 + WorkBuddy 入口已落地，证据留痕机制已落地。**多机跑批三期地基已落地（2026-09-18）：环境铺设 + 多机执行 + 自动下载/复位/常亮。**
- **仓库已转 private（2026-09-18 15:20）**：此前因 public 而滞留工作区的文档已全部入库并 push。
- **工作区干净（2026-09-18 17:05）**：多机就绪修复 + 共享盘供给脚本已全部 commit + push。
  `a1c97be`（多机就绪 5 处阻断修复）+ `4cd6e23`（provision_share.ps1）。
  远端 `origin/main` = `4cd6e23`，与本地一致。
- **机器规模已定（2026-09-18 15:47 组长）**：**一批 5 台、跑完再换下一批**，不是一次 20 台。
  → 「服务端并发上限」待问**关闭**（5 台规模下 `max_concurrent=5` 不触发，天然对齐）；
  → **批次调度代码暂不实现**（5 台以内直接 dispatch 即可；真需要时补也快），触发条件见运行手册。
- **安装包分发已定案**：组长明确**没有官方下载地址** → 走自建共享盘，已启用并端到端验证
  （`config.yaml` 填了 `package_share.dirs: ["//192.168.0.4/hall-packages"]`，
  共享盘放 1.6.10.7S + 1.6.11.4S 两个真包，392MB 实拷 `source=share` 通过）。
  **前提**：包源机器（你这台笔记本）要常开，长期跑建议换常开测试机。
- **上第二台机器的完整路径已打通（2026-09-18 17:05）**：唯一剩下的人工动作是
  **「以管理员身份跑一次 `tools\provision_share.ps1`」**（建 hall-farm 共享，实测非提权报「系统错误 5」，
  是绕不过的硬墙）。按完这一次，5 台机器共用，不必每台提权。
- **下一步第一件事**：
  1. （需提权，仅一次）`powershell -NoProfile -ExecutionPolicy Bypass -File tools\provision_share.ps1 -SharePassword "<密码>"`
     它会建 `hall-farm` 共享（节点要 **Change** 权限，给 Read 会让节点静默失败）+ 防火墙 445 + 农场四目录 + 网络自测。
     退出码 `0` 成功 / `2` 非提权 / `1` 有失败项；末尾打印 `RESULT: OK|NO_SHARE_ACCOUNT|EXCEPTION`。
     跑完 `HALL_FARM_ROOT` 才能指 `\\192.168.0.4\hall-farm`（临时验证仍可用本地 `C:/hall-farm`）。
  2. 机器到位后跑 `tools\bootstrap_machine.ps1` 铺环境（新机器**必须先跑它**，
     否则只会读到 `config.yaml` 的假路径 `C:/Users/ASUS/...` → 找不到包 → 降级下载 → 地址为空 → 失败）。
  3. 每台机器**手工填 `farm_node.env`**（bootstrap 只生成空模板）——注 `HALL_TEST_USER/PASSWORD/MS_USER`。
     不填 → 登录套件静默跳过（回执里 `credentials` 会标出来，不会假绿但也拿不到结论）。
     **这条建议保持人工**：红线是密码不落盘/不进 git，自动导出到文件会破坏它。
  4. 你人工回看几轮确认证据机制可行后，把 `--evidence` 默认从 `all` 改 `failure-only`（只改 `tests/conftest.py` 一处）。
- **待问组长只剩 1 条**（低优先级）：几台先跑、什么时候到位、装没装大厅。见 `待问组长-话术.md`。

## 第七期：上第二台机器的可执行性审计 + 共享盘脚本化（2026-09-18 17:05，已 push）

**起因**：用户问「当前脚本可以在另外一台测试机上跑了吗」→ 答「代码能跑，但实际跑不了」→
再问「必须人工吗，不能进脚本吗」→ 审计后分三层处理。

**结论：3 个阻断项里只有 1 个必须人工，且只需按一次。**

| # | 阻断项 | 处置 |
| --- | --- | --- |
| 1 | commit + push 17 项修复 | ✅ 已做（`a1c97be`） |
| 2 | `hall-farm` SMB 共享未建 | ✅ 脚本化 `tools/provision_share.ps1`，但仍需一次提权（`net share` 实测报「系统错误 5 拒绝访问」，非提权进程无解） |
| 3 | 新机器没跑 bootstrap | ✅ 本来就是幂等脚本，直接跑 |

**新增 `tools/provision_share.ps1`**（幂等）：建账号 → 建 `hall-packages`(Read) + `hall-farm`(**Change**) →
配 NTFS → 防火墙 445 → 农场四目录 → 网络路径自测。

**两个关键设计**：
- **两共享权限故意不同**：`hall-packages`=Read；`hall-farm`=**Change**。给 Read 会让节点
  rename `tasks/`、写 `done/`、推 `results/` 全部**静默失败**。
- **防假绿**：`Report()` 用 `$script:ok = $false`。PowerShell 函数内裸 `$ok = $false` 会创建
  局部副本并静默丢弃 → 脚本永远报 OK、退出码恒 0。同类假绿本项目 P0 层已踩过一次。

**顺带修 `bootstrap_machine.ps1`**：删死参数 `--skip-scale`（Python 侧没有，传了直接报错退出）；
补 3 个缺失透传 `--skip-share`/`--skip-farm`/`--no-download`。两侧现已 8 对 8 对齐（`--help` 核对过）。

**验证**：两个 `.ps1` 经 PS 5.1 解析器 `PARSE_OK`；`provision_share.ps1` 纯 ASCII（0 非 ASCII 字节）；
全量单测 **229 passed / 0 failed**。

**⚠️ 未验证（环境限制，非脚本缺陷）**：`provision_share.ps1` 的**运行时行为没实测过**——
安全策略封堵了所有「拉起子进程」路径（shell 方式调 PS、`Start-Job`、经 Python 拉子进程全被拒）。
**首次提权跑它时请留意输出**，尤其确认末尾 `RESULT: OK` 与退出码 0。


## 第六期：多机就绪审计 + 修复（2026-09-18 16:45，已 push `a1c97be`）

**起因**：用户问「检查一下当前脚本可以在其他测试机上执行了吗」→ 审计出 5 处阻断问题 → 用户说「帮我修复吧」。

**核心结论**：代码本身**早就是机器无关的**（`hall_auto/` 零硬编码绝对路径、仓库位置走 `$PSScriptRoot`、
机器差异都在 `config.local.yaml`、节点 id 走 `HALL_NODE_ID > config > 主机名`）。
**阻断问题全在「跑批链路」和「安全门禁」，不在被测代码。**

**修复清单**：

| # | 问题 | 后果 | 修法 |
| --- | --- | --- | --- |
| 1 | 任务文件里的 `env` 字段被 `farm_agent` **完全忽略** | 节点机上登录/SSO 套件静默跳过 | `hall_auto/env_pack.py`（新建）+ `build_suite_env` 三级优先级 |
| 2 | `reset_before_run()` **无门禁且顺序颠倒**（先卸载再取包） | 办公机误跑 → 大厅被卸且装不回 | 只读预检前置 + `HALL_ALLOW_INSTALL` 门禁 + `ResetBlocked`(rc=3) |
| 3 | `awake.py` 在 context 退出时**还原**常亮；无 RDP 保护 | 长套件中途息屏 → 黑屏假失败 | 还原挪 `atexit`；daemon 守护线程 5s 重设 + 检测会话丢失 |
| 4 | `machine_profile()` 缺 WebView2 / MS 会话字段 | 环境缺件被误判成产品缺陷 | `hall_auto/profile.py`（新建），唯权威实现，失败降级 `unknown` |
| 5 | `take_task()` 无去重、空套件照写成功回执；陈旧 `.running` 永久阻塞 | 假绿 + 节点假装无任务 | 去重拒收 / 空套件不写回执 / `_reap_stale_running()` |

**新增单测 48 条**（env_pack 15 + farm_agent 11 + profile 11 + reset/awake 改 11），
**181 → 229 passed / 0 failed**（已核实 junit xml）。

**端到端验证**：`C:/hall-farm` 本地全链路跑通 —— dispatch → `farm_agent --once` →
回执（含 `credentials` + 增强 `profile`）→ 证据回传 → `aggregate` HTML 带新列
（WebView2 / WV2 版本 / 微软会话 / 密码凭据 / SSO 凭据 / 共享盘）。

**真跑才发现的一个坑（写进运行手册了）**：上次进程被强杀留下 `tasks/<node>.json.running`，
导致后面所有任务 `rename` **静默失败**，节点一直报「无任务」而任务文件明明在 `tasks/` 里。
`_reap_stale_running()` 已修，并且异常时不再 `return None` 静默吞掉。

**另一处安全修正**：`fetch.py` 原来在 `url_template` 为空时回落到**猜的** CDN 模板（实测 404），
会让复位预检误判「包拿得到」→ 放行一次注定失败的卸载。
现在 `check_packages_available()` 只认**显式配置**的 URL，空模板判"拿不到"。

## 本轮新增：多机跑批（20+ 台）

**背景**：要推到 20+ 台异构测试机跑华硕大厅功能主流程。小硕知道暂不测。**当前手上还没有测试机**，代码先落地。

**第一期（环境铺设，已完成）**：
- 修 `tools/run_install_elevated.ps1` 写死 `$repo` → `Split-Path -Parent $PSScriptRoot`
- `requirements.txt` 补 `pywin32>=306`（`hall_auto/login.py` 直接 import win32gui/win32process）
- `hall_auto/dpi.py`：环境画像 + 交互式会话检测
- `hall_auto/evidence.py`：证据目录加节点前缀；summary 增 `node`/`profile`；报告页头显示环境画像
- `hall_auto/config.py`：`node_id` 字段（`HALL_NODE_ID` > config > 主机名）
- `tests/conftest.py`：`_env_precheck` 非交互态硬中止、`--skip-env-check`、`node`/`machine` fixture、报告头画像
- `tools/bootstrap_machine.ps1` / `.py`：一键铺环境，幂等
- `tools/collect_machine.py`：环境采集（`--json`/`--csv`）
- `machines.yaml` 台账模板、`待确认-组长问题清单.md`

**第二期（多机执行，已完成）**：
- `hall_auto/suites.py`：套件定义 + 并发预算 + 分片规划（**核心：只读类全并行 / 装装卸+登录限流 ≤5 / 人在环串行**）
- `tests/conftest.py` 新增 `--shard-id` / `--shard-count`（按收集顺序取模，实测 157 条切 2 片 = 79+78 不重不漏）
- `tools/farm_control.py`：控制机侧 `dispatch` / `status` / `aggregate`（出「节点 × 用例」矩阵报告）
- `tools/farm_agent.py`：节点侧 `--once` / `--loop` / `--local <suite>`
- 控制通道走**共享盘投递 + 轮询**（`HALL_FARM_ROOT` 指向农场目录），零新增服务

**已验证**：单测 **127 passed**（新增 26 条：dpi 11 + suites 15）；分片不重不漏；投递→执行→回传→汇总全链路本地模拟跑通；P2 安全套件实跑 4 passed/1 skipped。

**关键实测订正（重要，别再走错方向）**：
- **本机真实缩放是 150%（物理 2560×1600 / DPI 144），不是 100%。** 脚本经 `screen.py` 在 import 时设 Per-Monitor Aware，全套走物理像素坐标，**非 100% 不影响正确性**——P0/P1/P2 全部套件在此前提下实测跑通。
- **不要设 100% 缩放**：Win10 1703+ 缩放每显示器存储，`HKCU\Control Panel\Desktop\LogPixels` 本机**不存在**，写了不生效。相关代码已废弃。
- **读 DPI 前必须先设 DPI 感知**：不设读出 `96 DPI / 1707x1067`（假），Per-Monitor Aware 才读到真实 `144 / 2560x1600`。初版检测逻辑因此永远报「100%」，已修 + 单测锁定。
- **真正要门禁的只有会话状态**：锁屏/息屏/RDP 断连 → 抓屏黑屏、点击无落点 → 全套批量失败。

**待你定**：①控制通道是否就用共享盘投递（代码已按这个写了）。**有测试机后第一件事**：跑 `tools\bootstrap_machine.ps1` 铺环境，再用 `collect_machine.py` 采集配置汇总成表。

## 本轮新增：组长 7 问已回 + 第三期落地（2026-09-18 下午）

**组长答复（全 7 条）**：
1. 「功能正常」= **主流程功能测试正常即可**（不做细粒度功能点断言）
2. 验证目标 = **版本放量前的兼容性验证**
3. 20 台**机型不一样**；**不论装没装都先检测，有就先删掉重下**；**要求脚本在测试机自动下载安装包**
4. **先跑几台，后面再跑全部**
5. 测试机**无人碰屏**，脚本自己保证屏幕常亮即可
6. 服务端并发上限——**组长不清楚，当前未解决**
7. 测试账号 **1 个**，组长确认多机登录不踢

**第三期产出（代码已落地并验证）**：

- `hall_auto/fetch.py`（新建）：安装包获取。**本地 → `_cache` → 下载**三级查找，前三步不联网就不联网；走 `.part` 原子改名；配了 sha256 就校验；失败重试 3 次；只下 zh-CN。**下载地址 `config.yaml` 的 `download.url_template` 仍为空，等组长给地址**
- `hall_auto/installer.py`：新增 `resolve_setup()` / `resolve_setup_full()` / `reset_before_run()`（检测→卸载→备包）；`install_baseline_clean` / `upgrade_to_latest` 改走 `resolve_setup`，装不下时自动下载
- `hall_auto/awake.py`（新建）：屏幕常亮。`SetThreadExecutionState`（进程级、退出自动失效、不改电源计划、不需管理员），**已接进 conftest 的 `_env_precheck`，跑 pytest 自动生效**
- `tools/reset_machine.py`（新建）：跑批前复位工具，`--dry-run` / `--no-download` / `--json`；报告落 `reports/reset/`
- `tools/bootstrap_machine.py`：新增「安装包准备」步骤（`step_fetch_packages`），"目录不存在"从 fail 降级为提示（因为能自己下）
- `tests/conftest.py`：`_env_precheck` 变 generator 并在其中启用常亮；**顺手清掉 3 个重复定义/废弃的 `cfg` fixture**（原 125/142/147 行）
- `tests/unit/test_fetch_policy.py`（12 条）、`test_awake_policy.py`（6 条）、`test_reset_policy.py`（6 条），**新增 24 条**
- `config.yaml`：新增 `download` 块（`url_template` / `sha256` 映射，**当前均为空**）

**已验证**：单测 **154 passed**（131 原有 + 23 新）；`reset_machine.py --dry-run` 实跑正确识别已装大厅 + 3 个本地包。

**未解决 / 待确认（重要）**：
- ⚠️ **官方下载地址**：组长没给。`download.url_template` 仍为空。**但已用共享盘绕过**（见下）
- ⚠️ **服务端并发上限**：组长"不清楚"。已把问法改成讲清风险（假失败污染报告）再问一次，见《待问组长-话术.md》
- 先跑哪几台、机器到位时间待组长给

## 第四期：共享盘分发（2026-09-18 下午，实测通过）

**背景**：组长说没下载地址，用户决定自建共享盘分发安装包。

**实测结论：通了。** 本机 `LAPTOP-VS5F7HF4`（192.168.0.4，Wi-Fi + 公用网络）建 `hall-packages` 共享，
另一台机器用 `net use \\192.168.0.4\hall-packages /user:hallshare "<密码>"` 成功访问并列文件。

**最大的坑**：**空密码账号无法网络登录**。本机 `admin` 没密码 → 用 admin 访问一律失败，
且报错是"路径不存在"（不是权限错，极具迷惑性）。**解法**：建带密码的专用账号 `hallshare`。

**代码落地（`hall_auto/fetch.py`）**：查找链变成 **本地 → 缓存 → 共享盘 → 下载**。
- `share_dirs()` / `share_reachable()` / `ensure_from_share()` / `_copy_from_share()`
- **探测带 5s 超时**（UNC `is_dir()` 断网会卡 20s+，必须防）
- **UNC 异常是 `OSError` 不是 `FileNotFoundError`**，判断存在性必须 catch `OSError`
- 共享盘不可达 → 静默跳过、降级下载，**绝不因共享盘挂了让整轮失败**
- 支持多共享盘冗余；`HALL_PACKAGE_SHARE` 环境变量可临时覆盖
- `config.yaml` 新增 `package_share.dirs`（**当前为空，填上本机地址即可启用**）

**同步改**：`tools/reset_machine.py`（dry-run 显示共享盘状态 + "从共享盘取"动作）、
`tools/bootstrap_machine.py`（安装包准备步骤加共享盘）。

**验证**：单测 **168 passed**（154 → 168，新增 14 条共享盘策略）；
**真实端到端跑通**（真从共享盘拉 1.6MB 包，sha256 一致，二次命中缓存）；
降级路径实测（假地址 5s 超时 → 自动转下载）。

**完整步骤 + 4 个坑 + 排错表 + 实测场景表**已写入 `项目知识库/运行手册.md`。

**遗留**：本机留了 `C:\hall-packages` 共享 + `HallShare-SMB` 防火墙规则 + `hallshare` 账号（暴露面）。
用户未决定是否清理。**启用方法**：`config.yaml` 填 `package_share.dirs: ["//192.168.0.4/hall-packages"]`。

## 第五期：共享盘真正启用（2026-09-18 15:10，端到端验证通过）

**用户拍板**：先放两个真包，由我选。

- **选了 1.6.11.4S（目标版）+ 1.6.10.7S（基线版）**。弃用 `config.yaml` 里的 1.6.8.17S
  （跨 3 个小版本，升级链太长），弃用 1.6.9.x（中间版本无特殊意义）。
- 清掉 `C:\hall-packages` 里的测试垃圾（README + 7z），拷入两个真包（793MB）
- `config.yaml` → `package_share.dirs: ["//192.168.0.4/hall-packages"]`
- `config.local.yaml` → 补 `baseline_setup: myappstore_1.6.10.7S_Setup.exe`
  （原来只有 `latest_setup`，基线走的是 config.yaml 的旧值 1.6.8.17S）

**验证**：端到端真取包（屏蔽本地包 + 禁下载 → `source=share`，392MB 一致）；
降级路径（假地址 5.0s 超时返 None）；`reset_machine.py --dry-run` 输出正确。

**踩坑 + 修复**：填了 `package_share.dirs` 后 `test_share_dirs_empty_when_unset` 变红。
根因是 `tests/unit/test_fetch_policy.py` 的 `cfg` fixture 直调 `load_config()`，
**被本机 `config.local.yaml` 污染**（`load_config` 在 DEFAULT_CONFIG 路径下会 deep-merge 它）。
修复：fixture 里 `HALL_CONFIG` 指向临时空配置 + 清 `HALL_PACKAGE_SHARE`。
**规则：单测 fixture 调 `load_config()` 必须隔离本机配置，否则本机一改配置单测就红。**
已写进 `项目知识库/运行手册.md` 环境事实节。

**单测**：168 passed / 0 failed（junit xml 核实）。

**遗留**：本机共享 + `HallShare-SMB` 防火墙规则 + `hallshare` 账号保留（用户决定）。
`C:\hall-packages` 现在放的是**真包**（不是测试文件），正式跑批可直接用。

## 稳定知识去哪了

提权跑法、探壳结论（控件图 / 定位坑 / oracle 口径）、各套件命令、多机跑批打法、
安装包获取与共享盘细节、稳定环境事实已迁到 `项目知识库/`
（入口 `00-index.md`；跑命令 / 查环境看 `运行手册.md`；写断言前读 `探壳结论.md`）。
**本文件只留会变的。** 历史过程条目 1–19 在 `交接归档/会话交接-20260917.md`，
20–32 的过程叙事已压成下面「已完成」的结论行，细节看对应 commit message。
**遗留**：本机共享 + `HallShare-SMB` 防火墙规则 + `hallshare` 账号保留（用户决定）。
`C:\hall-packages` 现在放的是**真包**（不是测试文件），正式跑批可直接用。

提权跑法、探壳结论（控件图 / 定位坑 / oracle 口径）、各套件命令、稳定环境事实已迁到 `项目知识库/`（入口 `00-index.md`）。**写断言前读 `项目知识库/探壳结论.md`；跑命令 / 查环境看 `项目知识库/运行手册.md`。** 本文件只留会变的。历史过程条目 1–19 在 `交接归档/会话交接-20260917.md`，20–32 的过程叙事已压成下面「已完成」的结论行，细节看对应 commit message。

## 当前状态

- 本机 `C:\Users\admin` 是当前唯一执行机（**目标是扩到 20+ 台异构测试机**），已装大厅 1.6.11.4。
- **本机显示真实是 150% 缩放 / 物理 2560×1600（DPI 144）**。报告或日志里若见到 `96 DPI` / `1707x1067`，那是进程未设 DPI 感知读到的**假值**，别当结论。
- 登录态：跑完停在**未登录**（登录套件最后一条会退出登录）；要复现已登录界面得先跑密码登录用例。
- 夹具现状：机器上**没装网易云音乐**（注册表无条目、无残留进程，`C:\Program Files\NetEase\CloudMusic` 只剩两个 0 字节残留）；`C:\AsusMCenterDownload` 留着 165MB 安装包（缓存，下轮复用）。7-Zip 更新夹具钉 26.02，`reset_fixture.py` 每轮自动压回起点。起点干净可直接复跑。
- 覆盖对照（组长表 sheet「1.6.11.2S」26 点）：对照后已补 11 / 合并 1 / 偏弱 1 / 漏或受阻 13。xlsx 在桌面 `华硕大厅-1.6.11.2S-覆盖对照.xlsx`（本机无 D 盘归档目录=受阻，仓库公开不进 git）。
- git 身份本机没配（global 无 user.name/email），提交用 `-c` 临时按历史作者 `wwd <alven3330859445@gmail.com>`，没改任何 git config。

## 已完成（只留结论；过程见 commit message 与归档）

- **P0** 全层通过；P0-05 探明无登录墙，跳过。
- **P1-A** 真装真卸 + 点更新端到端通过（0 人工，走 `HallAutoP1` 计划任务）；sync 夹具裁定为只读断言用例并通过，`fixture_apps.sync` 永久留空。
- **P1-B** 全结：密码 / 短信（人在环）/ 忘记密码往返改密码（用户终端）/ 微软 SSO 免密 / 注册格式校验。
- **P1-C** 主题三档通过，收尾还原原档位。
- 等待机制 sleep→条件轮询、首页停留长根因修复（就绪循环重构 + handle/pid 缓存）：`3f661bf` / `60b5bfb`，已 push。
- **P2** 安全验证 4 项无人值守（`df48e8e`）；WB 非破坏 6 点 + 行 25 破坏往返 + 行 29 静态采样 + 行 24 隐私协议门控（`1a12f71`），已 push。
- **证据留痕**（`8931a88` / `62a92fe`，**已 push**——2026-09-18 核实两提交均为 HEAD `91f8086` 祖先，此前"未 push"记录有误已订正）：每用例终态截图 + result.json + 自包含 report.html，`--evidence` 开关；报告已补用例中文说明行（取 docstring 首行）、行级配色、skip 原因副行（按 marker 分类 + `pytest.skip()` 原文）。

## 下一步 / 待办

1. **证据机制收尾**：①你回看确认可行后 `--evidence` 默认改 `failure-only`（只改 conftest 一处）；②要「关键断言」列有内容得上步骤级 `step("...")` 显式记录（目前通过用例该列为空）；③嫌 unit 纯逻辑用例截图吵，可在 conftest 按 marker 排除；④样例 run `reports/evidence/2026-09-17_1052/`(89MB)、`2026-09-17_1108/` 看过可删；⑤改 docstring 后要重跑一轮报告才刷新说明行。
2. **待报组长两处表前提差异**（非脚本缺陷，转发前自己先过一眼）：①行 29「原入口 GIF」——本机 1.6.11.4 原入口（小硕知道）图标加载期/稳态/hover 全程零帧变化，是静态图不是 GIF；②行 24「未同意协议 WB 入口正常」——未同意时产品把全部内容（含 WB 入口）门控在协议页之后，WB 入口根本不渲染，「正常」只在同意后才谈得上。
3. 密码登录那条仍要你自己终端设 `HALL_TEST_USER/PASSWORD` 补验（凭据不进对话，代理侧非 tty 自动 skip）。注册「真成功」happy path 用你那个一次性备用号，跑不跑由你定；假验证码无反馈那条是 `xfail`，跟开发确认后转正式缺陷或删用例。
4. **P2 后续**：正式安装包到了改 `config.local.yaml` 的 `security.package` 重跑（结论只代表本地 1.6.11.4S 包）；安全包验证正向/逆向/Hash/升级 8 条（行 7-13）**卡点已探针实测确认在服务端**（2026-09-17 `reports/probe/update_link_probe.py`：点「立即检查更新」60s 零反馈、`C:\ProgramData\AppStore` 不创建、无包落地 → SN 未下发更高版本包，本地无法绕过），等服务端对本机 SN 下发更高版本包后再补，行 12/13 最易（版本复位已具备）；**行 14 清单对比骨架已落地**（`test_p2_security.py::test_manifest_matches_package` + `security.manifest`/`manifest_scope` 配置 + `tests/unit/test_manifest_compare.py`）：开发清单一到在 `config.local.yaml` 填 `security.manifest`（一行一个相对包根路径）即跑，没填自动 skip；oracle 已用真实包端到端验过（正确清单 PASS、故意错清单 FAIL 且双向差异报得准）；WB 行 19/20/21/22/30 缺后台开关/多机型设备受阻。
5. 可选：`VersionLabel` 等无障碍缺陷（清单见 `项目知识库/探壳结论.md` 末尾）当产品问题提一条。
6. **机器路径带 Windows 用户名进公开仓库**这个老决策仍 open（本轮又新增 `待确认-组长问题清单.md`、`machines.yaml`）。2026-09-18 核对：仓库经匿名 clone 实测确认 **public**；且**已 push 的 `主流程自动化设计.md`、本文件本来即含 `C:\Users\admin`**，故"路径不进公开仓库"实际是半做半没做、口径不一致。三选项待你定：**A** 真去路径化（已推文档改 `$PSScriptRoot`/相对路径，改造后 `AGENTS.md`/`学习库/` 一并入库；**`run_install_elevated.ps1` 本轮已改成 `$PSScriptRoot`，下轮 P0 要复跑验一次**）；**B** 维持现状只挡新；**C** 承认本机用户名不敏感，全推（需你明确推翻先前决策）。
7. **多机跑批**：组长 7 问已全回（见上节）。③控制通道已按共享盘投递实现并**已拍板**（共享盘 `hall-farm`，见第七期）。**原「最大卡点：没有下载地址」已绕过**——改走自建共享盘 `hall-packages`，端到端验证通过（`source=share`）。`download.url_template` 仍留空（组长确认官方无稳定地址，填了也只会 404）。待问话术见 `待问组长-话术.md`（含已确认/待确认两栏）。
