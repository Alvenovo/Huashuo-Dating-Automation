# 会话交接

最后更新：2026-09-16

## 当前任务

本机（`C:\Users\admin`）是唯一执行机，已装 **1.6.11.4**。P0 全层（01/02/03/04/06/07）已在本机通过；P0-05 探明无登录墙（未登录态跑通搜索/详情），跳过。**P1-A 全通过**（17:04 提权那轮 3 过 / 138s：三个列表刷新 + 真装真卸网易云 + 7-Zip 点更新升级，提权走 `HallAutoP1` 计划任务，全程 0 人工）。**P1-B 账号密码登录 + 注册格式校验通过**（3 过 / 69s、10 过 1 xfail / 186s）。**P1-C 主题切换通过**（17:20 那轮 2 过 / 118s，切深色立刻生效、重启后保持，收尾还原原档位）。剩 P1-B 的**微软账号**一条（短信登录、忘记密码均已结：短信见第 22 条，忘记密码往返改密码 2026-09-16 用户终端实跑通过见第 23 条）、sync 夹具未定；本轮改动已本地 commit `277c91d`（未 push）。**等待机制改造（sleep 改条件轮询，第 20 条）+ 就绪循环重构（首页停留长的根因修复，第 21 条）已于 2026-09-16 提交为本地 commit `3f661bf`（未 push），提交前查过 diff：无真测试号、无密码值、无凭据赋值。**

## 本轮做完的事

1. 安装层 P0-01/02 本机管理员重验通过：卸 1.6.11.4 → 基线 1.6.8.17 → 升回 1.6.11.4，77s；注册表 `DisplayVersion` 与 exe `FileVersion` 一致。
2. 版本核对加第三路：软件内「设置 → 关于」页版本。新增 `hall_auto/about.py` + `tools/ocr_text.ps1`（Windows 自带 OCR，无新依赖），P0-01/02 现在断言三路；`read_about_version` 单独实跑读到 1.6.11.4。
3. 修了首跑两个坑（见探壳结论），冒烟复跑 4 过 / 154s。
4. P1-B 账号密码登录 + 退出登录已自动化，实跑 3 过 / 69s（`tests/launch/test_p1_login.py`，标记 `login`）。登录模块 `hall_auto/login.py` 一律按 **pid 跨窗口**找控件：登录弹窗、已登录后的用户菜单都是独立顶层窗，会顶掉缓存的主窗口句柄，按主窗口 wrapper 找必报 `ElementNotFoundError`。
5. P1-A：「我的」页三个列表（更新/卸载/同步）检测用例通过。新增 `hall_auto/apps.py` + `run_p1_apps.ps1`。
6. P1-A **向导点不动的根因已定位并修好**（更正第 6/7 条旧结论）：厂商安装器是 **NSIS**，有一个标准 Win32 对话框 `#32770`「网易云音乐 安装」，底下 17 个子控件用 `WM_GETTEXT` 全能读到文字（`关闭(&L)`/`取消(&C)`/`< 上一步(&P)`）。**UIA 对它完全是瞎的**（返回 0 个 descendants，还把每个子控件误报成独立顶层窗），之前几轮一直往 UIA 上撞，方向错了。而且**非提权就能读到、能点**，旧文档说「必须提权才看得见向导」也是错的。
7. 据此把 `hall_auto/apps.py` 的向导层整段换成 Win32：`installer_dialogs`/`installer_pids` 按「映像在下载目录 + 窗口类是 `#32770`」或「标题含应用名 + 安装类关键词」认对话框（排除大厅自身，也排除绿色应用的窗口）；`wizard_buttons` 枚举 Button 子控件；`pick_wizard_button` 挑按钮 —— 文字命中白名单优先，否则认「可见可用、无文字、面积 ≥ 12000」的自绘主按钮（网易云「立即安装」是 360x60=21600，右上角关闭叉只有 36x36=1296）；`drive_vendor_installer` 先 `BM_CLICK`，按钮签名没变再用真鼠标点同一坐标。已对**在跑的真安装器**验证识别正确（选中 360x60 主按钮，没选关闭叉）。`button_label` 要连 `(&L)` 一起剥掉，否则白名单永远不命中（单测抓到过）。
8. 卸载列表刷新兜底**已验证生效**：装完当场「我的→卸载应用」列表里没有新装应用（列表是进页面时的旧快照），`uninstall_item_button` 原地等 40s → 退回「推荐」重进「我的」逼页面重拉，本轮实跑找到了卸载按钮，**不算产品缺陷**。
9. wrapper 跑 pytest 前先执行 `tools/reset_fixture.py`：先按 `installer_pids` 杀掉上一轮残留的安装器进程（这种进程**非提权 taskkill 报拒绝访问**，实测 pid 19444 杀不掉），再按注册表 `UninstallString` 加 `/S` 静默卸掉夹具应用，保证干净起点。
10. 单测 84 过（新增 4 条 `pick_wizard_button` 策略回归 + 3 条 `is_installer_dialog` 判据，按钮表都是 2026-09-15 从真安装器/真卸载器实抓的）。
11. **卸载卡死的根因**（14:24 那轮：装成功、卸超时）：大厅点「卸载」只是拉起**厂商卸载器**，真卸要在它自己的对话框里再确认一次，而驱动认不出那个窗口 —— 标题是「网易云音乐 卸载/ENABLECANCEL」（没有安装类关键词），映像被 NSIS 解包到 `%TEMP%\~nsuA.tmp\Au_.exe`（不在下载目录），两条旧判据全落空。
12. **更险的一点**：卸载确认页把按钮画成两块**尺寸位置完全相同**的自绘块（都是 360x60=21600，一个 (663,674) 一个 (1041,674)）。提权截图 + OCR 认出来是**左「再想想」（取消）、右「狠心卸载」（确认）**。而树里那两个隐藏的文字按钮是 `卸载(&U)` 在左、`取消(&C)` 在右 —— **跟自绘块的左右正好相反**，按位置猜必然点到取消，还会表现成「点过了但没卸掉」。修法：不猜几何，改点被皮肤藏起来的 NSIS 文字按钮 `卸载(&U)`，`BM_CLICK` 对隐藏按钮照样有效（提权实测 3s 后注册表条目消失）。据此 `pick_wizard_button` 的优先级改成「可见文字 → **唯一一块**自绘大按钮 → 隐藏文字」，两块以上自绘并存时一律返回 None 不猜；`is_installer_dialog` 加了卸载类关键词和 NSIS 临时映像两条判据；`drive_vendor_installer(drawn_ok=False)` + 新增 `dismiss_vendor_dialogs` 在装完/卸完把残留对话框点掉（收尾**只点有文字的按钮** —— 收尾页那块自绘大按钮常是「立即体验」，点下去会把刚卸掉的应用又拉起来）。
13. **端到端实跑通过**：14:52 `MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`，2 过 / 101s，`admin=True`，`reset_fixture` 先杀掉了 14:34 残留的卸载器 pid 1996。跑完机器干净：注册表无条目、无残留厂商对话框/进程、`Program Files\NetEase\CloudMusic` 只剩 `debug.log` 和 `isc_chat.dat` 两个 0 字节残留（厂商自己的行为，注册表才是 oracle）。
14. **冒烟层复验**（2026-09-15，`config.py` 10:58 / `pytest.ini` 11:02 / `config.local.yaml` 14:13 改动之后首次复跑）：单测 84 过 / 0.38s，P0-03/04/06/07 **4 过 / 162s**。P1-A 的 `apps.py` 最后改动就在 14:52 那轮，结论仍有效；**P1-B 登录套件尚未在这些改动之后复验**（要 `HALL_TEST_USER`/`HALL_TEST_PASSWORD` 才能跑密码登录那条）。
15. **可更新夹具找到了：7-Zip**（2026-09-15 15:51 提权探针，50s 跑完，机器已还原干净）。三条实测结论：①官方安装器 `/S` 静默装可用，rc=0，注册表 `HKLM\...\Uninstall\7-Zip`、`DisplayVersion=26.02`、`InstallLocation=C:\Program Files\7-Zip\`，而且**自带 `QuietUninstallString`**，复位能直接走注册表、不用写特例；②**大厅认外部装的老版本**，启动后第一轮更新列表就出现 `['Python3', '7-Zip（64位）', '飞书']`；③静默卸载把 `C:\Program Files\7-Zip` 整个目录删干净（比网易云还干净，网易云留两个 0 字节文件）。探针脚本 `reports/probe/update_fixture_probe.py`，日志同名 `.txt` + `update_fixture_run.log`，更新列表截图在 `reports/launch/20260915_155219_update_list_round1.png`。**没点「更新」按钮**，那一步还没验。
16. **P1-B 登录套件复验 + 注册页探壳**（2026-09-15 16:09～16:21，用户提供了测试号）：
    - 登录套件在 `config.py`/`pytest.ini` 改动之后复跑 **3 过 / 69s**，跑完停在未登录态。密码只走环境变量，代码里没有打印密码的路径（`login.py:129` 只读不打，skip 消息不带值），唯一会进日志的是手机号尾号。
    - **注册页是客户端内 WPF 窗口**（不是网页，UIA 全可读，不需要 Playwright），控件图见探壳结论。
    - **探针 3 的「没有实时格式校验」结论不完整**：当时没填手机号/验证码、没勾协议，`RegisterBtn` 本来就灰，分不清原因。探针 4 补完，见第 17 条。
    - **客户端内确认没有注销账号入口**：用户菜单只有 `QuitBtn`（退出登录），设置页清单里也没有账号安全/注销。所以"注册出来的号在客户端删不掉"成立。
    - 探针：`reports/probe/register_probe.py`（控件图 + 设置页清单）、`register_validation_probe.py`（提示节点多种读法 + 用户菜单）、`register_tip_probe.py`（整屏抓图 + 裁剪 + OCR + 控件清单 diff），输出同名 `.txt`，截图在 `reports/probe/case*_*.png`。
17. **注册格式校验已自动化，一条短信没发**（2026-09-15 16:34 探针 4 + 16:52 用例实跑 10 过 1 xfail / 186s）：
    - 探针 4（`reports/probe/register_submit_probe.py`）用公开示例号 `13800138000` + 假码 `000000` + 勾协议，**全程不点 CodeSendBtn**（用户批准过这一步）：`RegisterBtn` 立刻 enable → 按钮灰的真相就是「字段没填全或协议没勾」，不是必须先真发验证码。**方案 A 成立，一次性备用号不用烧**。
    - 客户端**有**实时格式校验，反馈是两件事：`RegisterBtn` 变灰 + 对应提示行**变红**（密码格式错红第一行，确认密码错/不一致/为空红第二行，7 组非法数据逐条对上）。
    - **疑似缺陷**：假验证码 + 合法密码点「注册并登录」，2/4/8s 三个时间点都**无任何反馈**（无 toast、无红字、无新文字节点、窗口不关、按钮不变灰）。用例里写成 `xfail(strict=False)`，待与开发确认。
    - 落地：`hall_auto/screen.py`（整屏 BitBlt 抓图 + 红像素计数）、`hall_auto/register.py`（开注册页/填表/勾协议/按钮态/红提示）、`tests/launch/test_p1_register.py`（11 条，标记 `login`）。红提示 UIA 读不到颜色，按提示节点矩形截屏数红像素：灰=0、红≈900～6300，阈值 50。
18. **P1-A 更新用例落地，一次通过**（2026-09-15 16:54 探针 5 + 17:04 套件 3 过 / 138s）：
    - 探针 5（`reports/probe/update_click_probe.py`，提权）回答了三问：**更新不是静默的** —— 大厅把新包下到 `C:\AsusMCenterDownload`（实测 `7z2603-x64.exe`）再拉起，弹**英文 NSIS 向导**「7-Zip 26.03 (x64) Setup」（`Install`/`Cancel`/`...` 选目录），点 Install 后注册表 26.02→26.03、`7zFM.exe` FileVersion 26.2.0.0→26.3.0.0、条目从更新列表消失；装完向导停在收尾页不自己关。截图 `reports/probe/update_dialog_3673016.png`（能看到大厅那行按钮变成「安装中」）。
    - 用例 `test_update_fixture_via_hall`（`apps` + `destructive`）：起点靠 `tools/reset_fixture.py` 新增的 `reset_update_fixture` 压回钉住版本（卸现有 → 静默装 SHA256 校验过的 26.02 包）；点「更新」后 `drive_vendor_installer` 驱动向导；**目标版本运行时从向导标题读**（`wizard_target_version`，不写死 26.03），断言注册表 == 标题版本、FileVersion == 四段形式（`version.four_segment`）、条目离开更新列表。
    - 配套改动：config 新增 `update_fixture` 块（name/registry_hint/pinned_version/package/sha256/url/install_dir/version_exe，仓库默认空、本机填 7-Zip）；`apps.py` 新增 `uninstall_entry`（按 DisplayName 包含读注册表，目录名和注册表名不是一回事）、`installer_titles`、`update_item_button`（和卸载按钮同款旧快照兜底），`WIZARD_BUTTONS` 补英文 `Install`/`Finish`/`Close`（放在中文后面，中文向导仍优先中文）。
    - 跑完机器状态：7-Zip 停在更新后的 26.03（**这是用例的正常终态**，下轮 wrapper 的 reset 会压回 26.02）；网易云音乐已卸；无残留向导/进程。复跑一行不变：`MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`。
19. **P1-C 主题切换已自动化**（2026-09-15 17:16 探针 6 + 17:20 用例 2 过 / 118s）：
    - 探针 6（`reports/probe/theme_probe.py`，日志同名 `.txt` + 7 张 `theme_*.png`）摸出三条：①三档是 **RadioButton**，AutomationId 分别 `LightMode`（浅色）/`DarkMode`（深色）/`AuroMode`（跟随系统，**本机原档位**）；②选中状态 `is_selected()` 能读（SelectionItem 模式），**Toggle 模式不支持**（`NoPatternInterfaceError`），legacy State 是 1048592/1048576 这种位掩码不好用；③**外观断言用主窗口平均亮度**：浅色 227～255、深色 41～53，差五倍以上，阈值 128 稳。切换**立刻生效不用重启**，深色**重启后保持**。
    - 坑：按名字找会撞到同名 **ToolTip** 节点（点完之后大厅会冒一个名字也叫「深色」的 ToolTip），所以 `settings.py` 只按 `RadioButton` + AutomationId 找。点选走 `select()`（SelectionItem 模式，不依赖坐标），坐标点击只当兜底 —— 主题三档隔壁就是「点击关闭按钮」两档，点偏了会改掉关闭行为。
    - 新增 `hall_auto/settings.py`（`open_settings`/`ensure_settings`/`theme_nodes`/`theme_selected`/`set_theme`/`main_luminance`）、`screen.mean_luminance`、`tests/launch/test_p1_theme.py`（新标记 `settings`）。用例两条：三档控件齐不齐；切深色→断言选中+变暗→杀进程重开→断言档位和外观都还在→切回浅色→断言变亮且明暗差 >50。**收尾在 fixture teardown 里还原成进页面时的档位**，重启会换 pid/main 句柄，所以用 `Session` 持有可变引用。
    - 跑完机器状态：主题还原成 `跟随系统`（探针把它改成了浅色，事后单独跑一段还原脚本压回来，已确认 `theme_selected` = 跟随系统、大厅进程已退）。
20. **等待机制改造：固定 sleep 改条件轮询**（2026-09-15 17:33～18:15，未提交，等用户看 diff）：
    - 新增 `hall_auto/waiting.py`：`wait_until(predicate, timeout_sec, interval)` 返回 bool（调用方自己收场）+ `wait_until_or_raise(..., message)` 超时报错写清等的什么条件；谓词抛异常按未就绪处理（页面切换中途 UIA 读半截树是常态）；默认超时 10s、间隔 0.3s，不自动重试。`installer._wait_until` 委托共享版，间隔 2s→1s。
    - 页面死等全部改掉：搜索框写入后读回值确认（`search._edit_value`，ValuePattern 读不回时退化 0.3s 固定等）；`open_detail` 点完的 sleep(3) 直接删（调用方本来就轮询详情态）；切「我的」页签从 sleep(2) 改成按页签等条件 —— 更新/卸载页等自己的列表容器出现，**同步页等更新/卸载容器消失**（防止把上个页签的 ListItem 误当同步页数据）；登录弹窗/页签切换（`switch_login_tab` 新增 `ready` 谓词参数，两处调用分别等 PasswordSecInput / CodeInput）、退出登录、关于页两步跳转、设置页打开、主题选中、注册页打开、注册填表（读回值或掩码变化即算写入）全部条件化；各轮询循环间隔 1~2s→0.3~0.5s。
    - 安装器侧只缩间隔、超时上限不变：装/卸/更新主循环 5s/3s→1s，向导点击后 sleep(2) 改成轮询「关窗或按钮签名变化」（上限 2s），`dismiss_vendor_dialogs` 轮间 sleep(3) 改成轮询「对话框已消失」（上限 3s），`_leave_mine` 改成轮询等 Mine 容器消失。
    - **验证第一遍全绿**（18:00 前后）：单测 88 过；P0 冒烟 4 过 / **154s**（基线 162s）；P1-A 检测 1 过 / 45s；P1-A 提权全套 3 过 / **157s**（基线 138s，本轮 reset 多压了一次 7-Zip，装卸耗时本身有波动）；注册 10 过 1 xfail / **197s × 2 遍**（基线 186s，两遍完全一致，+11s 稳定但来源未定位，疑启动阶段波动，非等待回退）；登录（无凭据）1 过 2 skip / 42s；主题 2 过 / **116s**（基线 118s）。
    - **残留风险**：①`login_with_password` 的协议勾选等待和账密页签谓词这轮没跑到（凭据不在环境变量），要补验得用户自己终端设 `HALL_TEST_USER/PASSWORD` 跑登录套件；②P0 安装层（`installer.py` 的间隔改动）没跑，改动仅轮询间隔、逻辑未变，下轮 P0 复跑覆盖即可；③注册套件的 +11s 待第二遍对照。
    - 约定：**不 commit**，等用户看 diff；第二遍全套 + 耗时对比另约时间（跑前用户打招呼）。
21. **就绪循环重构：首页停留长的根因修复**（2026-09-16 09:40～10:05，未提交，与第 20 条同一份 diff 等用户看）：
    - 根因（探针实测）：`wait_until_ready` 每轮要 ~16s —— `_overlay_windows` 一轮被调 3 次（每次含 `Desktop(backend="uia").windows()` 全系统 UIA 枚举 ~1.0s + 每个覆盖层的 Button 子树遍历）、全树 `descendants()` 走 2 次（各 ~1.4s）、轮间还 sleep 1s。页面早就绪了，检测要等一整轮才看见。
    - 改造（5 点，全部落地）：①每轮只枚举一次覆盖层，白名单/未知弹窗判定复用同一份 `Overlay(name, node, first_run)` 列表（first_run 只对非白名单**顶层窗**算，子 Window 不再走昂贵的 Button 遍历；`unknown_popups` 函数删除，折进循环）；②顶层窗枚举换 Win32 `EnumWindows` + pid 过滤（实测 0.00s，UIA Desktop 枚举 0.99s），命中的 hwnd 才包成 `UIAWrapper(UIAElementInfo(hwnd))`；③`_auto_ids_present` + `_webview_ready` 合并成一次全树遍历 `_structure(main, settings) -> (aids, webview)`；④轮间 sleep 1s→0.5s；⑤`_back_if_subpage` 三级兜底：aid=BackBtn → 名叫「返回」的按钮 → 侧边栏「推荐」ListItem（详情页没有 BackBtn，之前会烧满 ready_sec 150s 报超时）。
    - 新增 `hall_auto/winapi.py`：通用 Win32 原语（`_user32` ctypes 声明、`_top_hwnds`/`_child_hwnds`/`_hwnd_pid`/`_hwnd_class`/`_hwnd_text`/`_hwnd_rect`/`_hwnd_alive`/`_hwnd_visible`）从 `apps.py` 抽出来，`apps.py`/`launch.py` 共同导入；`HOME_TAB_NAME`（「推荐」）从 `apps.py` 移到 `launch.py`，apps 反向导入。
    - **踩到一个坑**：Win32 `EnumWindows` 会把进程的**隐藏消息窗**（`Default IME`、`MSCTFIME UI`、`.NET-BroadcastEventWindow`、`Hidden Window` 等）也列出来，UIA 的 Desktop 枚举不会 —— 第一次跑探针全被当成「未知弹窗」直接 raise。修法：顶层窗过滤加 `IsWindowVisible`。这条写进探壳结论级别：**换 Win32 枚举必须自己过滤可见性**。
    - 验证：单测 88 过；一次性计时探针（已删）GREEN —— 主窗口→就绪 **11.4s**（SearchBarInput 9.0s 出现，检测滞后仅 ~1.5s ≈ 一次全树遍历的理论下限；加载期 overlay 遍历 3.1s 是 UIA 对忙碌进程的固有延迟，被应用加载时间掩盖）；P0 冒烟两遍 **172s / 155s**（基线 154s，第一遍偏慢是冷启动波动，test_p0_04 单用例 95s 全是应用启动时间）；P1-A 检测 1 过 / **38.7s**（改造前 45s）。
    - 探针 `tools/time_home_ready.py`（[DEBUG-h7k3]）用完已删；`tools/probe_uia.py` 已适配新签名（`_overlay_windows(main, cfg.launch)` 返回 `Overlay` 对象）。
    - **残留风险**：覆盖层枚举改了实现，未知弹窗兜底网靠 P0/主题/注册套件的正常通过间接验证过，但「真弹一个未知窗」的场景没有专门用例；P1-A 提权全套、登录、注册、主题四个套件尚未在这次重构之后复跑（属第二遍全套的范围）。
    - **第二遍全套已复跑，全绿**（2026-09-16 10:04～10:16）：P1-A 提权 3 过 / **146s**（基线 138s，`admin=True`，起点自动压回：网易云本来就没装、7-Zip 26.03 静默卸后重装 26.02）；注册 10 过 1 xfail / **187s**（基线 186s，第一遍的 +11s 确认是启动波动）；主题 2 过 / **102s**（基线 118s）；登录（无凭据）1 过 2 skip / **34s**（基线 42s）。加上当天早些的 P0 冒烟两遍 172s/155s（基线 154s）和 P1-A 检测 38.7s（基线 45s），验收达标。密码登录那条仍要用户自己终端设 `HALL_TEST_USER/PASSWORD` 补验。
22. **P1-B 短信登录已结：人在环实跑通过 + 用例固化**（2026-09-16 10:30 前后，未提交）：
    - 实跑流程（三段脚本在 `reports/probe/`：`sms_login_phase_a.py` 开弹窗到短信页签 / `sms_send_code.py` 勾协议点发送 / `sms_login_phase_b.py` 回填登录）：人工填手机号（尾号 1935）→ 脚本点「发送验证码」→ 人工把收到的码发代理 → 脚本回填 `CodeInput`、点「登录」→ **进入已登录态**（用户区显示「已登录用户：61935」）→ 退出登录收尾，机器回到未登录干净态。全程一条命令没让用户跑。
    - 实测发现：**点完「发送验证码」后 `CodeSendBtn` 节点从控件树消失**（倒计时态不暴露按钮），发送成功断言不能靠按钮签名，已写进探壳结论。
    - 固化：`hall_auto/login.py` 新增 `request_sms_code`（填号/勾协议/点发送）+ `login_with_sms_code`（回填/提交/等已登录），协议勾选抽成 `_check_agree` 与密码登录共用；`tests/launch/test_p1_login.py` 新增 `test_sms_login_manual`（标记 `login` + 新标记 `manual`）。**防呆**：用例先查 `HALL_TEST_USER` 再查 `sys.stdin.isatty()`，无人值守（非 tty）自动 skip，不会出现「短信发了没人回填」；验证码经 `input()` 回填，不落盘不进 git。
    - 手机号本轮没进对话（人工自己填在弹窗里）；验证码是一次性短码，过期即废。

23. **P1-B 忘记密码已结：往返改密码用例用户终端实跑通过**（2026-09-16，未提交）：
    - 发送链路实跑（探针在 `reports/probe/`：`forgot_probe.py` 探页面控件图 / `forgot_send_code.py` 填号点发送）：人工点「忘记密码」→ 登录窗口**原地换页**（客户端内 WPF 页，不是新窗、不是网页）→ 人工填手机号（尾号 1935）→ 脚本点「发送验证码」→ `CodeSendBtn` 消失 → **手机收到重置短信**（用户确认）。这一轮**走到发送为止没点提交**。
    - 用户随后指出「停在发送验不到重置本身」，据此把用例升级成**真往返改密码**：OLD→NEW 重置 + 用 NEW 重新登录验证 + NEW→OLD 还原 + 用 OLD 登录验证，账户最终回到原状、`HALL_TEST_PASSWORD` 不用改、可重复跑。
    - 页面控件图（写进探壳结论）：`MobileInput`（手机号，注意登录弹窗是 `MobileInputBox`、注册页才叫 `MobileInput`）/ `CodeInput` / `CodeSendBtn` / `PasswordSecInput` / `RePasswordSecInput` / 提示行 `PasswordInputTip` / `RePasswordInputTip` / 提交 `SetBtn`（name「提交」）/ 关闭 `CloseBtn`；**没有协议勾选框**。
    - 固化：`hall_auto/login.py` 新增 `open_forgot_password_page`（**幂等**：已在该页直接返回，方便还原程重新导航）+ `request_forgot_sms`（填号/点发送/等按钮消失）+ `submit_forgot_reset`（填码/新密码×2/点「提交」，**按 name「提交」精确挑按钮避开 SetBtn 撞车**，成功信号=页面关或自动登录）+ `login_with_password_value`（按显式密码登录，给往返验证用，`login_with_password` 改成它的薄壳）；`config.py` 新增 `new_password()`（只读 `HALL_TEST_NEW_PASSWORD`，绝不落盘）；`tests/launch/test_p1_login.py` 的 `test_forgot_password_reset_manual`（标记 `login`+`manual`）用 **try/finally** 把还原程兜住——只要第一次提交发出去了，哪怕新密码验证报错也一定把密码压回 OLD，账户不会卡在 NEW。collect 5 条、单测 88 过。
    - **为什么只能用户自己终端跑**：改密码要原密码 + 新密码，两者**不能进对话**（红线），代理 shell 又拿不到用户终端的环境变量；两个验证码发到用户手机、`input()` 要 tty（代理侧非 tty 自动 skip）。**2026-09-16 用户在自己终端实跑往返全程通过**：OLD→NEW 重置 → 用 NEW 登录验证 → NEW→OLD 还原 → 用 OLD 登录验证，账户已回到原密码。此前唯一没观测过的「提交后页面行为」风险已消——成功判据「页面关或自动登录」没误报，finally 还原程也正常走完。
    - **两个坑/缺陷候选**：①入口必须 `Button` + 全名精确匹配 `_by_name(pid,"Button","忘记密码",exact=True)`——密码提示文案「…可点击忘记密码进行重置」也含这四字，模糊匹配会点到 Text 上毫无反应（实测踩过）；②`SetBtn` 不能当页面判据——主窗口导航栏「设置」按钮 aid **也是** `SetBtn`（探壳结论级）。③忘记密码页在主窗口树里的容器名是「**注册窗口**」（容器复用、名字没改，无障碍缺陷候选，和 VersionLabel、注册假码无反馈一起报开发）。

## 提权怎么跑（本机已验证的路）

- 代理侧无法自我提权：自己建 `/RL HIGHEST` 计划任务被拒绝访问；键鼠注入进不了交互桌面（只能截图）。UAC 的「是」画在安全桌面上，非提权进程碰不到 —— 这是唯一绕不过去的硬墙。
- **为什么 P1-A 必须提权**：夹具应用（网易云音乐）要写 `C:\Program Files\NetEase\CloudMusic` 和 HKLM 卸载表。另外厂商**卸载器本身也是提权进程**（实测 pid 1996 `TokenElevation=1`），非提权点不动它。
- **UIPI 的边界（本轮实测）**：非提权进程对提权窗口，`WM_GETTEXT`/`WM_GETTEXTLENGTH` **放行**（在 UIPI 的消息白名单里），所以控件文字读得到；`BM_CLICK`、`SetForegroundWindow`、`PrintWindow` **被挡**，所以点不动、也前置不了。表现就是「读得到、点不动」—— 别把读得到误判成能自动化。想给提权窗口截图 OCR，必须在提权进程里先 `SetForegroundWindow` 再抓屏（非提权抓下来的是挡在它前面的窗口，OCR 会读出编辑器里的字，本轮踩过）。
- 安装层 P0-01/02 的人工步骤：在已开好的管理员 PowerShell 里粘一行回车：
  `powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\admin\Desktop\Huashuo-Dating-Automation\tools\run_install_elevated.ps1`
  结果看 `reports/install_run.log`，头行写 `admin=True/False`；非提权跑只会失败在权限检查，不碰已装大厅。
- **P1-A 走一次性计划任务**（已建好并验证）：用户在管理员 PowerShell 里建了 `HallAutoP1`（`/RL HIGHEST`，动作是跑 `run_p1_apps.ps1`）。**标准（过滤）令牌就能启动它**：2026-09-15 14:19 那轮 `schtasks /Run` 后日志头行 `admin=True`，还杀掉了非提权 `taskkill` 拒绝访问的残留安装器 pid 19444。以后每轮 **0 人工**，不要再让用户粘贴命令。
  Git Bash 里 `/Run` 会被当成路径转换，必须写 `MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`。
  建任务的原始命令（万一要重建，管理员 PowerShell）：
  `schtasks /Create /TN HallAutoP1 /SC ONCE /ST 00:00 /RL HIGHEST /F /TR "powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\admin\Desktop\Huashuo-Dating-Automation\run_p1_apps.ps1"`
  非提权直接跑脚本会在 `install_fixture` 开头立刻失败并给出提示，不会白等 300s。日志 `reports/p1_apps_run.log`。
- P1-A 的**检测**部分（三个列表刷新）不改机器状态，非提权就能跑：`-m "apps and not destructive"`。

## 探壳结论（写断言前必读）

- 首装/升级后首跑先出**独立顶层窗「权限确认窗口」**（同意/取消），点完同意主窗口才出现 → `wait_main_window` 等主窗口前先关前置窗。
- 大厅**会恢复上次的页面**（实测首跑落在设置/关于页）→ 就绪判定缺结构且存在 `BackBtn` 时点返回回首页。
- 关于页版本节点 `aid=VersionLabel` 的 UIA name 是占位符 `ver`（**产品无障碍缺陷**），真版本号读不到 → 截版本行放大 8 倍二值化后 OCR；本机仅 zh-Hans 引擎，小灰字误认靠 `normalize_ocr` 归一（`l→1`、`．→.` 等）。
- 结果页下方挂第三方广告位，节点坐标在窗口外 → 标题只从主窗口矩形内的「安装/打开/更新 X」主按钮取。
- 主按钮命名三处不一致：结果页 `安装 {名}`、首页 `安装应用：{名}`、详情页只有 `一键安装`。
- 搜索框 `SearchBarInput` 是原生 `Edit`，WebView2 内容 UIA 可读 → 不需要 Playwright。
- 「我的」页面（`aid=Mine`）三页签：`UpTabBtn`/`UnTabBtn`/`NoTabBtn`；列表 `UpAppListBox`（更新）/`UnAppListBox`（卸载），条目是 `ListItem`，条目内按钮 `UpBtn`（更新）/`UnstallBtn`（卸载），更新页还有 `UpdateAllBtn`「全部更新」。**禁止点 UpdateAllBtn 或任何非夹具条目**——列表里全是微信、WPS、Python3 这类日常软件。
- 卸载确认弹窗是主窗口下的子 Window「卸载应用对话框」，按钮 `aid=ConfirmBtn`、name「卸载」（不是「确定」），只在弹窗里点。
- **卸载应用列表 = 系统卸载注册表的镜像**，所以注册表 DisplayName 是可靠 oracle；大厅不维护独立的已装清单。
- **但更新列表的口径比"注册表镜像"宽，而且条目名不一样**（2026-09-15 15:51 实测）：`Python3`、`飞书` 出现在更新列表里，却**不在 HKLM 卸载表**（HKCU 或别处），说明更新检测不只扫 HKLM。条目名用的是**大厅目录里的名字**（实测装完 7-Zip 26.02 后列表显示「7-Zip（64位）」，而注册表 DisplayName 是 `7-Zip 26.02 (x64)`）→ `apps.py` 的 `match_display_name` 是按注册表名匹配的，**对更新列表不适用**；列表断言按目录名，安装结果仍按注册表 `DisplayVersion` 断言。
- **厂商安装器只能用 Win32 读，UIA 是瞎的**：网易云音乐的安装器是 NSIS，UIA 报 0 个 descendants，还把它的每个子控件误报成独立顶层窗（class 就叫 `Button`/`Edit`/`Static`、名字全空），所以「按控件名找按钮」「枚举 descendants」两条路都是死的。换成 Win32 立刻全通：顶层有标准对话框 `#32770`「网易云音乐 安装」，子控件 `WM_GETTEXT` 能读到 `关闭(&L)`/`取消(&C)`/`< 上一步(&P)`（**要连 `(&L)` 一起剥掉才能命中白名单**）；主按钮「立即安装」是自绘的、**没有文字**，只能按面积认（360x60=21600，右上角关闭叉 36x36=1296，协议链接最大 144x25=3600）。`tools\probe_win32.py` 是这条路的探针。
- **分层窗截图要用 `PrintWindow` 的 `PW_RENDERFULLCONTENT`(flag=2)**：普通 flag=0 和整屏裁剪对这类窗都是纯空白（`distinct_colors=1`），flag=2 才有内容（6000+ 种颜色）。这是走 Win32 之前的兜底方案，现在用不上了，但结论留着（`tools\probe_installer.py`）。
- **厂商卸载器同样只能用 Win32，而且比安装器更坑**（2026-09-15 实抓）：大厅点「卸载」后拉起的是 NSIS 卸载器，映像被解包到 `%TEMP%\~nsuA.tmp\Au_.exe`（**不在下载目录**），对话框标题「网易云音乐 卸载」，卸完那页变成「网易云音乐 卸载/ENABLECANCEL」。确认页上有**两块尺寸位置完全相同的自绘按钮**（都是 360x60=21600），OCR 出来是**左「再想想」= 取消、右「狠心卸载」= 确认**；而控件树里被皮肤藏起来的文字按钮是 `卸载(&U)` 在左、`取消(&C)` 在右 —— **左右正好相反，按几何猜必点错**。唯一可靠的点法：`BM_CLICK` 那个隐藏的 `卸载(&U)`（隐藏不影响 BM_CLICK，提权实测 3s 后注册表条目消失）。卸完那页只剩一块来历不明的自绘大按钮，**别点**，改点隐藏的文字「关闭」，否则可能把应用又拉起来。
- **但这个镜像是进页面时的一次性快照**：装完新应用当场去看不到（本轮实测），重启大厅后必然刷新；**退出「我的」重进也能刷出来**（2026-09-15 14:52 实跑验证，`uninstall_item_button` 的兜底生效）。所以「装完立刻卸」的用例必须先逼列表重拉，否则会假失败。
- **注册窗口控件图**（2026-09-15 实抓，客户端内 WPF 窗口，独立顶层窗名「华硕应用商店」，按 pid 找）：`MobileInput` 手机号 / `CodeInput` 验证码 + `CodeSendBtn`「发送验证码」/ `PasswordSecInput` 密码 + `PassShowBtn` / `RePasswordSecInput` 确认密码 + `RePassShowBtn` / `PasswordInputTip`、`RePasswordInputTip` 提示 / `AgreeCheck`「同意协议」/ `BooksAi` 协议文本 / `RegisterBtn`「**注册并登录**」/ `CloseBtn`。密码规则写在提示区：**8-20 位，至少包含数字、字母、特殊符号其中两种**；确认密码提示是「两次密码要完全一致」。
- **`PasswordInputTip`/`RePasswordInputTip` 的真实文字 UIA 读不到**（`name`/`window_text`/`texts` 全被钉成「密码提示」「确认密码提示」，`legacy_Value` 空，`get_value` AttributeError）—— **和 `VersionLabel` 的 `ver` 是同一类无障碍缺陷的第二例**，可合并提一条产品问题。但**变红这件事可以断言**：按提示节点矩形截屏数红像素，灰=0、红≈900～6300（阈值 50），见 `hall_auto/screen.py` + `register.py:tip_red_flags`。探针 3 说「提示是静态的」是错的（当时只 OCR 了文字没比颜色）。
- **设置页清单**（2026-09-15 实抓）：默认下载目录（编辑/查看）、磁盘空间提示、安装包清理（`安装完成后删除`/`下载完成一周后删除`/`不自动删除`）、客户端更新（`自动检查更新`/`自动下载提示`/`立即检查更新`）、推送设置、升级弹窗设置、**主题设置（`浅色`/`深色`/`跟随系统`）**、**点击关闭按钮（`最小化到托盘`/`关闭应用`）**、关于。主题三档是 P1-C 的目标；「关闭按钮」这两档解释了为什么点 X 未必结束进程 —— 冒烟的冷启动一律用 `taskkill /F /T` 才靠得住。**主题三档已探明**：RadioButton `LightMode`/`DarkMode`/`AuroMode`，读选中用 `is_selected()`（Toggle 模式不支持），外观用主窗口平均亮度（浅 227～255 / 深 41～53），**按名字找会撞到同名 ToolTip**，见 `hall_auto/settings.py`。
- 同步应用页未登录时没有列表，只有文案「登录后查看同账号在其他电脑已安装应用」+ `LoginBtn`「立即登录」。
- 绿色单文件应用（实测 Win截图）**当不了夹具**：装 = 下到 `C:\AsusMCenterDownload` 直接运行，**从不写卸载注册表条目**（不是延迟，是压根没有）、大厅卸载列表里也从不出现、详情页永远停在「一键安装」，所以「装上了/卸掉了」无从断言；它自己还要求提权才能启动（非提权 `WinError 740`）。另外它的映像同样落在下载目录里，**按映像认安装器时必须排除**，否则会把它的窗口误当向导去点、甚至被收尾的 taskkill 打死。夹具只能选带安装器的大厂应用（网易云音乐：装完 55s 内注册表就有，卸载器 `UnInstall.exe /S` 能静默卸干净）。
- 下载目录 `C:\AsusMCenterDownload` 会留安装包（网易云音乐 165MB 装完仍在），属缓存，不算缺陷，但跑完要清，否则下次装同名应用可能复用旧包。
- 登录弹窗是独立窗 `aid=LoginWindow`。账号密码页：`MobileInputBox` / `PasswordSecInput` / `LoginBtn`（name「登录」）/ CheckBox「同意协议」；短信页：`CodeInput` / `CodeSendBtn`（name「**发送验证码**」，不是「获取验证码」）；页签是 Text `PassLoginSel` / `CodeLoginSel`；另有「微软账号登录」「注册账号」「忘记密码」入口。**不勾「同意协议」登录按钮是灰的**，`invoke()` 无效。已登录后点 `UserInfoPart` 开的是独立菜单窗（标题「华硕应用商店」），里面才有「退出登录」。**点完「发送验证码」后 `CodeSendBtn` 节点从控件树里消失**（倒计时态不暴露按钮，2026-09-16 实测），所以「发送成功」不能按按钮签名断言，只能以手机收到短信为准。
- 忘记密码页是登录窗口**原地换的客户端内 WPF 页**（不是新窗、不是网页）。控件：`MobileInput`（手机号，**注意登录弹窗是 `MobileInputBox`、注册页才叫 `MobileInput`**）/ `CodeInput` / `CodeSendBtn` / `PasswordSecInput` / `RePasswordSecInput` / 提示行 `PasswordInputTip` / `RePasswordInputTip` / 提交 `SetBtn`（name「提交」）/ 关闭 `CloseBtn`；**没有协议勾选框**。三个坑：①入口要 `_by_name(pid,"Button","忘记密码",exact=True)`，密码提示文案「…可点击忘记密码进行重置」也含这四字，模糊匹配会点到 Text；②`SetBtn` 不能当页面判据，主窗口导航栏「设置」按钮 aid **也是** `SetBtn`；③这页在主窗口树里的容器名是「**注册窗口**」（容器复用没改名，无障碍缺陷候选）。点完发送 `CodeSendBtn` 同样消失。**走到发送为止，别点提交**（会真改密码）。

## 工程

- 安装层：管理员粘上面那行（等价于 `.\run_p0_install.ps1`，但日志落盘更稳）
- 冒烟：`.\run_p0_launch.ps1`（P0-03/04/06/07）。必须带 `.\`
- 单测：`.\.venv\Scripts\python.exe -m pytest tests/unit -m unit`
- 登录套件（P1-B）：先设环境变量再跑，**密码只走环境变量，不落盘、不进 git、不写进任何文件**
  `$env:HALL_TEST_USER="<测试号>"; $env:HALL_TEST_PASSWORD="<密码>"; .\.venv\Scripts\python.exe -X utf8 -m pytest tests/launch/test_p1_login.py -m login -v`
  没设凭据时密码登录用例自动 skip，控件图和退出登录用例照跑。
- 人在环套件（P1-B，`-m manual` 选中**短信登录 + 忘记密码往返改密码**两条，**会真发短信、真改密码**，只在用户自己终端跑）：
  `$env:HALL_TEST_USER="<测试号>"; $env:HALL_TEST_PASSWORD="<原密码>"; $env:HALL_TEST_NEW_PASSWORD="<临时新密码>"; .\.venv\Scripts\python.exe -X utf8 -m pytest tests/launch/test_p1_login.py -m manual -v -s`
  短信登录（只需 `HALL_TEST_USER`）：脚本填号、勾协议、点「发送验证码」后停在 `input()` 等人工回填码。忘记密码往返（需三个环境变量）：OLD→NEW 重置（停 `input()` 等第 1 个码）→ 用 NEW 登录验证 → NEW→OLD 还原（停 `input()` 等第 2 个码）→ 用 OLD 登录验证；还原程在 finally 里，账户最终一定回到原密码。只想跑一条加 `-k sms` 或 `-k forgot`。两条都没设够环境变量或非交互式终端（stdin 不是 tty，代理侧跑就是）都自动 skip，不会发了短信/改了密码没人管。`-s` 必须带，不然 input 提示被 pytest 捕获吞掉。
- 注册格式套件（P1-B，不需要凭据、不发短信，约 3 分钟，会占屏）：`.\.venv\Scripts\python.exe -X utf8 -m pytest tests/launch/test_p1_register.py -m login -v`
- 主题套件（P1-C，不需要凭据、不装卸任何应用，约 2 分钟，屏幕会忽明忽暗并重启一次大厅；**收尾自动还原原档位**）：`.\.venv\Scripts\python.exe -X utf8 -m pytest tests/launch/test_p1_theme.py -m settings -v`
- P1-A 检测（不改机器状态，非提权可跑）：`.\.venv\Scripts\python.exe -X utf8 -m pytest tests/launch/test_p1_apps.py -m "apps and not destructive" -v`
- P1-A 真装真卸：**首选 `MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`**（0 人工，代理侧自己就能触发）；备选是在**管理员** PowerShell 里 `.\run_p1_apps.ps1`。两者等价于 `-m apps` + `HALL_ALLOW_INSTALL=1`，日志都在 `reports\p1_apps_run.log`。wrapper 跑 pytest 前先执行 `tools\reset_fixture.py`：先按 `installer_pids` 杀掉残留的安装器/**卸载器**进程，再按注册表 `UninstallString` 加 `/S` 静默卸掉夹具应用，保证干净起点（残留会让用例直接 skip）；复位是准备动作，被测的卸载仍从大厅界面点。
- 关于页版本单读：`.\.venv\Scripts\python.exe -c "from hall_auto.config import load_config; from hall_auto.about import read_about_version; print(read_about_version(load_config()))"`
- 首装初始化慢，本机超时覆盖在 `config.local.yaml`（launch 120 / ready 150），没改入库默认
- 探壳 dump：`tools\probe_uia.py`（大厅 UIA 树）；`tools\probe_win32.py`（厂商安装器的 Win32 对话框 + 控件文字 + 按钮矩形表）；`tools\probe_installer.py`（截图 + OCR 那条兜底路，含 `PW_RENDERFULLCONTENT` 抓图）；OCR：`tools\ocr_text.ps1 <png> [-Boxes] [-Language en-US]`
- 本机 `python` 不在 PATH，一律 `.venv\Scripts\python.exe`；跑 python 带 `PYTHONUTF8=1`

## 下一步

1. P0-05 已结：未登录态跑通 P0-06/07 → 主流程无登录墙，跳过。**P1-A 也是未登录态跑通的**（14:52 那轮），所以搜索/详情/下载/安装/卸载全都不需要登录，这条悬念已结。
2. P1-B 剩：**微软账号**（测到系统/WebView 登录窗为止，出 MFA 标人工）。**短信登录已结**（2026-09-16 人在环实跑通过 + 固化为 `test_sms_login_manual`，见第 22 条）。**忘记密码已结**（2026-09-16 往返改密码用例用户终端实跑通过：真改密码 + 新密码重登验证 + 还原原密码 + 原密码重登验证，固化为 `test_forgot_password_reset_manual`，见第 23 条）。**注册格式校验已结**（第 17 条，不烧号）；用户那个一次性备用号现在只留给可选的「真注册成功」 happy path，跑不跑由用户定。假验证码提交无反馈那条是 `xfail`，跟开发确认后要么转正式缺陷、要么删用例。测试号由用户口头提供，**只走环境变量，不写进仓库**。
3. **P1-A 已结**（真装真卸 + 点更新端到端通过，见第 11–13、15、18 条）。复跑一行：`MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`，看 `reports\p1_apps_run.log`，超时证据自动落盘 `reports\probe\wizard_dump.txt` + `wizard_timeline.txt`。更新夹具已定 **7-Zip**（钉 26.02，`reset_fixture.py` 每轮自动压回起点）。**只剩同步夹具未定**（要登录后看列表内容再定）。换别的夹具应用前必读探壳结论里厂商安装器/卸载器那两条 —— 换肤卸载器的按钮左右和文字按钮是反的，按几何猜必点错。
4. **P1-C 已结**（主题三档，见第 19 条）。设置页其余功能（下载目录、安装包清理、客户端更新、推送、升级弹窗、关闭按钮行为）设计文档里明确第一期只做主题，要不要扩由用户定。
5. **本轮改动已本地 commit `277c91d`，未 push**（34 文件 +3767/-53）。提交前自查结论：暂存内容里**没有**真测试号、没有密码、没有凭据赋值（只有公开示例号 `13800138000` 和 `<测试号>` 占位）、没有内网地址、没有禅道/飞书访问信息；`reports/`、`config.local.yaml` 本来就在 `.gitignore` 里。两个待用户拍板项：①`交接归档/` 已加进 `.gitignore` 不入库（那份归档讲的是本机个人规则部署，提到私有仓库名和用户目录，跟项目无关，文件仍在磁盘上）；②**机器路径带 Windows 用户名**已随文档进公开仓库（`C:/Users/ASUS/...` 是早先提交就有的，本轮新增 `C:\Users\admin\...` 出现在两份文档、`tests/unit/test_apps_policy.py` 的桩路径、`tools/run_install_elevated.ps1` 第 3 行）—— 只是用户名、不含凭据；那个 ps1 可以像 `run_p1_apps.ps1` 一样改用 `$PSScriptRoot` 去掉路径，但它是已验证过的 P0 安装入口，改了要下轮 P0 复跑才算数。git 身份本机没配（global 里没有 user.name/email），这次是用 `-c` 临时按历史作者 `wwd <alven3330859445@gmail.com>` 提的，**没有改任何 git config**。
6. 可选：把 `VersionLabel` 无障碍缺陷当产品问题提一条（描述已给用户）。

## 环境事实

- 已装：华硕大厅 1.6.11.4（三路版本一致）；`AppStoreServer` 允许残留
- 登录态：登录套件最后一条用例会退出登录，所以跑完停在**未登录**态；要复现已登录界面得先跑密码登录用例
- 夹具应用现状（2026-09-15 14:56 实测，14:52 那轮跑完后）：**机器上没装网易云音乐** —— 注册表无条目、无残留厂商对话框、无残留安装器/卸载器进程，`C:\Program Files\NetEase\CloudMusic` 只剩 `debug.log` 和 `isc_chat.dat` 两个 0 字节残留。`C:\AsusMCenterDownload` 里还留着 165MB 安装包（缓存，下轮安装直接复用，所以 101s 就能跑完）。起点干净，可以直接复跑。
- **本机只有 C 盘**（2026-09-15 实测 `Get-PSDrive`）。设计文档第 14 行写的参考目录 `D:\华硕大厅安装包` 是旧机器的，**在本机不存在**；本机安装包目录是 `config.local.yaml` 里的 `installer_dir: C:/Users/admin/Desktop/Test/华硕大厅`（放着 7 个 `myappstore_*_Setup.exe`）。
- **7-Zip 更新夹具包已就位**：`C:\Users\admin\Desktop\Test\华硕大厅\fixtures\7z2602-x64.exe`，1657896 字节，SHA256 `6745fa76dc2ea031596d8678f6f6b99c3c1b435b4164a63485adbbc7b8d82ef0`，官方源 `https://www.7-zip.org/a/7z2602-x64.exe`（GitHub releases `ip7z/7zip` 同样有，24.06～26.03 全在，永久可下）。大厅目录里 7-Zip 当前版本 26.03 / 更新时间 2026-09-04，与官方 26.03 发布日期一致 → 钉住 26.02 就永远落后一个版本，夹具不会过期。
- Python 3.12.10 + `.venv`（pytest 9.1.1 / pywinauto 0.6.9 / PyYAML / Pillow）
- 只有 Windows PowerShell 5.1，无 pwsh 7；OCR 仅 zh-Hans-CN 引擎
- 办公机不要 `--allow-install`
