# 会话交接

最后更新：2026-09-22（16:45）

> ✅ **同步状态（09-22 18:10）**：本机 `HEAD` = **`b01a3ab`**（= 本轮人在环提示语修复，见下面状态锚点那条）。
> 提交前核过 `git ls-remote`：`share` main = `origin` main = 当时的 `4d3b770`，两边都跟得上。
> ⚠️ **`b01a3ab` 还没推** —— 节点要拿到这版（含绑定短信码提示 + `logout` 清弹窗）必须推 `share` 再 `git pull`。
> **别再写「两边都是 bd8166c」「HEAD 是 2484ab4」「origin 落后一个 commit」。**
> ⚠️ **推 `origin` 的口径已更正（09-22 16:38 实测）**：**直接 `git push origin main` 就过** ——
> 沙箱给 shell 注入了 `http_proxy=http://127.0.0.1:53125`，git 走 libcurl 认这个变量。
> **别再写 `-c http.proxy=…:7897`**：Clash Verge 进程虽在跑，但**混合端口没开**（7897 无监听），
> 显式指定它反而 `Could not connect to server`。`share` 是本地路径，不受影响，照常直推。

> 只放**会变的**：当前进度、环境现状、下一步、未提交清单。稳定知识在 `项目知识库/`（入口 `00-index.md`；
> **跑命令 / 查环境 → `运行手册.md`，写断言前 → `探壳结论.md`**）。已结历史条目在 `交接归档/`
> （最近 `会话交接-20260921.md`）。⚠️ **该目录只在本机**（已 gitignore），**clone / 整包拷贝后不存在**。

## 状态锚点（新对话先读这块）

- **焦点**：华硕大厅 1.6.11.4 客户端 UI 自动化（pywinauto + Win32 + pytest）。P0 / P1-A/B/C 全结，
  P2 安全验证 + WB 入口 + 证据留痕已落地。**多机跑批已在真机端到端跑通，但节点上是出厂预装旧大厅，
  下一步是复位到 1.6.11.4 再取真结论。**
- ✅ **首台真机 DESKTOP-DOHED68（Win10 19045 / Py 3.13.14）12 步 bootstrap 全跑过**：第 1–11 步 `[OK]`，
  第 12 步红 = 杀软误杀，与代码无关。**可以继续发第二台。**
- ✅ **共享盘链路已修复（09-22 13:36 重跑全绿）** —— 三个错逐个定位，**全是环境/脚本，不是共享盘坏了**：
  ① `53` = **控制机上的 Clash Verge / TUN 劫持名字解析**（两侧关掉 TUN 即消失）；
  ② `1219` = **bootstrap 第 7 步的探针先用当前 Windows 身份建了隐式会话**，再换 `hallshare` 去连被拒
  —— 这种会话 **`net use` 看不到、`net use * /delete /y` 也删不掉**，要 `Restart-Service LanmanWorkstation -Force`；
  ③ `1909` = `hallshare` 被撞锁（控制机策略 **10 次 / 10 分钟**），**等 10 分钟**，排障期建议把阈值临时设 0。
  **机制 + 前置条件 + 错误码速查已落进新建的 `项目知识库/共享盘与跑批前置条件.md`。**
- ✅ **`--loop`「卡住不动」已修（09-21）**：不是卡死，是**空闲分支一个字都不打**；更糟的是它与真故障**同形**
  （UNC 一断 `Path.is_file()` 把 `OSError` 吞成 `False`，节点报假平安）。修：空闲打心跳 + 探不到农场换中文
  指引 + 关键 `print` 加 `flush=True`；`--once` 不可达返回 **3**。**退出码 `0`/`2`/`3`**。详见
  `运行手册.md`「节点侧的四道防呆」。
- ✅ **首台真机第一轮农场跑批（09-21）：链路全通，结果不算数。** 投放 → 取任务 → `launch` → 证据回传 →
  写回执 → 空转等任务，全程无误。P0-03/04/06 **passed**，**P0-07 failed**（`详情页没出现主按钮`）。
  ⚠️ 回执 `hall_version = "1.6.8.17"` = **出厂预装旧大厅**，oracle 在 **1.6.11.4** 标定 → **先复位再跑。**
- ✅ **首台真机第二轮跑批（09-22，控制机代投 `task_20260922_135743` / `135917`）**：大厅已复位到
  **1.6.11.4**（回执确认），**`launch` 4 passed 全绿**（09-21 挂的 P0-07 已不复现）、`settings` 2 passed；
  `apps-detect` 1 failed = **登录进不去**，根因是**测试账号密码已过期**（用户改密码后恢复，非代码问题）；
  `unit` 1 failed / 450 passed，唯一红的是测试没隔离 `HALL_TEST_USER` —— **已修（`4fb599a`）**。
- ⚠️ **控制机自己投任务不能用 UNC（09-22 实测）**：`dispatch` 写 `\\<自己>\hall-farm\tasks\` 撞
  `PermissionError [Errno 13]`，而 `status` 只读能过 —— 因为 `hall-farm` 的**共享级权限只给 `Everyone` 读**，
  只有 `hallshare` 是 `Change`（`provision_share.ps1:128` / `:225`），SMB 取交集。**提权也没用。**
  **解法：控制机 `farm_root` 写本地路径 `C:\hall-farm`**（不走 SMB，只受 NTFS 管）。已落知识库 + 手册。
- ✅ **两层常亮已落地（09-21）**：进程级 `hall_auto/awake.py` + 机器级 `tools/set_keep_awake.ps1`
  （bootstrap **第 13 步**，可 `-Restore`）。**实跑抓出一个假绿**：`Get-Timeout` 取错 `powercfg` 里那两个 `0x`
  （取值范围在前）→ 任何机器都读成 `AC=0`、`-Check` 恒 `[OK]`。已改取**末尾两个** + 补 DC 校验。
- ✅ **控制机 `farm_root` 已落盘为本地路径（09-22 15:55）**：bootstrap 第 5 步「**运行那一刻环境变量有值**
  才写」，之前配里是 UNC。**实测 UNC 写 `tasks/` 仍报 `Permission denied`**（`status` 只读能过，与上一条吻合），
  已把 `config.local.yaml` 改成 `C:/hall-farm`，`status` 核过解析为本地路径。**判据是配置里有没有 `farm_root`。**
- ⚠️ **未验项**：`install_git.ps1` 的安装程序、`add_av_exclusions.ps1` / `set_keep_awake.ps1` 的「真去改」
  路径**都没在真机验过**（本机非管理员 + 沙箱里 `powercfg` 不可用）。**跑 bootstrap 盯第 13 步 `[OK]`。**
- ✅ **杀软结论已更正（09-22 复测，`d56b682`）**：是**病毒库版本的误报、会自愈**，不是"这台机器就这毛病"。
  证据：同机同用例没改任何东西，探针 `[1]` 全 `0(OK)`；病毒库 **1.459.324.0** 于 09-21 22:07 更新后就好，**全程没人关过实时保护**。**顺序：探针干净就走 → 报 225 先更新病毒库再复探 → 仍报 225 才关防护 / 加排除项。**
  ⚠️ 别再拿"昨天关杀软就好了"当证据。
- ⚠️ **`bootstrap_machine.py` 不能当"参数校验器"跑**（09-22 自己踩的）：加 `--installer-dir /nonexistent`
  想验 argparse，**它真把 `config.local.yaml` 写了** —— `installer_dir` 与 `security.tools_dir` 双双被改坏
  （已重跑第 5 步修回，并核过包与安全工具都在）。**要验参数只跑 `--help`。**
- ✅ **09-22 四批修复已落地（均已加守卫；原文见 `交接归档/会话交接-20260922.md`，仅本机）**：
  `-m unit` 漏跑（补 `pytestmark`，选择集 127→438，守卫改走 AST）、`--skip-keep-awake` 未注册进
  argparse（bootstrap 第 13 步崩）、4 个重名函数已删 + `test_repo_hygiene` 2 条守卫、
  发新机前清单两处硬伤（3.5 步引用未建的 `.venv` / 第 1 步没钉 Py 3.12）。
  **桌面发放版没密码也能按行级 diff 搬** → `交付前验收.md` 末节。
- ⚠️ **git 身份**：global 无 `user.name/email`；本仓库已设 **repo-local** `wwd <alven3330859445@gmail.com>`。
- 🔒 **仓库已转 private（09-22 17:14）**：此前是 **public**，`运行手册.md` 的旧共享盘口令、
  注释与夹具里的真实微软邮箱因此公开过一段。已转 private + 两处改占位符 + 加 2 条守卫
  （`test_repo_hygiene.py`）。**没改写历史**（用户明确允许那个旧口令暴露）。
- ⚠️ **本机拿不到 `origin/main` 远端跟踪引用**（`git status` 显 `[gone]`）。**别拿它判断"没 push"** ——
  权威口径是 `git ls-remote origin` / `share`。
- **机器规模（组长 09-18）**：一批 5 台、跑完再换下一批，**批次调度代码暂不实现**。**安装包分发**：
  组长明确**没有官方下载地址** → 走自建共享盘（已端到端验证），**前提是包源机常开**。
- **7-Zip 自动装钉 26.02**（不是最新版）：它是 P1-A「更新夹具」的起点，装最新版会让起点断言失配。
  **只有 Python 仍须人工**（引导悖论，绕不开的第 0 层）。
- **共享盘侧两条遗留**：① `HallShare-SMB` 防火墙规则（445 入站）跑批结束记得关；② `hallshare` 账号密码
  已重置且永不过期（**值不进任何文件**），节点 bootstrap 必须用同一个。建共享是**唯一需提权的动作**，5 台共用。
- **发给测试同事两份**：`测试机操作手册.md`（完整版）+ `新机操作清单.md`（纯步骤版）。桌面副本由
  `tools/sync_handbook.py` 生成**发放版**（真密码现场替换，仓库只有占位符；没设变量报错退出；
  覆盖前**只在本脚本没生成过这份文件时**才留档 —— 不再堆旧版备份）。别让人看《运行手册.md》现学。

- ✅ **清单缺 `cd` 已修（09-22 真跑踩出）**：管理员窗口 cwd 是 `C:\WINDOWS\system32`（**不继承**旧窗口），
  第 3.5 步探针走相对路径 → 报「找不到文件」，看着像探针被杀。清单第 3 / 3.5 步 + 手册第 1 步 +
  运行手册坑 6 已补 `cd`；`test_sync_handbook.py` 加 2 条守卫（注入法验过能红）。
  ✅ **桌面发放版已按行级 diff 搬过去**（没密码也能搬 → `交付前验收.md` 末节）：527→543 / 1085→1088 行，
  补回上一轮搬运丢的末尾 CRLF 后**与 `sync_handbook.py` 渲染产物逐字节一致**（= 等于跑过脚本）；
  改前副本在 `%TEMP%\hall_desktop_backup\20260922_122009\`（本机）。

- ✅ **「一次跑完 + 人在环收码」已落地（09-22 16:35，已提交 `bd8166c`，已推 `origin`+`share`）**：三件事 ——
  ① **全量档** `dispatch --suites all`（`unit,launch,apps-detect,login,settings,security,wb`，
  唯一定义在 `hall_auto/suites.py:FULL_RUN_SUITES`，刻意不含 `install`/`apps-lifecycle`/`login-manual`）——
  治的就是「手打套件名漏一个 → 节点跑完手上的活回轮询 → 看着像卡住」；
  ② `farm_agent` 跑完**全部**自动套件后，**stdin 与 stdout 都是终端**才问「要不要参与人在环」，
  回「能」依次跑 manual（短信 1 码 / 微软邮箱 1 码 / 忘记密码 2 码）；计划任务加 `--no-manual`；
  ③ **修了微软状态机认错页面**：填邮箱点下一步后微软改出「我们向 <邮箱> 发送登录代码。」页，
  正文含邮箱 → 旧判定 `email in n` 把它认成 `account_picker`，去点纯文本再死等已登录态，
  报「提交后未进入已登录态」（**与真失败同形**，09-22 整轮都绕在这）。现在 `email_otp` 先判。
  `tests/unit` **499 passed**（+31 条守卫，注入法验过能红）。跑法见 `运行手册.md`「一次跑完 / 人在环」，
  控件图见 `探壳结论.md` 微软登录窗那节。

- ⚠️ **人在环提示语两处错已修（09-22 18:0x，已提交 `b01a3ab`，未 push）** —— 用户指出「微软那条不只收邮箱码」时查出的：
  ① **终端提示语漏了绑定短信码**：微软登录**邮箱验证通过、登录成功后同样会弹「绑定手机号」**，
  脚本自动填号 + 自动点发送，**还要人再给 1 个短信码**；提示语只写了「1 个邮箱验证码」→
  人只备邮箱、手机不在手边就卡在弹窗上（模态窗，还挡着后面每条）。
  ② **顺序反了**：提示语把「短信登录」写在「微软登录」前面，实际执行顺序是
  `test_p1_login.py` 的文件顺序 = 微软 → 短信 → 忘记密码。
  改法：提示语改为从 `MANUAL_SEQUENCE` / `_MANUAL_BIND_WARNING`（`tools/farm_agent.py`）打印，
  守卫拿**真实文件顺序**去对（`test_farm_agent_policy.py`，3 条）。
  **首次跑最多 6 个码、绑过号稳定 4 个**（表见 `运行手册.md`）。
- ⚠️ **连带修掉一个假红（同日）**：无人值守的 `test_microsoft_login_sso` 登进去也可能弹绑定弹窗，
  它走 finally 的 `logout()` → 弹窗挡着用户区 → 抛「退出登录：点不开用户菜单」，
  **看着像登出功能坏了**。修法：`logout()` 里先跑 `dismiss_bind_dialog`（新增，默认 `timeout_sec=0` 只探一次不等待）。
  放共用处是因为只有人在环那两条会自己处理弹窗。守卫 3 条（`test_login_policy.py`，注入法验过能红）。
  `tests/unit -m unit` **535 passed**；`运行手册.md` / `探壳结论.md` 已同步。

- ✅ **《新机操作清单》已重写成纯线性版（09-22 16:49，用户定调）**：结构 = **前提条件置顶**（两机共用 / 测试机 /
  控制机 / 要齐 4 样）+ **18 步「步骤 → 预期结果」**一条线走到底，每步头一行固定标
  **机器 ｜ 终端（新开还是继续用当前窗口）｜ 权限（普通还是管理员）**；砍掉全部表格、报错对照表、"为什么"。
  连带改了 **5 处交叉引用**（手册「8 步」→18 步、`2B-1/2B-2`→步骤六 A/B、运行手册 `3.5 步`→步骤八/九、
  多机代码分发 `2A`→步骤四～五、共享盘前置条件「第 0 节」→前提条件一/二/三）。
  `pytest tests/unit -m unit` **514 passed**（5 条文档守卫全过；503 是 16:49 那次，之后 `8ece106`/`46ec136` 又加了 11 条）；桌面两份发放版已重生成（16:49，含真密码）。
  **改这份清单前先读 `tests/unit/test_sync_handbook.py`** —— 它锁着 fenced 块顺序、`cd` 必须与命令同块、
  `.venv` 时序、占位符白名单。

### 本轮修复（09-22 真机踩出 → 当天改完，全部落在 `tools/bootstrap_machine.py`）

1. ✅ **`is_dir()` 没包 `OSError`**（`step_farm_root`，另含共享盘 wheelhouse 的同类调用）：
   pathlib 只把 ENOENT 类（含 WinError 53/67）吞成 `False`，**1909/1326/5 是 EACCES 类会重抛** ——
   53 那次走的是干净 `[FAIL]`，1909 这次**直接 traceback 退出**，后面 4 步 + 报告 JSON 全丢。
   已把 `is_dir()` 并进 `mkdir` 的 `try`，并按 winerror 补一句中文下一步。
2. ✅ **`_share_connect` 先探后连招来 1219**：命中 1219 先按**同一 target** `/delete` 再重试一次；
   仍失败就打「跑一次 `Restart-Service LanmanWorkstation -Force`」。另给 53/67/1326/1909/5 各配一句下一步。
3. ✅ **`step_share_login` 假绿**：新增 **`[WARN]` 档**（`[FAIL]` 优先于 `[WARN]`）——
   配了共享盘却连不上不再打 `[OK]`，报告 JSON 单列 `warned`。三档口径见 `运行手册.md`「三档输出」。
4. ✅ 守卫 **8 条**加在 `tests/unit/test_bootstrap_policy.py`（**注入法验过能红**）；
   `tests/unit` **461 passed**；真 UNC 冒烟 `[OK] 农场目录 / 四个子目录齐全`。
5. ✅ **桌面发放版已重新生成（09-22 13:47）**：`测试机操作手册.md` + `新机操作清单.md` 新增
   「代理/TUN 必须关」+ 错误码表（补 1219/1909、修正 53 的归因、纠正 `net use * /delete /y` 删不掉隐式会话）。
   已跑 `tools/sync_handbook.py`（`HALL_SHARE_PASSWORD` 从本机 `farm_node.env` 读进环境变量，值不进对话），
   两份桌面副本 `--check` 报 `[同]`（sha256 与渲染产物一致）；上一版本就是脚本产物，按设计**未留档**。
   **口径：以后改这两份文档，改完立刻跑一次 `sync_handbook.py`，别等发之前。**

### 下一步第一件事

1. ⚠️ **人在环（`login-manual`）—— 当前第一优先。09-22 17:40 又修了两处（`184e841`）**：
   **① 微软【发送验证码】原来"只点一下不验证"** —— webview 上 `invoke()`/`click_input()`
   会静默失败，现场表现是「脚本没点就回来问我要码」（用户 17:26 真机反馈）。现在
   `Button` 优先 + 点前确认前台 + **点完等代码框出现**，等不到就落盘控件树抛错。
   **② 新增登录后「绑定手机号」弹窗的自动处理**：自动填号 → 自动点获取验证码 → 人只输码。
   节点上先 `git pull` 到 `184e841`，在 `farm_node.env` 补一行 `HALL_BIND_PHONE=<要绑的号>`
   （**留空则回退 `HALL_TEST_USER`**，多数情况不用配），再
   `.\\.venv\\Scripts\\python.exe -X utf8 tools\\farm_agent.py --local login-manual`。
   ⚠️ **绑定弹窗的控件 aid 是按惯例推的、尚未探壳** —— 第一次跑很可能报「找不到…」，
   按报错里给的 `reports\\bind_ui_dump\\<ts>.txt` 把 aid 补进 `hall_auto/login.py` 的
   `_BIND_*_AIDS`，**不用重跑一次抓**；补完更新 `探壳结论.md` 那张表。
   `--local` 那条走 `run_suite`、**不是 `execute_suite`，证据不回传农场**，只在节点
   `reports\evidence\<节点>_<ts>\` 下；要进农场汇总就走「投任意任务 + agent 问时答能」。
   **别用管理员窗口跑**（会改掉 launch/login 的权限上下文）。
2. ✅ **节点代码落后已在 09-22 17:15 定位（决定性证据）**：节点 16:08 那轮 `unit` 是 **468 passed**
   （本机 `-m unit` 现在 collect 516 / 跑过 514），且节点日志里 `test_farm_agent_policy.py` **没有
   `test_manual_prompt_*` / `test_manual_phase_*` 这批用例**（`bd8166c` 才加的守卫）
   → **节点代码早于 `bd8166c`，它的 `farm_agent.py` 里根本没有「跑完问人在环」那段**。
   所以「投了任务也不问」不是配置/终端问题，是**代码没到位**。
   **`git pull` 的目标是 `2484ab4`（= `share` main），不是 `62060c8`** —— `62060c8` 有「问人在环」但
   **没有 `8ece106` 的 `-s` 修复**，跑出来是三条用例全 skip + 「退出码 0」的**假绿**。
   命令（节点，普通窗口）：`git pull`（`origin` 是 UNC 裸仓库，`pull` 不撞 ownership 检查）。
   ✅ **冒烟任务已投好**：`manual_smoke_20260922_1715`（只含 `settings`，节点上约 17s）在
   `\\LAPTOP-VS5F7HF4\hall-farm\tasks\DESKTOP-DOHED68.json`，pull 完在普通窗口跑
   `.\\.venv\\Scripts\\python.exe -X utf8 tools\\farm_agent.py --once` 就能取到，跑完会问「是否现在参与人在环」，
   回 **能**。成功判据：`done/DESKTOP-DOHED68_manual_smoke_20260922_1715.json` 里多出一条
   `{"suite": "login-manual", "interactive": true}`，且 `logs/` 里那一段有「人在环：本段输出直接打到终端」头。
3. ✅ **DESKTOP-DOHED68 已在 1.6.11.4 —— 原「本轮第一优先」那条已做完**（回执三次确认：09-22 13:57 / 16:06 / 16:14）。
   **别再跑 `tests/install -m install`，那是真卸载重装。** `install` 仍**走不了农场**（`is_admin()`
   不满足是 `pytest.fail` 硬失败，agent 非提权），要复跑只能在节点管理员窗口手工跑；
   `apps-lifecycle` 同理（另需 `HALL_ALLOW_INSTALL=1`）。
4. **发下一台新机**：照 `项目知识库/新机操作清单.md` 走（管理员窗口内**先**设 `HALL_SHARE_USER/PASSWORD` +
   `HALL_FARM_ROOT` **再**跑 bootstrap，顺序反了变量全丢）。**发之前先 `git ls-remote share` 核一眼**，
   **并按《共享盘与跑批前置条件.md》把两侧前置条件过一遍**（代理/TUN 先关、控制机常开、`hallshare` 未被锁）。
   ⚠️ **农场现在不是空的**：09-22 16:04–16:14 那轮全量档的结果/日志/回执还在，加上本轮的
   `manual_smoke_20260922_1715` 待取；清空前的备份在 `%TEMP%\hall_farm_backup\20260922_155339`（**仅本机**）。
5. 每台机器**手工填 `farm_node.env`**（bootstrap 只生成空模板）—— 不填则登录套件静默跳过（回执会标出）。
   ✅ DESKTOP-DOHED68 已填（16:10 那轮回执四项凭据全 `true`，`apps-detect` 是真登进去了才挂的）。

## 当前状态

- 本机 `C:\Users\admin`（= `LAPTOP-VS5F7HF4`）是当前唯一执行机 + 包源机 + 控制机，已装大厅 1.6.11.4。
  **真实 150% 缩放 / 物理 2560×1600（DPI 144）** —— 报告里见到 `96 DPI`/`1707x1067` 是未设 DPI 感知的**假值**。
- 登录态：跑完停在**未登录**（登录套件最后一条会退出登录）；要复现已登录界面得先跑密码登录用例。
- 夹具：机器上**没装网易云音乐**；`C:\AsusMCenterDownload` 留 165MB 缓存。7-Zip 更新夹具钉 26.02，
  `reset_fixture.py` 每轮自动压回起点。**起点干净可直接复跑。**
- 覆盖对照（组长表「1.6.11.2S」26 点）：补 11 / 合并 1 / 偏弱 1 / 漏或受阻 13；xlsx 在桌面（不进 git）。

## 下一步 / 待办

1. **证据机制收尾**：✅①`--evidence` 默认已改 `failure-only`（09-22；含手册 + 2 条守卫，unit 门禁 **2m09s→11s**）。
   ②「关键断言」列要有内容得上步骤级 `step("...")`；③unit 截图吵按 marker 排除（默认改了之后基本不吵）；
   ④样例 run `2026-09-17_1052/`(89MB) 可删。
2. **待报组长两处表前提差异**（非脚本缺陷）：①行 29「原入口 GIF」——1.6.11.4 原入口图标加载期/稳态/hover
   全程零帧变化，是静态图；②行 24「未同意协议 WB 入口正常」——未同意时内容全被门控在协议页后，入口不渲染。
3. ⚠️ **`login` 套件首次上农场跑出两条红（09-22 16:11，`test_password_login_success` 待定性）**：
   `test_password_login_success` —— 大厅**确实进了已登录态**，但用户区是 `asus_1883771273`，
   不是配置号的尾号 `61935`（截图印证）。**要么断言过时（用户区改成显示 ASUS 账号名了），
   要么 `logout()` 没真登出、复用了上次账号** —— 属产品口径变更，**跟开发确认后再改断言，别猜着改**。
   （另一条 `test_microsoft_login_sso` 已定性为代码 bug，本轮已修。）
   注册 happy path 用一次性备用号；假验证码无反馈那条是 `xfail`，跟开发确认后转缺陷或删。
4. **P2 后续**：正式包到了改 `config.local.yaml` 的 `security.package` 重跑（结论只代表本地 1.6.11.4S 包）。
   行 7-13 **卡点在服务端**（探针：点「立即检查更新」60s 零反馈）；行 14 骨架已落地；WB 行 19-22/30 缺后台开关。
5. 可选：`VersionLabel` 等无障碍缺陷（见 `探壳结论.md` 末尾）当产品问题提一条。
6. **多机跑批**：组长七问已全回，**口径表在 `运行手册.md`「验证目标与组长口径」**（写断言前先看）；
   控制通道已实跑通；`download.url_template` 留空（官方无稳定地址）。
7. **新机拿代码走共享盘裸仓库**：命令、push/pull 口径、已验/未验清单全在 `项目知识库/多机代码分发.md`。
8. ✅ **「发放版」已定**：`sync_handbook.py` 生成发放版 —— 凭据从 `$env:HALL_SHARE_PASSWORD` 现场替换，
   仓库只存占位符，没设变量报错退出；覆盖前只在「上一版不是本脚本产物」时留档，不再堆备份。
   **`交接归档/新机操作清单-发放版.md`（仅本机）用户 09-22 明确要保留** —— 纯步骤、带密码，
   **不是"多余备份"，别再提删**。
