# 会话交接

最后更新：2026-09-23（12:15）

> ✅ **同步状态现场核**：`git rev-parse --short HEAD` 对上 `git ls-remote origin refs/heads/main`
> （`share` 同法，两边都应等于本机 HEAD）。**别在这份文件里写"未推"/"落后 N 个 commit"/拿某个 hash 当结论**
> —— 每次提交都会过期，写在这儿还自指。
> ⚠️ **本机 `.git/refs/remotes/` 写入被沙箱拦** → `git status` 显 `[gone]`、`git log origin/main..HEAD`
> 列出的"未推"提交**全是假的**，**一律用 `git ls-remote`**。推 `origin` 直接 `git push origin main`
> （沙箱注入的 `http_proxy` git 认；**别写 `-c http.proxy=…:7897`**，那个端口没监听）；
> GitHub 侧偶发 `schannel` 断连 / `502`，**重试即过**（09-23 第 3 次成功）。

> 只放**会变的**：当前进度、环境现状、下一步、未提交清单。稳定知识在 `项目知识库/`（入口 `00-index.md`；
> **跑命令 / 查环境 → `运行手册.md`，写断言前 → `探壳结论.md`**）。已结历史条目在 `交接归档/`
> （最近 `会话交接-20260923-瘦身前全文.md`）。⚠️ **该目录只在本机**（已 gitignore），
> **clone / 整包拷贝后不存在**。

## 状态锚点（新对话先读这块）

- **焦点**：华硕大厅 1.6.11.4 客户端 UI 自动化（pywinauto + Win32 + pytest）。P0 / P1-A/B/C 全结，
  P2 安全验证 + WB 入口 + 证据留痕已落地，多机跑批已端到端跑通。**当前焦点 = 跑批口径已定为
  「冒烟 → 全量档 → 提权补跑」，等节点 `git pull` 到新代码后按新清单跑一遍。**
- **跑批三段（09-23 定，清单步骤十七 / 十八）**：① 冒烟 `--suites launch` —— **必须看到
  「已交回执」才往下走**；② 全量档 `--suites all --timeout 14400`（默认 1 小时不够）；
  ③ 测试机**管理员终端** `run_p1_apps.ps1`（夹具真装真卸 + 大厅内点更新）+ `run_p0_install.ps1`
  （大厅自身装卸）。**跑完 ③ 才算跑全。**
- **全量档 `all` = 7 套件**（`unit, launch, apps-detect, login, settings, security, wb`），**刻意不含**
  `install` / `apps-lifecycle`（要管理员 —— agent 非提权会 `pytest.fail` 而非 skip，而整体提权会改掉
  `launch`/`login` 的权限上下文）/ `login-manual`（人在环）。定义在 `hall_auto/suites.py:FULL_RUN_SUITES`。
- **控制机自己投任务不能走 UNC**：`dispatch` 写 `\\<自己>\hall-farm\tasks\` 撞 `PermissionError [Errno 13]`
  （共享级权限只给 `Everyone` 读、只有 `hallshare` 是 `Change`，SMB 取交集；**提权也没用**），
  而 `status` 只读能过。**控制机 `farm_root` 必须是本地 `C:/hall-farm`**，已在 `config.local.yaml`。
- **节点 DESKTOP-DOHED68**（Win10 19045 / Py 3.13.14）：12 步 bootstrap 跑过（第 12 步红 = 杀软误报，
  与代码无关）；大厅已复位 **1.6.11.4**；`farm_node.env` 已填（不填则登录套件静默跳过）。
  ⚠️ 该机 `pypi.org:443` 可达，但真下载 `files.pythonhosted.org` **`ReadTimeoutError`** →
  **在线装依赖走不通，离线 wheelhouse 是唯一出路。**
- **本机**（`C:\Users\admin` = `LAPTOP-VS5F7HF4`）= 唯一执行机 + 包源机 + 控制机，已装大厅 1.6.11.4。
  **真实 150% 缩放 / 2560×1600（DPI 144）** —— 报告里见 `96 DPI` / `1707x1067` 是未设 DPI 感知的**假值**。
- **登录态**：跑完停在**未登录**（登录套件最后一条会登出）。
- **夹具**：机器上**没装网易云音乐**；`C:\AsusMCenterDownload` 留 165MB 缓存；7-Zip 更新夹具**钉 26.02**
  （装最新版会让起点断言失配），`reset_fixture.py` 每轮压回起点。**起点干净可直接复跑。**
- **git 身份**：global 无 `user.name/email`，本仓库设 **repo-local** `wwd <alven3330859445@gmail.com>`；
  🔒 仓库已转 **private**（09-22 17:14）。
- **机器规模（组长 09-18）**：一批 5 台、跑完再换下一批，**批次调度代码暂不实现**；**安装包分发**
  组长明确**没有官方下载地址** → 走自建共享盘，**前提是包源机常开**。

## 本轮（09-23）已闭环

- ✅ **`tools/run_farm.py` 落地** —— 一条命令走完「投任务 → 每 30 秒查进度 → 自动出报告」。
  **核心价值：任务投出去 90 秒还没被取走，它直接把排查清单打出来**（agent 没跑 / 连不到农场 /
  名字对不上）—— 这正是 `dispatch` + `status` 分开敲时**完全看不出来**的卡死。退出码
  `0` 全交回执 / `1` 超时 / `2` 投失败 / `130` Ctrl+C；Ctrl+C **不丢任务**。完成判据 =
  `done/<节点>_<task_id>.json`（**不带 task_id 会让上一轮回执把新任务判成已完成 = 假绿**）。
  守卫 18 条（注入法验红 3 处全抓住）。
- ✅ **清单 / 手册改造（`b31a01a`）** —— 清单步骤十七改「先冒烟再全量档」、步骤十八改「测试机
  管理员补跑装卸 + 更新」；手册 3.1 清掉一处**严重误导**（原写 `install` 的「卸载闸由 `dispatch`
  自动开」，**暗示能投农场** —— 实际投了必挂）。步骤数仍 18；`tests/unit` **558 passed**、
  文档守卫 **50 passed**；桌面两份发放版已重出。
- ✅ **三条已修 / 已定性**（细节在 `运行手册.md`）：① **「`--once` 陷阱」** —— 「按清单走完却投了
  没人取」的**直接原因**（取一次就退出，先敲它后投任务 = 任务永远躺在 `tasks\`，节点侧零报错），
  清单步骤十六现在只留 `--loop`；② **`status` 的「1/0/0」是正常瞬时态**（`dispatch` 只写
  `tasks/<节点>.json`，**节点取走时才改名 `.running`**；待执行超 30 秒不降 = 节点侧问题）；
  ③ **无外网新机第 2 步的零依赖回退** `_share_dirs_from_config_files()` + 7 条守卫。
- ⚠️ **09-23 11:19 那轮跑批**：`launch` **4 passed 全绿**；`apps-detect` 1 passed / **1 failed**
  （`test_sync_list_logged_in`，**待真机定性**）；另有 agent 追问后追加的 `login-manual` 3 条
  （1 passed / 1 failed / 1 skipped）。
- ⚠️ **「冒烟跑完却不动」= 人在环问句挂住了（09-23 真机，已修）**：原来**任何**任务跑完都问
  「是否现在参与人在环」—— 36 秒的冒烟任务也弹，人不答就永久 `input()` 阻塞，**控制机只看到
  「执行中」不动、与死机同形**（真机：`launch` 36.9s 跑完，之后两分钟没动静）。修法：加判据
  `task_covers_full_run()`（**只对覆盖全量档的任务问**）+ `--ask-manual`；3 条守卫、注入法验红。
  **节点上仍是旧代码会复现 —— 去节点窗口敲回车解卡，再 `git pull`。**

## 下一步第一件事

1. ⚠️ **节点 `git pull`** —— 目标是 `share` 的 main（**现场核 `git ls-remote share refs/heads/main`，
   别钉死 hash**）。**唯一要避开 `62060c8`**：它有「问人在环」但**没有 `8ece106` 的 `-s` 修复**，
   跑出来是三条用例全 skip + 退出码 0 的**假绿**。pull 完按新清单跑：**先冒烟，再全量档**。
   ⚠️ 节点「投了任务也不问人在环」的判据 = **代码没到位**（日志里没有 `test_manual_prompt_*`），
   **不是配置 / 终端问题**。
2. ⚠️ **节点重跑 bootstrap 时不设 `HALL_PACKAGE_SHARE`** —— 设了就**验不到**第 2 步那条零依赖回退。
   （救急绕法：老代码也认，节点管理员窗口补
   `$env:HALL_PACKAGE_SHARE = "\\LAPTOP-VS5F7HF4\hall-packages"`。）
3. ⚠️ **`test_sync_list_logged_in` 真机复现** —— 跑批前把无关窗口最小化（上次截图被窗口挡住，定不了性）。
4. ⚠️ **用户区显示口径待开发确认**：登录**确实成功**，但用户区显示 `asus_1883771273`、不是手机号尾号；
   `test_password_login_success`（09-22）与 `test_sms_login_manual`（09-23）**同源**。**别猜着改断言**
   —— 要么断言过时，要么 `logout()` 没真登出、复用了上次账号。
5. **配 `update_fixture` 块**（`config.local.yaml`）才能让更新用例真跑，**没配会 skip 而不是失败**。
6. **5 台并发下的 `aggregate` 应体现「覆盖节点 5 台」**（组长口径）。

## 待办 / 遗留

- **证据机制收尾**：`--evidence` 默认已 `failure-only`；「关键断言」列要内容得上步骤级 `step("...")`；
  样例 run `2026-09-17_1052/`（89MB）可删。
- **待报组长两处表前提差异**（非脚本缺陷）：①行 29「原入口 GIF」是静态图（1.6.11.4 全程零帧变化）；
  ②行 24「未同意协议 WB 入口正常」—— 未同意时入口不渲染（内容全被门控在协议页后）。
- **P2 后续**：正式包到了改 `config.local.yaml` 的 `security.package` 重跑；行 7-13 **卡点在服务端**；
  WB 行 19-22/30 缺后台开关。
- **发下一台新机**：照清单走（管理员窗口内**先**设 `HALL_SHARE_USER/PASSWORD` + `HALL_FARM_ROOT`
  **再**跑 bootstrap，顺序反了变量全丢）；发前 `git ls-remote share` 核一眼 + 过
  《共享盘与跑批前置条件.md》（代理 / TUN 先关、控制机常开、`hallshare` 未被锁）。
- **共享盘侧遗留**：跑批结束记得关 `HallShare-SMB` 防火墙规则（445 入站）。
- ⚠️ **未验项**：`install_git.ps1` 的安装程序、`add_av_exclusions.ps1` / `set_keep_awake.ps1`
  的「真去改」路径**都没在真机验过**。**跑 bootstrap 盯第 13 步 `[OK]`。**
- ⚠️ **`bootstrap_machine.py` 不能当"参数校验器"跑**：加 `--installer-dir /nonexistent` 验 argparse，
  **它真把 `config.local.yaml` 写了**（`installer_dir` / `security.tools_dir` 双双被改坏）。
  **要验参数只跑 `--help`。**
- ⚠️ **杀软口径**：是**病毒库误报、会自愈**。**顺序：探针干净就走 → 报 225 先更新病毒库再复探 →
  仍报 225 才关防护 / 加排除项。**
- **`交接归档/新机操作清单-发放版.md`（仅本机）用户 09-22 明确要保留** —— 纯步骤、带密码，
  **不是"多余备份"，别再提删**。
- **桌面发放版**：`tools/sync_handbook.py` 从仓库两份文档生成，真密码从 `$env:HALL_SHARE_PASSWORD`
  现场替换。**改完文档立刻跑一次，别等发之前。**
