# 会话交接

最后更新：2026-09-03

## 当前任务

华硕大厅 PC 端主流程自动化：**安装层已落地**。P0-03 及之后未做。未写禅道用例。

## 已拍板

- 远程仓库：https://github.com/Alvenovo/Huashuo-Dating-Automation.git （`origin/main`，首次提交已推送）
- 执行机：专用测试机；安装包目录 `C:\Users\ASUS\Desktop\华硕大厅`
- 版本策略 B：基线 `1.6.8.17S` → 升最新 `1.6.10.7S`；文件名末尾 `S` 不进版本号
- 安装器/卸载器：NSIS 3.08，静默 `/S`，需管理员

## 工程

- 库：`hall_auto/`（配置、版本解析、注册表、安装卸载）
- 配置：`config.yaml`；本机覆盖用 `config.local.yaml`（已 gitignore）
- 单元测试：`tests/unit`（本机 13 passed）
- P0-01/02：`tests/install`，无 `--allow-install` 时 skip，避免在办公机装卸
- 专用机入口：管理员运行 `run_p0_install.cmd`，或 `HALL_ALLOW_INSTALL=1` + pytest

## 计划表

- `自动化计划表.md`（仓库内）
- 可在对话旁打开 Canvas：`hall-automation-plan.canvas.tsx`

## 下一步

1. 专用机拉仓库并 `pip install -r requirements.txt`，安装包放在 `C:\Users\ASUS\Desktop\华硕大厅`，管理员跑 P0-01/02。
2. 通过后再做 P0-03/04 启动就绪（含黑屏、未知弹窗、冷启动 3 次）。不要先做截图比对主界面。
3. 夹具应用、测试账号仍缺，不阻塞安装层。

## 环境事实

- 办公机已装华硕大厅 1.6.8.17，只读注册表正常；**不要**在办公机加 `--allow-install`
- 专用机路径在办公机上不存在，属预期
