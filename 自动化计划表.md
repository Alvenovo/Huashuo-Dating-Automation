# 华硕大厅自动化计划表

最后更新：2026-09-16  
依据：`主流程自动化设计.md`。不是用例，不导入禅道。

**当前该做：** P0 全层（01/02/03/04/06/07）已于 2026-09-15 在本机通过，P0-05 探明无登录墙跳过。**P1 全部结清**：P1-A 真装真卸 + 更新用例全通过（2026-09-15 17:04 提权那轮 3 过 / 138s，靠 `HallAutoP1` 计划任务提权，0 人工；更新夹具 7-Zip 钉 26.02，目标版本运行时从向导标题读）；**sync 夹具已裁定**（2026-09-16）：同步页列的全是「其他电脑已装的日常软件」，没有安全可点的执行对象，用例做成**只读断言**（登录后 `NoAppListBox` 出现、条目名可读、`InstallAllBtn` 存在但绝不点），`fixture_apps.sync` 永久留空，实跑 1 过 / 24s；P1-B 账密登录 + 注册格式校验 2026-09-15 通过（3 过 / 69s、10 过 1 xfail / 186s），**短信登录、忘记密码往返改密码、微软 SSO 免密三条 2026-09-16 全部实跑通过并固化**；P1-C 主题切换通过（2 过，收尾还原原档位）。厂商向导根因已定：安装器/卸载器都是 **NSIS，UIA 完全读不到**，只能走 Win32 `#32770` + `WM_GETTEXT`；卸载确认页两块自绘按钮与隐藏文字按钮左右**相反**，只按文字点、绝不猜几何。**登录态大坑**：登录后主窗口 UIA Name 变「华硕应用商店」（Win32 标题不变），按标题懒解析的 WindowSpecification 全失效，登录态操作主窗口必须用 `concrete_main` 按 hwnd 重包。**剩下：** 只有 P2（会员/活动抽检，未开始）和四条可选产品问题（`VersionLabel` 占位、注册假验证码提交无反馈、忘记密码页容器名「注册窗口」、同步条目名占位符 `AppStore.NetWork.AppListInfo`）；本地多个 commit 未 push。

## 实施顺序

| 优先级 | 阶段 | 编号 | 做什么 | 状态 | 何时进行 | 依赖 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 底座 | UNIT | 文件名去 S、读配置、只读注册表 | 已完成 | 每次改代码、办公机随时 | 无 |
| 1 | P0 闸门 | P0-01 | 干净安装基线包，核对三路版本：注册表 DisplayVersion + exe FileVersion + 关于页 | 已通过（本机 2026-09-15 · 管理员 77s；关于页断言当日新增、已单独实跑验证） | 专用机、换包或验版本 | 管理员 + 安装包目录 |
| 1 | P0 闸门 | P0-02 | 从基线升到最新包，再核对三路版本 | 已通过（本机 2026-09-15 · 升到 1.6.11.4） | 紧接 P0-01 | P0-01 |
| 1 | 执行 | — | 仓库拉到专用机，管理员 `.\run_p0_install.ps1` | 已完成 | 写 P0-03 之前 | `C:\Users\ASUS\Desktop\华硕大厅` 里有两个 Setup.exe |
| 2 | P0 冒烟 | P0-03 | 启动，主窗口标题为华硕大厅；白名单协议/权限弹窗可自动过 | 已通过（1.6.11.4 · 新执行机） | 随 `run_p0_launch.ps1` | UIA 能找到主窗口 |
| 2 | P0 冒烟 | P0-04 | 超时内结束黑屏，进可交互首页；未知弹窗失败；冷启动 3 次；只比结构不比 Banner | 已通过（1.6.11.4 · 新执行机） | 紧接 P0-03 | P0-03 |
| 2 | P0 冒烟 | P0-05 | 若必须登录则账号密码登录，否则跳过 | 已探明无墙，跳过（2026-09-15 未登录态跑通 P0-06/07） | — | 安装/下载类动作是否要登录留到 P1 探 |
| 2 | P0 冒烟 | P0-06 | 搜 qq，至少 2 条标题命中 | 已通过（命中 腾讯QQ / QQ浏览器 / QQ影音） | 随 `run_p0_launch.ps1` | 探壳已确认：搜索框是原生 `Edit`，WebView2 内容 UIA 可读，**不需要 Playwright** |
| 2 | P0 冒烟 | P0-07 | 点进详情，看到名称和主按钮 | 已通过（详情页主按钮实测叫「一键安装」，不带应用名） | 紧接 P0-06 | P0-06 |
| 2 | 探壳结论 | — | 结果页下方挂第三方广告位（`ListItem`/`Hyperlink`，坐标落在窗口外） | 已记录 | 写任何列表断言前必读 | 标题只从**窗口可视区内**的「安装/打开/更新 X」主按钮取，禁止整页文本匹配 |
| 2 | 探壳结论 | — | 首装/升级后首跑先出独立顶层窗「权限确认窗口」（同意/取消），点完同意主窗口才出现 | 已修复 | 换包或清数据后首跑 | `wait_main_window` 等主窗口前先关前置窗 |
| 2 | 探壳结论 | — | 大厅会恢复上次的页面（实测首跑落在设置/关于页），就绪判定会超时 | 已修复 | 任何冷启动断言 | 结构缺失且存在 `BackBtn` 时点返回回首页 |
| 2 | 探壳结论 | — | 关于页版本节点 `aid=VersionLabel` 的 UIA name 是占位符 `ver`（产品无障碍缺陷），真版本号读不到 | 已记录 | 版本三路对照 | 截版本行放大 8 倍二值化后走 Windows 自带 OCR（`tools/ocr_text.ps1`，仅 zh-Hans 引擎） |
| 2 | 探壳结论 | — | 登录弹窗是独立窗 `aid=LoginWindow`，账号密码页 `MobileInputBox`/`PasswordSecInput`/`LoginBtn`，短信页 `CodeInput`/`CodeSendBtn`（name「发送验证码」），页签 `PassLoginSel`/`CodeLoginSel` | 已记录 | 写任何登录断言前必读 | 登录窗和用户菜单会顶掉主窗口句柄 → 一律按 **pid 跨窗口**找控件；不勾 CheckBox「同意协议」登录按钮是灰的；退出登录在独立菜单窗「华硕应用商店」里 |
| 2 | 探壳结论 | — | 「我的」页（`aid=Mine`）三页签 `UpTabBtn`/`UnTabBtn`/`NoTabBtn`，列表 `UpAppListBox`/`UnAppListBox`，条目内 `UpBtn`/`UnstallBtn`，更新页有 `UpdateAllBtn` | 已记录 | 写 P1-A 断言前必读 | 列表里全是微信/WPS/Python3 等日常软件：**禁止点 UpdateAllBtn 和任何非夹具条目**；卸载确认弹窗是子窗「卸载应用对话框」+ `ConfirmBtn`（name「卸载」） |
| 2 | 探壳结论 | — | 卸载应用列表就是系统卸载注册表的镜像；厂商安装器/卸载器都是 **NSIS**，安装包落在 `C:\AsusMCenterDownload`，卸载器把自己解包到 `%TEMP%\~nsuA.tmp\Au_.exe` | 已记录 | 夹具真装真卸、写任何厂商向导断言前必读 | 注册表 DisplayName 是可靠 oracle。**UIA 对 NSIS 完全是瞎的**（0 descendants，还把子控件误报成顶层窗），只能走 Win32 `#32770` + `WM_GETTEXT`。UIPI 边界：非提权**读得到**文字（`WM_GETTEXT` 在白名单），但 `BM_CLICK`/`SetForegroundWindow`/`PrintWindow` **被挡** → 卸载器是提权进程，点它必须提权，走 `HallAutoP1` 计划任务。换肤卸载页两块自绘按钮左右与隐藏文字按钮**相反**，只按文字点 |
| 3 | P1 | P1-A | 更新/卸载/同步三个列表能刷出数据 | 已通过（本机 2026-09-15，非提权，`-m "apps and not destructive"`） | 随 P1-A 套件 | 同步页未登录只有登录提示，属正常状态 |
| 3 | P1 | P1-A | 夹具应用真装真卸 + 点更新升级 | 已通过（本机 2026-09-15 17:04 · 3 过 / 138s · `admin=True`，装完卸完机器干净） | `MSYS_NO_PATHCONV=1 schtasks /Run /TN HallAutoP1`（0 人工），日志 `reports/p1_apps_run.log` | 提权（写 Program Files + HKLM，卸载器本身也是提权进程）。根因：厂商安装器/卸载器都是 **NSIS，UIA 完全读不到**，只能走 Win32 `#32770` 对话框 + `WM_GETTEXT`；卸载确认页两块自绘按钮尺寸位置全同，OCR 是**左「再想想」右「狠心卸载」**，与树里隐藏的 `卸载(&U)`/`取消(&C)` 左右**相反**，所以只按文字点、绝不猜几何。**更新夹具 = 7-Zip**：官方 26.02 静默装/卸干净、大厅更新列表认外部装的老版本；**点「更新」不是静默的**——大厅把 `7z2603-x64.exe` 下到 `C:\AsusMCenterDownload` 再拉起英文 NSIS 向导 `7-Zip 26.03 (x64) Setup`，点 Install 后注册表 26.02→26.03、`7zFM.exe` FileVersion 26.2.0.0→26.3.0.0、条目从更新列表消失；目标版本运行时从向导标题读、不写死，起点版本由 `tools/reset_fixture.py` 静默压回钉住的 26.02。列表条目用的是**大厅商品名**（`7-Zip（64位）`），跟注册表 DisplayName（`7-Zip 26.02 (x64)`）不一样，所以配置里 `name` 和 `registry_hint` 分开。sync 夹具已裁定：见下一行 |
| 3 | P1 | P1-A | 同步应用列表（登录后）只读断言 | 已通过（本机 2026-09-16 · 1 过 / 24s，`test_sync_list_logged_in`，标记 `apps`+`login`） | 有凭据时随 `-m login` 或单独跑，约 25s 占屏 | 要 `HALL_TEST_USER/PASSWORD`（无凭据自动 skip）。**裁定**：同步列表列的是「其他电脑已装的日常软件」（实测 11 条：QQ/百度网盘/钉钉等），没有安全可点的执行对象，`fixture_apps.sync` 永久留空，只断言容器 `NoAppListBox` 出现、条目名可读（真名在条目内 Text，ListItem name 是占位符 `AppStore.NetWork.AppListInfo`——无障碍缺陷候选）、`InstallAllBtn` 存在；**红线：绝不点「全部安装」和任何条目安装**。登录后主窗口 UIA Name 变「华硕应用商店」，spec 失效，必须用 `concrete_main` 按 hwnd 重包（用例自带起停，放模块最后别挪） |
| 3 | P1 | P1-B | 账号密码登录 + 退出登录 | 已通过（本机 2026-09-15 · 3 过 / 69s，`-m login`） | 有测试号时单独跑 | 凭据只走 `HALL_TEST_USER` / `HALL_TEST_PASSWORD`，不落盘 |
| 3 | P1 | P1-B | 注册格式校验（空/过短/纯数字/含中文/超长/两次不一致，不真注册、不发短信） | 已通过（本机 2026-09-15 · 10 过 1 xfail / 186s，`tests/launch/test_p1_register.py`） | 随 `-m login`，不需要凭据 | 公开示例号 + 假验证码即可：反馈 = `RegisterBtn` 变灰 + 对应提示行变红（UIA 读不到颜色，按节点矩形截屏数红像素，阈值 50）。假验证码点提交**无任何反馈**记 xfail，待开发确认 |
| 3 | P1 | P1-B | 短信 / 微软 / 忘记密码 | 已通过（本机 2026-09-16：短信人在环实跑通过固化 `test_sms_login_manual`；忘记密码**真往返改密码**（OLD→NEW→还原→双登录验证）用户终端实跑通过固化 `test_forgot_password_reset_manual`；微软 SSO 免密 1 过 / 31s 固化 `test_microsoft_login_sso`，本机有缓存 MS 会话可无人值守） | 短信/忘记密码标 `manual`，会真发短信/真改密码，只在用户自己终端 `-m manual -s` 跑（非 tty 自动 skip）；微软单条不需密码 | 短信/忘记密码要 `HALL_TEST_USER`（往返还要 `HALL_TEST_PASSWORD/NEW_PASSWORD`，只走环境变量）；微软邮箱走 `HALL_MS_USER` 或 config.local.yaml；出 MFA / 落密码页标人工，不算脚本失败 |
| 3 | P1 | P1-C | 浅色/暗色切换，重启后保持 | 已通过（本机 2026-09-15 17:20 · 2 过 / 118s，`-m settings`） | 随 P1-C，不需凭据、不装卸任何应用（约 2 分钟，屏幕会忽明忽暗并重启一次大厅） | P0-03。三档是 RadioButton `LightMode`/`DarkMode`/`AuroMode`（本机原档位 = 跟随系统）；选中读 `is_selected()`，**Toggle 模式不支持**；外观断言用主窗口平均亮度（浅 227~255 / 深 41~53，阈值 128）；**切换立刻生效、深色重启后保持**；按名字找会撞到同名 ToolTip，只按 AutomationId 找；点选用 SelectionItem 的 `select()` 不用坐标（隔壁就是「点击关闭按钮」两档，点偏会改掉关闭行为）；用例收尾还原原档位 |
| 4 | P2 | 会员 | 会员 vs 非会员：入口和拦截 | 未开始 | P1 有空再抽检 | 两种身份账号 |
| 4 | P2 | 活动 | 入口能开、能跳转；下线则跳过 | 未开始 | P1 有空再抽检 | 当期活动入口 |
| — | 范围外 | — | MuMu、微信小程序宿主、控制面板搜 QQ、6 包全量矩阵、截图点选 | 不做 | 除非以后单独立项 | — |

## 日常：什么时候跑哪一类

| 时机 | 跑什么 | 机器 | 失败了怎么办 |
| --- | --- | --- | --- |
| 改了 Python / 配置解析 | `tests/unit` | 办公机 | 先修计算，不要去装软件 |
| 新安装包、要验版本 | `tests/install`（P0-01/02） | 专用机 · 管理员 | 后面窗口测试全部停 |
| 安装已通过，问这版能不能用 | P0-03～07 冒烟 | 专用机 | 安装结果仍算数；启动失败则不做搜索 |
| 有测试号 / 夹具应用 | 对应 P1，一次只跑一类 | 专用机 | 验证码没回填记阻塞，不记产品缺陷 |
| 发版前 | P0 全套 + 已具备夹具的 P1；P2 可选 | 专用机 | P0 红则不发；活动下线可跳过 |

## 失败规则

- 安装失败 → 后面全停。
- 启动失败 → 不做搜索/详情，安装结果保留。
- 搜索失败 → 不做详情，版本正确仍算通过。
