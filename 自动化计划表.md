# 华硕大厅自动化计划表

最后更新：2026-09-03  
依据：`主流程自动化设计.md`。不是用例，不导入禅道。

**当前该做：** 专用机 `git pull` 后跑 `.\run_p0_launch.ps1`（P0-03/04）。不要先做搜索。

## 实施顺序

| 优先级 | 阶段 | 编号 | 做什么 | 状态 | 何时进行 | 依赖 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 底座 | UNIT | 文件名去 S、读配置、只读注册表 | 已完成 | 每次改代码、办公机随时 | 无 |
| 1 | P0 闸门 | P0-01 | 干净安装基线包，核对 DisplayVersion + FileVersion | 专用机已通过 | 专用机、换包或验版本 | 管理员 + 安装包目录 |
| 1 | P0 闸门 | P0-02 | 从基线升到最新包，再核对版本 | 专用机已通过 | 紧接 P0-01 | P0-01 |
| 1 | 执行 | — | 仓库拉到专用机，管理员 `.\run_p0_install.ps1` | 已完成 | 写 P0-03 之前 | `C:\Users\ASUS\Desktop\华硕大厅` 里有两个 Setup.exe |
| 2 | P0 冒烟 | P0-03 | 启动，主窗口标题为华硕大厅；白名单协议/权限弹窗可自动过 | 代码已完成，专用机待跑 | 安装已通过，现在做 | UIA 能找到主窗口 |
| 2 | P0 冒烟 | P0-04 | 超时内结束黑屏，进可交互首页；未知弹窗失败；冷启动 3 次；只比结构不比 Banner | 代码已完成，专用机待跑 | 紧接 P0-03 | P0-03 |
| 2 | P0 冒烟 | P0-05 | 若必须登录则账号密码登录，否则跳过 | 缺账号则阻塞 | 探明是否有登录墙之后 | 密码测试号（若有墙） |
| 2 | P0 冒烟 | P0-06 | 搜 qq，至少 2 条标题命中 | 未开始 | 能点到搜索框之后 | UIA 或 WebView2 调试口 |
| 2 | P0 冒烟 | P0-07 | 点进详情，看到名称和主按钮 | 未开始 | P0-06 通过后 | P0-06 |
| 3 | P1 | P1-A | 更新/卸载/同步列表 + 夹具应用真装真卸 | 阻塞 | P0 稳定后单独跑 | 四个夹具应用名称 |
| 3 | P1 | P1-B | 短信 / 微软 / 注册格式 / 忘记密码 | 阻塞 | 有测试号后单独跑 | 短信号、微软号 |
| 3 | P1 | P1-C | 浅色/暗色切换，重启后保持 | 未开始 | 能进设置页之后 | P0-03 |
| 4 | P2 | 会员 | 会员 vs 非会员：入口和拦截 | 未开始 | P1 有空再抽检 | 两种身份账号 |
| 4 | P2 | 活动 | 入口能开、能跳转；下线则跳过 | 未开始 | P1 有空再抽检 | 当期活动入口 |
| — | 范围外 | — | MuMu、微信小程序宿主、控制面板搜 QQ、6 包全量矩阵、截图点选 | 不做 | 除非以后单独立项 | — |

## 日常：什么时候跑哪一类

| 时机 | 跑什么 | 机器 | 失败了怎么办 |
| --- | --- | --- | --- |
| 改了 Python / 配置解析 | `tests/unit` | 办公机 | 先修计算，不要去装软件 |
| 新安装包、要验版本 | `tests/install`（P0-01/02） | 专用机 · 管理员 | 后面窗口测试全部停 |
| 安装已通过，问这版能不能用 | P0-03～07 冒烟 | 专用机 | 安装结果仍算数；启动失败则不做搜索 |
| 有测试号 / 夹具应用 | 对应 P1，一次只跑一类 | 专用机 | 验证码没回填记阻塞，不记产品缺陷 |
| 发版前 | P0 全套 + 已具备夹具的 P1；P2 可选 | 专用机 | P0 红则不发；活动下线可跳过 |

## 失败规则

- 安装失败 → 后面全停。
- 启动失败 → 不做搜索/详情，安装结果保留。
- 搜索失败 → 不做详情，版本正确仍算通过。
