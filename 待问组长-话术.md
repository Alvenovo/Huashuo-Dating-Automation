# 待问组长 · 话术（2026-09-18 更新）

> 组长回复已到。下面**只列还需要问的**，已确认的不再重复。

---

## 一、组长已确认（2026-09-18）

| # | 问题 | 答复 | 落地情况 |
|---|------|------|---------|
| 1 | 「功能正常」判定标准 | 主流程功能测试正常即可 | ✅ 已按此口径做 |
| 2 | 验证目标 | 版本放量前的兼容性验证 | ✅ 报告按兼容性矩阵出 |
| 3 | 20 台是否同型号 | 不一样；不论装没装都先检测，有就删掉重下；**要能自动下载安装包** | ✅ 已实现（fetch.py + reset_machine.py） |
| 4 | 机器到位节奏 | 先跑几台，后面再跑全部 | ✅ 已按分片支持 |
| 5 | 屏幕常亮 | 测试机无人碰屏，脚本自己保证常亮即可 | ✅ 已实现（awake.py，自动生效） |
| 6 | 20 台并发打服务端 | **不清楚，反问有什么意义** | ⚠️ 需补问，见下 |
| 7 | 测试账号数 | 1 个，组长说不会踢 | ✅ 已适配（登录限流） |

---

## 二、还需要问的（3 条）

### 第 1 条（最关键）：安装包从哪下？

组长要求"脚本自动下载对应大厅"，但现在**我不知道从哪下**。需要明确：

> X哥，自动下载这块我做进去了，但得知道从哪下：
>
> 1. **下载地址给一个**？是官网的公开直链，还是内网/测试环境的包服务器？
> 2. **要下哪个版本**？基线包 + 最新包各一个文件名（比如现在配置里写的 `myappstore_1.6.8.17S_Setup.exe` 和 `myappstore_1.6.10.7S_Setup.exe` 还是不是最新的）？
> 3. **有没有 sha256 摘要**？给了能防下到半截包或被人换包，不给也能跑但少一层保险。

**为什么卡**：地址没有的话，代码里那条下载路径永远走不通，20 台机器还是得靠人拷包。

---

### 第 2 条：并发这块到底要不要管？（组长反问了，我得解释清楚）

组长说"不清楚，有什么意义"。**这条其实很重要**，我换个说法再问一次：

> X哥，第 6 条我解释一下为什么问：
>
> 20 台机器同时跑，每台都会真登录账号、真点"检查更新"、真下载安装包。这些动作是打到**服务端**的，不是打在本机。
>
> 我担心的不是服务端被打挂，是**限流**——如果服务端看到 20 个同一账号/IP 的并发请求，可能返回失败或降级响应。那我的脚本会把"服务端限流"误判成"大厅有 bug"，报告出来的全是假缺陷，白跑一轮。
>
> 所以我按"打服务端的用例最多 5 台并发"设了个上限（登录类、真装真卸类）。
>
> **要您定的是**：这个 5 是无所谓，还是需要更保守？或者能不能帮我问下服务端那边 20 台并发要不要打招呼？
>
> **如果您觉得没必要管**，我就按全速跑，但报告里会标注"并发未限流"，万一是环境问题不背锅。

---

### 第 3 条：几台先跑，跑哪几台？

组长说"先跑几台"。需要具体化：

> X哥，先跑几台的话：
>
> 1. **机器什么时候能到位**？我先在 1~2 台上把链路跑通（铺设 → 跑 → 出报告），验证没问题再铺开。
> 2. **第一批给我哪几台**？最好是环境差异大的（比如不同 Windows 版本），这样能提前暴露兼容性问题。
> 3. **机器上现在有没有装大厅**？装过的话我会先卸掉重装，保证起点干净。

---

## 三、附：这轮新增的能力（同步，不用您定）

- **自动下载安装包**：机器上没包会自动拉，本地有包优先用本地的（不联网）
- **跑批前复位**：检测已装的大厅 → 自动卸载 → 备齐安装包，保证每台起点一致
- **屏幕常亮**：脚本自己调系统 API 防息屏，不用改电源计划、不用管理员
- **一键铺环境**：新机器一条命令装好 Python/依赖/计划任务
- **结果汇总**：每台回传截图和结果，控制机出「节点 × 用例」矩阵，一眼看出哪台挂在哪条

---

## 四、内部备注（不要发出去）

**第 1 条（下载地址）是硬卡点。** 组长要求自动下载，但没给地址，这条不落实，
"自动下载"就是一句空话，20 台还是得人肉拷包。

**第 2 条要换个说法问。** 组长第一次说"不清楚"是因为我原来的问法太技术
（问了"并发上限多少"），他没法答。这次要讲清楚**风险是什么**（假失败污染报告），
再把选择权给他（限流 / 不限流但标注）。

**第 3 条优先级最低**，机器到位自然会知道，但提前问能早点规划。

**已确认的 1、2 条口径**：
- 「功能正常」= 主流程能走通（启动、搜索、进详情、登录、检查更新）
- 验证目标 = 版本放量前的兼容性验证 → 报告要按**环境维度分组**，结论是
  "哪些环境有问题"，不是"跑了几次都过"
