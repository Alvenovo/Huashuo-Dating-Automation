# 待问组长 · 话术（2026-09-18 15:30 更新）

> 组长回复已到。下面**只列还需要问的**，已确认的不再重复。
> **当前真正待问只剩 2 条**：并发转发（第 2 条）、机器安排（第 3 条）。
> 下载地址那条组长明确没有，已转共享盘方案，不问了。

---

## 一、组长已确认（2026-09-18）

| # | 问题 | 答复 | 落地情况 |
|---|------|------|---------|
| 1 | 「功能正常」判定标准 | 主流程功能测试正常即可 | ✅ 已按此口径做 |
| 2 | 验证目标 | 版本放量前的兼容性验证 | ✅ 报告按兼容性矩阵出 |
| 3 | 20 台是否同型号 | 不一样；不论装没装都先检测，有就删掉重下；**要能自动下载安装包** | ✅ 已实现（fetch.py + reset_machine.py） |
| 4 | 机器到位节奏 | 先跑几台，后面再跑全部 | ✅ 已按分片支持 |
| 5 | 屏幕常亮 | 测试机无人碰屏，脚本自己保证常亮即可 | ✅ 已实现（awake.py，自动生效） |
| 6 | 20 台并发打服务端 | **不清楚，反问有什么意义** | ⚠️ 已自行保守处理，仍需一次转发，见下 |
| 7 | 测试账号数 | 1 个，组长说不会踢 | ✅ 已适配（登录限流） |
| 8 | 官方下载地址 | **没有**（2026-09-18 二次确认） | ✅ 已用自建共享盘替代，正式启用 |

---

## 二、还需要问的

> 第 1 条已关闭（组长没有下载地址，方案自行改为共享盘，只需同步）。
> **真正需要组长回话的只有第 2 条**，第 3 条顺带问。

### 第 1 条（已关闭，仅需同步）：安装包改走共享盘分发

组长二次确认「没有官方下载地址」。**所以"自动下载"这条路作废，改为共享盘分发，已落地并验证。**

给组长的话（同步口径，不需要他决策）：

> X哥，下载地址这事您说没有，那我不走下载了，改成**共享盘分发**：
> 我把基线包和目标包放在一台机器的共享目录上，20 台测试机跑批时自己去取，
> 取不到才降级。现在已经实测通了（392MB 的包从共享盘拷进来，校验一致）。
>
> 有一件事需要提醒您：**这台当"包源"的机器得开着**，它关了其他机器就取不到包。
> 现在是用我这台笔记本顶着的，短期验证没问题；
> **如果后面要长期跑或者机器多，建议给一台常开的测试机当包源**，您看行不行。

**为什么不问「地址能不能再找找」**：已经问了两次，答案都是没有。
再问第三遍是浪费彼此时间，不如把绕路方案做扎实，只同步一个前提条件（包源机器要常开）。

**代码侧已落地**：`hall_auto/fetch.py` 四级查找链
（本地 → 缓存 → 共享盘 → 下载），共享盘不可达 5s 超时后静默降级；
`config.yaml` 的 `package_share.dirs` 已填 `//192.168.0.4/hall-packages`；
`tools/bootstrap_machine.py` 第 5 步自动 `net use` 连共享盘（凭据走环境变量）。
**`download.url_template` 保持为空**——留着占位，将来有地址了填上即可无缝启用。

---

### 第 2 条：并发这块我自己处理了，只需您帮一个小忙

> **这条不用您做技术判断，我已经按保守值做了，只是同步一下 + 求一个转发。**

---

**给组长的话（可直接发）：**

> X哥，上次问并发那个问题，是我问法太绕了，我换个说法。
>
> **背景**：20 台机器同时跑的时候，有一部分用例会真登录账号、真点「检查更新」、真下载安装包。
> 这些动作打到服务端，不打在本机。
>
> **我担心的事**：服务端要是看到同一个测试号短时间挤进太多请求，可能会限流或返回失败。
> 这本身不是问题——问题是**我的脚本分不清「服务端限流」和「大厅有 bug」**，
> 会把限流误判成产品缺陷写进报告，那这轮就白跑了。
>
> **我已经自己处理了**，不用您定：登录、真装真卸、应用更新这几类打服务端的用例，
> 我限制**最多 5 台同时跑**，其余纯界面操作（不联网的）就全速并行。这样应该碰不到限流线。
>
> **只需要您帮一个忙**：能不能帮我问一下服务端那边的同事——**版本放量前的自动化测试，
> 20 台机器打过来要不要提前打个招呼**？如果他们说不用，我就按现在这样跑；
> 如果他们说需要报备，麻烦给我一个联系人。
>
> 如果您觉得没必要问，我就直接跑，报告里会标注一句「并发未与服务端对齐」，
> 万一出现批量失败，我好区分是环境问题还是产品问题。

---

**为什么这样改（不要发出去）**：

上一版话术有三个毛病，组长说"不清楚，有什么意义"是正常的：

1. **太长**。五行技术铺垫，组长扫一眼就过去了。
2. **把决策推给了不懂的人**。问他"5 台够不够保守"——他没有依据回答，
   答了也是瞎答。**要问的是"能不能帮我转给服务端同事"，这是个他能做的小动作**。
3. **没说清代价**。他只看到"你在担心一件事"，没看到"不处理会怎样"。
   这次把代价说透了：**报告里全是假缺陷**。

**关键转向**：从「请您决策」改成「我已决策，请您转发」。组长最怕被问技术选择题，
最喜欢"已经做好了，你只需要点个头/转个话"。

**如果组长还是说"不用管"**：
那就全速跑，但**报告里必须标注并发口径**。真出现批量失败时，
第一件事是看失败是否集中在"打服务端"的用例上——如果是，大概率是限流不是 bug。

**实际实现（代码已按此）**：`hall_auto/suites.py` 里
`install` / `apps-lifecycle` / `login` / `wb` 四类 `max_concurrent=5`，
`login-manual` 串行（人在环），其余套件不限并发。

---

### 第 3 条：几台先跑，跑哪几台？（低优先级，可顺带问）

组长说"先跑几台"。需要具体化：

> X哥，先跑几台的话：
>
> 1. **机器什么时候能到位**？我先在 1~2 台上把链路跑通（铺设 → 跑 → 出报告），验证没问题再铺开。
> 2. **第一批给我哪几台**？最好是环境差异大的（比如不同 Windows 版本），这样能提前暴露兼容性问题。
> 3. **机器上现在有没有装大厅**？装过的话我会先卸掉重装，保证起点干净。

---

## 三、附：这轮新增的能力（同步，不用您定）

- **共享盘分发安装包**：没下载地址的替代方案，20 台机器自己从共享目录取包，实测已通
- **跑批前复位**：检测已装的大厅 → 自动卸载 → 备齐安装包，保证每台起点一致
- **屏幕常亮**：脚本自己调系统 API 防息屏，不用改电源计划、不用管理员
- **一键铺环境**：新机器一条命令装好 Python/依赖/计划任务
- **结果汇总**：每台回传截图和结果，控制机出「节点 × 用例」矩阵，一眼看出哪台挂在哪条

---

## 四、内部备注（不要发出去）

**第 1 条（下载地址）已关闭，转为共享盘方案。** 组长两次都说没有，不再问第三遍。
现在只需要他认一个前提：**包源机器要常开**。这条不说清，将来共享盘取不到包会怪到脚本头上。

**第 2 条（并发）本来是我问错了。** 组长第一次答"不清楚，有什么意义"是对的——
我问的是"并发上限该设多少"，这是让他替我做技术决策，他没有依据答。
**改成"我已按 5 台限流做好，只需您帮忙转给服务端同事问一句"**，
就变成他能做的小动作。这是这版话术最重要的改动。

**第 3 条（几台先跑）优先级最低**，机器到位自然会知道，但提前问能早点规划。

**已确认的口径**：
- 「功能正常」= 主流程能走通（启动、搜索、进详情、登录、检查更新）
- 验证目标 = 版本放量前的兼容性验证 → 报告要按**环境维度分组**，结论是
  "哪些环境有问题"，不是"跑了几次都过"
- 测试账号 1 个，多机同时登录不会互踢（组长确认）
