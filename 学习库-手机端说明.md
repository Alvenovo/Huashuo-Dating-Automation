# 手机上看学习库 · 操作说明

> 目标：在手机上流畅阅读 `学习库/` 的 31 篇笔记（含双链、折叠块、记忆卡样式）。
>
> **两条路，选一条就行：**
> - **路线 A（最快，2 分钟）**：用已生成的单文件 HTML，微信发给自己就能看。← **推荐先用这条**
> - **路线 B（长期，15 分钟）**：Obsidian + Git 插件，手机上也能编辑、和电脑双向同步。

---

## 🅰️ 路线 A · 单文件 HTML（最快，2 分钟）

**已经生成好了**：`学习库-手机版.html`（项目根目录，约 355 KB）

它把 31 篇笔记全部打包进一个文件，手机上用**浏览器**打开即可：

- 左上角 ☰ 出目录（8 个分组）
- `[[双链]]` 可点击跳转
- 自测题折叠块可展开
- 表格、代码块、记忆卡样式全都在
- **完全离线**，不联网也能看

### 怎么传到手机（选一种）

**① 微信文件传输助手（最省事）**
```
电脑微信 → 文件传输助手 → 把这个 .html 拖进去发送
→ 手机微信打开 → 点这个文件 → 选「用浏览器打开」
```

> [!tip] 如果微信里点开显示乱
> 微信内置浏览器对 JS 有限制。别在微信里直接看，
> 点右上角 `···` → 「用其他应用打开」/「在浏览器中打开」。

**② QQ 我的电脑** —— 同理，发给「我的电脑」。

**③ 邮件** —— 发给自己，手机邮件 App 里点开。

**④ 数据线 / 网盘** —— 直接拷进手机存储，用文件管理器打开。

### 重新生成

改了笔记后想更新手机版，跑一次：
```bash
.venv\Scripts\python.exe .workbuddy-ai\tools\build_mobile_html.py
```
（生成器在 `.workbuddy-ai/tools/`，该目录不进 git）

---

## 🅱️ 路线 B · Obsidian + Git（长期方案）

适合你想在手机上**也记笔记、改掌握度、和电脑同步**。配置稍麻烦，但之后一劳永逸。

### 📋 先看清全貌

```
GitHub 私有仓 Alvenovo/Huashuo-Dating-Automation   ✅ 已是 private
        │
        │  ← 手机只拉这一个子目录（sparse checkout）
        ▼
   学习库/
   ├── 00-Dashboard/     ← 先读这里
   ├── 01-方案设计/
   ├── ... 07-用例与断言策略/
   └── .obsidian/         ← 样式配置，会自动带下来
        │
        ▼
   手机 Obsidian 打开 学习库/ 作为 Vault
```

**为什么要用 sparse checkout**：仓库里 `reports/` 有 **2 万多个文件**（1.1 万张 PNG），
手机上全拉下来又慢又占地方，而且跟学习完全无关。

---

## ✅ 第一步：电脑端 · 确认仓库是私有（已完成）

仓库 `Alvenovo/Huashuo-Dating-Automation` **已经是 private**，无需任何操作。
（这条只对路线 B 有意义；路线 A 完全不涉及 GitHub。）

> [!tip] 想自己验证一下？
> 用下面这个命令（关键是**切断所有已保存的凭据**，否则测的只是"我能不能访问"）：
>
> ```bash
> env -u GITHUB_TOKEN -u GH_TOKEN \
>   GIT_TERMINAL_PROMPT=0 GIT_ASKPASS=/bin/echo \
>   GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null \
>   git -c credential.helper= clone --depth 1 \
>   https://github.com/Alvenovo/Huashuo-Dating-Automation.git /tmp/x
> ```
>
> - 报 `could not read Username for 'https://github.com'` → ✅ **私有**（正常）
> - 克隆成功 → ⚠️ 是公开的，需要注意

---

## ✅ 第二步：手机端 · 生成访问令牌（3 分钟）

手机上的 git 不能用 GitHub 密码，要用 **Personal Access Token（个人访问令牌）**。

> **什么是 Token？** 把它想成**一把专用钥匙**——只给"读代码"这一件事开门，
> 不能改密码、不能删仓库。比直接用密码安全得多。

```
① 手机或电脑浏览器打开
   https://github.com/settings/tokens?type=beta

② 点「Generate new token」

③ 起个名字：Obsidian-手机

④ Expiration（有效期）：选 90 days 或更长（到期要重新生成）

⑤ Repository access：选「Only select repositories」
   → 勾选 Huashuo-Dating-Automation

⑥ Permissions（权限）：只要一项
   → Repository permissions → Contents → 设为 Read-only

   （只用来看，不需要写回，所以只给读权限 —— 更安全）

⑦ 点「Generate token」

⑧ ⚠️ 立刻复制那串 ghp_ 开头的字符
   —— 它只显示一次，关掉页面就再也看不到
```

> [!danger] 这串 Token 等同密码
> - 不要发微信、不要写在笔记里、不要提交进 git
> - 只在这台手机上输入一次
> - 丢了就重新生成一个（旧的作废）

---

## ✅ 第三步：手机端 · 装 Obsidian + Git 插件（8 分钟）

### 3.1 装 Obsidian

- **iPhone**：App Store 搜 `Obsidian`（图标是紫色宝石）
- **Android**：应用商店 / Play 商店搜 `Obsidian`

免费，无需登录。

### 3.2 建一个空 Vault

打开 App → `Create new vault` → 名字随便起（比如 `临时`）→ 创建。

> [!tip] 为什么先建临时的？
> 因为 Git 插件需要在一个 Vault 里才能装，
> 但最终要打开的 Vault 是**克隆下来的** `学习库/`。
> 所以先用临时的把插件装上，之后再换。

### 3.3 装 Obsidian Git 插件

```
① 打开 Obsidian 设置（左下角齿轮）
② 找到「第三方插件 / Community plugins」
③ 点「关闭安全模式 / Turn on community plugins」
④ 点「浏览 / Browse」
⑤ 搜索框输入：Git
⑥ 找到作者是 Vinzent 的「Obsidian Git」→ 点「安装 / Install」
⑦ 安装完点「启用 / Enable」
```

> [!warning] 安全提示
> 这一步是**从 Obsidian 官方插件市场装插件**，等同从应用商店装 App，是安全的。
> 但请**只装 Git 这一个**。插件市场里有些插件会读取你的笔记内容，别乱装。

### 3.4 用 sparse checkout 克隆（关键步骤）

> [!important] 手机上没法直接执行 git 命令
> 这就是为什么需要 `Obsidian Git` 插件 —— 它内置了一个 git 环境。
>
> 但插件的图形界面**不支持 sparse checkout**，所以要用它提供的
> **"Source Control 面板 → 打开终端"** 或 **custom command** 功能。

**做法：**

```
① 在临时 Vault 里，打开命令面板
   （手机：下拉或点右上角 → 找命令面板）

② 搜「Obsidian Git: Open source control view」→ 打开 Git 面板

③ 在 Git 面板右上角找到 ⋯ 菜单 → 选「Open in terminal」
   （部分版本叫「Git 终端」；若没有此选项，见下方"备选方案"）
```

**在终端里依次执行：**

```bash
# ① 进入 vault 所在目录（插件的终端默认已在 vault 内）
cd ..

# ② 克隆时不要立刻检出文件（只拉历史）
git clone --no-checkout https://github.com/Alvenovo/Huashuo-Dating-Automation.git repo
cd repo

# ③ 开启 sparse checkout（只拉指定目录）
git sparse-checkout init --cone

# ④ 指定只拉 学习库/ 这一个目录
git sparse-checkout set 学习库

# ⑤ 现在才真正把文件拉下来
git checkout main

# ⑥ 看看结果（应该只有 学习库/ 和少量根文件）
ls
```

**完成后**，回到 Obsidian → `Open another vault` → `Open folder as vault`
→ 选 `repo/学习库` 这个目录。

### 3.5 备选方案（如果找不到终端）

如果插件版本没有终端，用这个**更简单的做法**：

```
① 先用插件把整个仓库克隆下来（会慢，会占空间）
   Obsidian Git → Clone an existing remote repo
   → 填 https://github.com/Alvenovo/Huashuo-Dating-Automation.git
   → 填 Token 作为密码

② 克隆完成后，进入 repo/学习库 作为 Vault
   （reports/ 虽然占了空间，但不影响阅读）

③ 之后有空再清理：用手机上的文件管理器删掉 reports/ 目录
   （git 会认为你在本地删了文件，git status 会显示，但没关系）
```

**或者最省事的第三条路**：

```
① 电脑上把 学习库/ 单独复制一份到 OneDrive 目录
② 手机装 OneDrive App → 直接能看
③ 缺点：手机上不能像 Obsidian 那样渲染双链和折叠块
```

---

## 🎨 第四步：确认样式生效

打开笔记后，应该看到：

| 效果 | 说明 |
|---|---|
| 标题左侧有**蓝色竖条** | `study-vault.css` snippet 生效 |
| 表格有**外框 + 隔行底色** | 同上 |
| 代码块左侧有竖线 | 同上 |
| 自测题是**虚线框**且默认折叠 | callout `[!question]-` |
| 内部链接带**虚线下划线** | 可点击跳转 |
| 顶部有 `难度` / `阅读时间` | frontmatter 属性 |

**如果样式没生效**：
```
设置 → 外观 / Appearance → 拉到最下「CSS 代码片段 / CSS snippets」
→ 确认 study-vault 是开启状态
```

---

## 📱 手机上的阅读建议

> [!tip] 手机屏幕小，这样读最省力
> 1. **从 `00-Dashboard/00-MOC.md` 开始** —— 它是总入口，有阅读路线
> 2. 手机上**优先读「🧠 记忆卡」** —— 短、密、好记，适合碎片时间
> 3. 长表格（控件表）**留到电脑上看** —— 手机上要横向滚动，很烦
> 4. 用**书签功能**把 Dashboard 五件套存起来（配置里已经预置好了）

**通勤/睡前最适合读的三篇**（都是短且记忆密度高的）：
- `00-Dashboard/03-考试陷阱集.md` → 只看「🎴 一页速记」那张表
- `00-Dashboard/04-术语表.md` → 只看「最难记的 5 个词 · 记忆卡」
- `05-跑法与提权/02-UIPI边界.md` → 只看「🧠 记忆卡」四句口诀

---

## ❓ 常见问题

**Q：手机上改了笔记，会同步回电脑吗？**
A：如果你给的 Token 是 Read-only，**不会**（只读）。
想双向同步，Token 要给 `Contents: Read and write` 权限。
但建议**手机只读**，避免和电脑产生**冲突副本**。

**Q：出现"冲突副本"文件怎么办？**
A：那说明两端同时改了同一个文件。手动对比两个版本，保留对的那个，删掉另一个。

**Q：Token 过期了怎么办？**
A：重新生成一个，在 Obsidian Git 插件的设置里替换。

**Q：手机上想不出网也能看吗？**
A：可以。Obsidian 是**本地应用**，克隆下来之后完全离线可用。

**Q：为什么我的笔记里有些链接点不开？**
A：`Obsidian Git` 是平铺克隆的，链接都用文件基名（如 `[[02-UIPI边界]]`），
在 Obsidian 里会自动解析。如果点不开，检查是否把 `学习库/` 整体作为 Vault 打开了
——**必须从 `学习库/` 这一层打开，不能从仓库根打开**。

---

## 🔒 安全清单（做完核对一遍）

- [x] 仓库是 **private**（已确认）
- [ ] Token 权限设为 **Read-only**（手机只读）
- [ ] Token 设了**有效期**（不是 No expiration）
- [ ] Token **没有**发到微信 / 写进笔记 / 提交进 git
- [ ] 手机 Obsidian 里**只装了 Git 这一个**第三方插件

---

*最后更新：2026-09-18*
