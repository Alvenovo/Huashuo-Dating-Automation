# 华硕大厅主流程自动化设计

最后更新：2026-09-02  
文档类型：**自动化方案**（范围、技术、环境、风险）。不是用例，不含逐步操作，不导入禅道。

---

## 0. 已对齐结论

| 项 | 结论 |
| --- | --- |
| 文档 | 先方案，后工程目录，再用例 |
| 执行机 | 专用测试机；不在当前办公机跑 |
| 安装包路径 | 配置项，默认：`C:\Users\ASUS\Desktop\华硕大厅`。本机另有参考目录：`D:\华硕大厅安装包` |
| 版本策略 | **B**：核对当前包 → 再升到目录内最新包 |
| 版本口径 | 安装包文件名末尾 `S` **不进入**系统版本号。`1.6.8.17S` 对应系统 `1.6.8.17` |
| 版本主断言 | 卸载注册表 `DisplayVersion` + `AsusMemberCenter.exe` 的 `FileVersion` |
| 控制面板 UI | 不作为每轮自动化主路径（它只是注册表的展示） |
| 技术栈 | Python + pytest；安装/注册表用脚本；窗口用 pywinauto/UIA；WebView2 能挂调试口再用 Playwright |
| 账号 | 冒烟只用账号密码测试号；短信/注册/找回要人工回填验证码；微软号半自动，遇 MFA 停等 |
| 商店装卸 | 专用机允许真装真卸，**只用约定夹具应用** |
| 默认不做 | MuMu、微信小程序宿主、图像识别当主路径、把 8 个模块串成一条超长脚本 |

---

## 1. 产品事实（方案依据）

华硕大厅对外显示名是「华硕大厅」，安装包名是 `myappstore_*_Setup.exe`，进程是 `AsusMemberCenter.exe`。  
安装目录：`C:\Program Files (x86)\ASUS\ASUS Member Center`。  
卸载项：`HKLM\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall\AsusMemberCenter`。

客户端是 **WPF 外壳 + WebView2 业务页**。  
登录还带微软 MSAL。安装目录里另有 MuMu、微信 WMPF 等宿主，**本期不测**。

本机已见到的安装包：

| 安装包 | 期望系统版本 |
| --- | --- |
| `myappstore_1.6.7.3S_Setup.exe` | 1.6.7.3 |
| `myappstore_1.6.8.7S_Setup.exe` | 1.6.8.7 |
| `myappstore_1.6.8.17S_Setup.exe` | 1.6.8.17 |
| `myappstore_1.6.9.7S_Setup.exe` | 1.6.9.7 |
| `myappstore_1.6.9.12S_Setup.exe` | 1.6.9.12 |
| `myappstore_1.6.10.7S_Setup.exe` | 1.6.10.7 |

日常主流程只强制两条：**当前基线包**（现为 `1.6.8.17S`）和 **目录内最新包**（现为 `1.6.10.7S`）。  
6 包全量升降级不进日常，只作为发布可选闸门。

搜索「QQ 出 QQ / QQ 音乐」指的是 **大厅内应用目录搜索**，不是控制面板搜索。

---

## 2. 设计原则

1. **主流程要短、要稳。** 安装、启动、首页、搜索、详情是一条；账号、装卸载、会员、活动各自独立，失败互不影响。
2. **能读系统状态就不要点 UI。** 版本用注册表和文件版本；已装应用用系统卸载项；UI 只验证「人看得到的那一层」。
3. **WebView2 按能力降级。** 外壳控件用 UIA；页面点不到就标受阻，不假装全自动。
4. **数据依赖全部夹具化。** 搜索词、可更新应用、可卸载应用、会员账号，都写进配置。线上运营一变，只改配置，不改脚本结构。
5. **破坏性操作可逆且范围小。** 专用机可以真装真卸，但只动夹具应用和大厅自己；不把随机软件当试验品。

---

## 3. 范围分层

### P0 冒烟（真正的主流程）

目标：新包或日常回归时，十几分钟内回答「这版能不能用」。

| 编号 | 能力 | 通过标准（方案级） |
| --- | --- | --- |
| P0-01 | 安装基线包 | 无已装则安装；已装则先卸再装。注册表显示名含「华硕大厅」，`DisplayVersion` = 包名去掉 `S` 后的四段号。exe `FileVersion` 相同 |
| P0-02 | 升级到最新包 | 在基线已装前提下直接跑更新包（覆盖安装）。版本变为最新包去掉 `S` 后的四段号 |
| P0-03 | 启动 | 进程起来，主窗口标题为「华硕大厅」。已知的首次协议/权限弹窗按配置自动同意 |
| P0-04 | 启动就绪（首页） | 在超时内从黑屏进入可交互首页：导航或内容区可见，搜索入口在（若本版有）。**未知弹窗 = 失败**（截图 + 标题/正文）。连续冷启动 N 次（默认 3）均须就绪。不把运营 Banner、活动素材差异判失败 |
| P0-05 | 登录墙 | 若搜索/详情必须登录：用账号密码测试号登录成功。否则本步跳过 |
| P0-06 | 搜索 | 配置关键词（默认 `qq`）返回列表；列表中至少两条标题命中该关键词（如 QQ、QQ 音乐）。无结果则失败，不把「运营下架」默认为通过 |
| P0-07 | 详情 | 从搜索结果点进一条，详情页出现名称、安装/打开/更新类主按钮 |

P0 **不**做：短信、微软登录、注册、找回密码、真下载安装第三方应用、主题、会员、活动页、像素级截图对比主界面。

组长反馈的「刚进大厅黑屏后弹窗」「主界面有时显示不一致」归 **P0-04**，不单独立项、不放到 P1。口径：

- **黑屏**：允许短暂过渡；超时仍全黑或 WebView 空白 = 失败。
- **弹窗**：白名单（协议、权限、已知更新提示）可点；名单外弹窗失败并留证。禁止盲点未知框。
- **显示不一致**：只比「结构是否就绪」（导航、内容区、搜索入口是否都在），不比 Banner 文案。未登录 vs 已登录、会员 vs 非会员看起来不同，算身份差异，走 P0-05 / P2，不当 P0-04 缺陷。
- **偶发**：P0-04 连续冷启动 3 次；要压 50 次过夜的，另开 P1 浸泡，不塞进日常冒烟。

大厅 **自身卸载** 属于 P0-01 的前置，不算商店应用卸载。

### P1 独立套件（有夹具再上）

每个套件单独跑，不串进 P0。

**P1-A 应用生命周期**

- 检测：更新列表、可卸载列表、可同步列表能刷新出数据（允许空，但空要区分「接口失败」和「本来没有」）。
- 执行：对 **配置里的夹具应用** 做下载安装、更新、卸载、同步。夹具名单待定。
- 断言安装结果：仍走系统卸载注册表 / 约定安装目录，不靠截图。

**P1-B 账号**

- 账号密码：全自动。
- 短信登录、注册验证码、忘记密码：脚本点到「已发送」，停下来等你回填验证码再继续。
- 微软账号：测到系统/WebView 登录窗为止；出现 MFA 则标记人工，不算脚本失败。
- 注册格式：空、过短、非法字符、两次密码不一致，用 UI 或页面提示断言。不依赖真注册成功（真注册会污染账号池，单独一条「成功注册」用一次性号）。

**P1-C 设置**

- 先做 **浅色 / 暗色切换**：切换后主窗口外观变化，重启进程后仍保持。
- 其他设置项等有清单再加。禁止写「设置各功能」这种无边界范围。

### P2 易变内容（抽检）

**会员：** 至少两个身份（非会员 / 会员）。只断言入口可见性、权益页能打开、无权限操作有明确拦截。不把具体价格文案写死。

**活动：** 只断言活动入口存在、能打开、外链/内开跳转发生。活动素材、时间、文案一律配置化或标「内容易变」。活动下线时套件跳过，不报 P0 红。

### 明确不做（除非以后单独立项）

- MuMu / 模拟器、微信小程序宿主
- 控制面板里搜索 QQ
- 本机日常软件（微信、WPS、钉钉等）当卸载/更新对象
- 6 个安装包全量升降级矩阵（发布闸门另说）
- 纯图像识别当主定位手段
- 大厅内「自己检测更新」若与 `Setup.exe` 升级不是同一条路径：记为 P1 候选项，不进当前 P0

---

## 4. 建议的运行结构（不是一条脚本）

```
安装层（P0-01/02）  →  启动层（P0-03/04）  →  冒烟 UI（P0-05/06/07）
                                              ↘ 账号套件（P1-B，按需）
                                              ↘ 应用套件（P1-A，按需）
                                              ↘ 设置套件（P1-C，按需）
                                              ↘ 会员/活动（P2，按需）
```

失败策略：

- 安装失败：后面全部跳过。
- 启动失败：UI 套件跳过，安装层结果仍保留。
- 搜索失败：详情跳过，不影响「版本正确」。
- 验证码未回填：该条记阻塞，不记产品缺陷。

---

## 5. 技术架构

### 5.1 栈

| 层 | 工具 | 用途 |
| --- | --- | --- |
| 运行 | Python 3 + pytest | 用例组织、夹具、报告 |
| 安装与版本 | PowerShell / Python `winreg` | 装包、卸大厅、读注册表和文件版本 |
| 桌面壳 | pywinauto（UIA backend） | 主窗口、导航、原生按钮 |
| WebView2 | 优先 UIA；失败则 Playwright 接调试端口 | 搜索、详情、登录页、活动页 |
| 配置 | `config.yaml`（或同等） | 路径、版本、账号、搜索词、夹具应用 |

安装包路径 **禁止写死**。配置示例：

```yaml
installer_dir: "C:/Users/ASUS/Desktop/华硕大厅"
baseline_setup: "myappstore_1.6.8.17S_Setup.exe"
latest_setup: "myappstore_1.6.10.7S_Setup.exe"
search_keyword: "qq"
accounts:
  password: { user: "", password: "" }
  microsoft: { user: "", password: "" }
fixture_apps:
  install: ""
  update: ""
  uninstall: ""
  sync: ""
```

版本解析规则（实现时写死这条，不要每个脚本自己拆）：

1. 从文件名取出 `1.6.8.17S` 这一段。
2. 去掉末尾一个 `S`（大小写不敏感）。
3. 得到期望版本 `1.6.8.17`。
4. 与 `DisplayVersion`、`FileVersion` 做字符串相等（不要只比前三段）。

### 5.2 WebView2 降级

1. 用 UIA 扫搜索框、列表项、按钮。扫到就用。
2. 扫不到：查进程能否开远程调试（启动参数或环境变量）。能开则 Playwright 连进去点页面。
3. 仍不行：该页标 **技术受阻**，P0 里只保留「窗口在、进程在」。不要改成截图点选撑场面。

专用机落地前，要在那台机器上用 Inspect / Accessibility Insights 看一次：搜索框是原生控件还是网页控件。结果会决定 P0-06 能不能按期自动化。

### 5.3 安装层注意

- 安装包和卸载程序均为 **NSIS 3.08**，静默参数用 `/S`（专用机上尚未用 `--allow-install` 实跑验证）。需要管理员权限。
- 升级（P0-02）优先「已装基线后直接跑新包」。若新包拒绝覆盖或版本未变，再走「卸载 → 装最新」。
- 卸载大厅用注册表里的 `UninstallString`（当前为 `asusUn.exe /S`），不要去点控制面板。

---

## 6. 环境与夹具

| 夹具 | 用途 | 状态 |
| --- | --- | --- |
| 专用测试机 | 真装真卸、可重装系统级软件 | 已定：有。系统版本、是否干净机待确认 |
| 安装包目录 | P0-01/02 | `C:\Users\ASUS\Desktop\华硕大厅` |
| 账号密码测试号 | P0-05、P1-B | 待提供，不要用个人号 |
| 收短信的测试号 | 短信登录、注册、找回 | 待提供；执行时人工回填验证码 |
| 微软测试号 | P1-B | 待提供；MFA 策略待确认 |
| 会员 / 非会员号 | P2 | 待提供 |
| 夹具应用 4 个 | 可装、可更新、可卸、可同步 | **未定，P1-A 的阻塞项** |
| 搜索词 | 默认 `qq` | 可改配置；若目录无 QQ 系应用，换一个稳定存在的词 |

验证码：脚本确认「发送成功」后向执行人要码，填入后再往下走。图形验证码按团队既有规则：脚本识图最多 3 次，失败再交给人。

---

## 7. 相对你最初 8 条的调整（有意为之）

| 你的原项 | 调整 |
| --- | --- |
| 1. 控制面板显示版本 | 改为注册表 + 文件版本；控制面板只作人工抽检。并补上「基线安装 + 升最新」 |
| 2. 模糊搜索 QQ | 保留，但限定为大厅内搜索；关键词和期望命中写配置 |
| 3. 实时检测更新/卸载/同步并执行 | 拆出 P1；检测与执行分开；执行只对夹具应用 |
| 4. 登录注册找回全套 | 冒烟只留账号密码；其余进 P1，接受人工验证码 |
| 5. 设置各功能 | 收缩为先做主题；其余要清单 |
| 6. 下载安装应用 | 归入 P1-A，不进 P0 |
| 7. 会员权限 | 降为 P2，先做身份差异，不做价格文案 |
| 8. 活动跳转展示 | 降为 P2，只测入口和跳转 |

另外补进方案、你原清单没有写清的：首次启动协议、大厅自身卸载、UAC/管理员、WebView2 点不到时的受阻口径。

---

## 8. 落地顺序（设计通过之后）

1. 专用机安装包路径已定为 `C:\Users\ASUS\Desktop\华硕大厅`（与本机 `D:\华硕大厅安装包` 不同，脚本只读配置）。
2. ~~方案确认后建工程目录~~ 已完成：`hall_auto/`、`tests/`、`config.yaml`。P0-01/02 已实现，默认 skip，专用机需 `--allow-install`。
3. 在专用机（管理员）跑通 P0-01/02（装、对版本、升级）。
4. 探壳：UIA 能否点到搜索。能则做 P0-03～07；不能则先交付安装层，搜索标受阻。
5. 有测试号后做密码登录。
6. 有夹具应用后做 P1-A。
7. 短信/微软/注册/主题/会员/活动按夹具齐套再加。

---

## 9. 待你补充（不补也能维持本文结构）

1. ~~专用机安装包目录~~ 已填：`C:\Users\ASUS\Desktop\华硕大厅`。
2. 基线包是否固定 `1.6.8.17S`，还是「每次指定一个当前要验的包」。
3. 账号密码 / 短信号 / 微软号 / 会员号。
4. 四个夹具应用名称。
5. 专用机是否管理员、能否关 UAC 弹窗、能否给 WebView2 加调试端口启动。
6. 设置页除主题外，哪些必须进第一期。

第 2～6 在搭脚本前再定即可。
