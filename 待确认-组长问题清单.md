# 20+ 台多机执行 · 待组长确认清单

---

## 微信可直接发的版本（2026-09-18）

X哥，华硕大厅的自动化我在往 20+ 台测试机上推，有几点想先跟您确认一下，免得方向做偏：

1. **这 20 台是同一批同型号，还是环境有差异？**（系统版本、分辨率、缩放比例）
   如果是批量采购的同配置机器，那只能测稳定性；要测兼容性得有不同环境的机器。我想按实际情况分层分配。

2. **这批机器是啥节奏给我？** 一次全到位还是分批？我打算先在 2~3 台上把环境铺设和执行链路跑通，再复制铺开，比较稳。

3. **这个多机验证是要证明什么、结论给谁看？** 是版本放量前的兼容性验证，还是回归稳定性验证？两者机器分配和用例集不一样。

4. **跑的时候这批机器能保持解锁、屏幕常亮、不用 RDP 断连的方式跑吗？** 这套脚本靠抓屏幕像素和真实鼠标点击，一旦锁屏/息屏/断开远程会话就会批量失败（不是怕有人操作，是怕系统把屏幕关掉）。另外环境铺设（装 Python、依赖、夹具）如果没人帮批量做，我写个一键脚本自己铺，20 台大概半天。

5. **20 台并发打服务端能接受吗？** 同时登录、同时点"检查更新"会真打账号服务和更新服务，需要有个并发上限，或者我先跟服务端那边打个招呼。

6. **测试账号能给几个？** 现在只有一个测试号的话，多机同时登录会互踢会话。

~~另外提前说一个技术问题：**现有脚本在非 100% 缩放的机器上会出现假失败**——按钮识别是按像素面积、提示文案是按红字像素数判的，没做缩放适配。125%/150% 的机器上会大面积误报。我打算先把判定改成比例制再上多分辨率机器。~~

**（2026-09-18 订正：上面这段结论是错的，已作废，别发。）**

实测订正：脚本走 Per-Monitor Aware 物理像素坐标，**非 100% 缩放不影响正确性**——本机真实就是
150%（物理 2560x1600），P0/P1/P2 全部套件在此前提下实测跑通，按钮面积判据就是在物理像素下标定的。
不需要也不应该把机器统一改成 100%。

所以这一条**从"预警"降级为"环境事实记录"**：缩放只作为机型画像写进报告页头，
供多机汇总时区分环境用，不做门禁、不阻断跑批。

真正需要门禁的只有一条：**跑批时会话必须保持解锁 + 屏幕常亮 + 不用 RDP 断连方式跑**。
锁屏 / 息屏 / 断开远程会话会让会话变非交互态，抓屏拿黑屏、点击无落点，全套用例批量失败。
脚本已加预检，非交互态直接中止（`--skip-env-check` 可绕过，仅调试用）。

麻烦您先给我 1、2、4 的答复，我这边就可以安排铺机器了。

---

## 完整清单（内部用）

> 2026-09-18 · 目的：确认"多机跑 UI 自动化"的约束条件，不是要方案评审。
> 技术方案我自己定，下面这些不问清就会做错方向。

## 一、必问（答案决定做不做、怎么做）

1. **机器从哪来、何时到位？**
   20 台是共享机 / 专用机 / 借的办公机？能不能长占（要复跑）？到位前能否先在 1~2 台跑通验证？

2. **"测试兼容"要证明什么？结论给谁看？**
   - 版本放量前的兼容性验证（覆盖多 OS / 多 DPI）→ 必须异构
   - 回归稳定性验证（同配置多跑）→ 必须同构
   - 客户现场适配报告（含驱动 / 机型）
   三者机器分配、用例集、报告形式完全不同，**不能由我假设**。

3. **执行窗口和频率**：跑一轮还是每次发版都跑？能否夜间跑？单轮最长占机多久？

4. **失败怎么定性（最重要）**：
   20 台挂 3 台，算"发现 3 个兼容性缺陷"还是"环境问题需排查"？谁来判是产品 bug 还是脚本未适配？
   要不要跟开发一起复核？

## 二、要争取的授权

5. **跑的时候能否保持解锁 + 屏幕常亮 + 不用 RDP 断连？**
   真问题不是"有人动鼠标"，而是：RDP 断开 / 锁屏 / 息屏 / 睡眠会让会话变非交互态，
   抓屏拿到黑屏或空帧、点击无落点 → **全套用例批量失败**。
   做法：设永不睡眠/不锁屏，用计划任务在本地交互会话里跑。
   **环境铺设**指让裸机具备跑脚本的能力：装 Python 3.10+64 位 → 建 `.venv` → 装 requirements
   （pytest/pywinauto/pywin32/Pillow/PyYAML）→ 铺夹具（大厅安装包、7z2602、CheckAppV/SignCheck_v2）
   → 生成每台自己的 `config.local.yaml` → 装 7-Zip → 建提权计划任务。20 台各做一遍。
   问 IT 能否批量推（域策略/镜像），否则我写一键脚本自己铺（约半天）。

6. **服务端压力容忍度**：20 台同时登录 / 同时点"检查更新"会真打账号服务和更新服务。
   并发上限多少？要不要提前跟服务端打招呼、会不会被限流封 IP？

7. **账号能给几个**：只有 1 个测试号会互踢会话。能按机器数给号吗？微软账号？短信验证码谁收？

8. **夹具应用能否多机反复真装真卸**：现在用网易云音乐 + 7-Zip，走厂商 CDN 下载。
   会不会限流？要不要换成更轻量的夹具？

9. **报告形式给谁看**：现有的 `report.html` 够用，还是要套组长那边的模板？

## 三、要主动预警（不是问，是告知）

> **跑批时必须保持解锁 + 屏幕常亮 + 不用 RDP 断连方式。**
> 锁屏 / 息屏 / 断开远程会话会让会话变非交互态，抓屏拿到黑屏、点击无落点 → 全套用例批量失败。
> 这不是"怕有人操作鼠标"，是系统层面的会话状态问题。
> **脚本已加预检**：非交互态直接中止（rc=3），不会产出垃圾报告。

**~~多 DPI 假失败~~ —— 2026-09-18 实测订正：此结论错误，已作废。**

实测：本机真实缩放 150%（物理 2560×1600），脚本走 Per-Monitor Aware 物理像素坐标，
P0/P1/P2 全部套件在此前提下实测跑通，按钮面积判据（360×60）就是在物理像素下标定的。
**非 100% 缩放不影响正确性，不需要也不应该把机器统一改成 100%。**
缩放只作为机型画像写进报告页头，供多机汇总分组，不做门禁。

## 四、可同步告知（技术侧我自己定）

- 控制通道走**共享盘投递 + 计划任务轮询**，零新增服务（不用自建调度器 / 不用 WinRM）
- 分两期：先"单机一键铺环境"（已落地 `tools/bootstrap_machine.ps1`），跑通试点再放量
- 执行分片按服务端压力分级：只读类全并行、真装卸跨机限流 ≤5、登录类并行（已确认多机登录不踢）、人工类不进农场
- 不用 `pytest-xdist` 跨机，不虚拟化（会引入假缺陷）
- 每台机器环境画像自动采集（`tools/collect_machine.py`），用于多机汇总按维度分组

## 五、我建议的提问顺序

先问 **1、2、5、4**——这四个决定做不做、怎么做、做多少。这四条不清就别铺机器。
其余可以异步确认。
