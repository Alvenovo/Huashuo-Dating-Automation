# 新测试机操作清单

> 照着「步骤」敲，对一下「预期结果」。**对不上就停，别往下走。**
> 为什么这么做、报错了怎么查 → 《测试机操作手册.md》。
>
> - **机器**：**测试机** = 新发下来那台；**控制机** = 投任务、出报告那台（当前 = 包源机 `LAPTOP-VS5F7HF4`）。
> - **终端**：每步都写了「新开」还是「继续用当前窗口」；**权限**写了「普通」还是「管理员」。
>   **别跳着开窗口** —— 管理员窗口的当前目录是 `C:\WINDOWS\system32`，不继承你原来那个窗口。
> - **跑批什么时候开始**：**步骤十六那个 `--loop` 窗口开起来之后**。
>   它没开，控制机投多少任务都只是躺在 `tasks\` 里等人取，**一台机器都不会动**。

---

# 前提条件（先全部满足，缺一条都别开始）

## 一、两台机器（测试机 + 控制机）都要

- [ ] **代理 / VPN / 加速器全部退出，TUN 模式必须关。** 验证：

  ```powershell
  ipconfig /all | findstr /i "198.18 Mihomo Tunnel"
  ```

  **预期结果**：什么都不输出。有输出 = TUN 网卡还挂着，关掉再来。
  （没关干净时共享盘会报「**系统错误 53** 找不到网络路径」，看着像网络坏了，其实是代理。）

- [ ] 两台机器**在同一个局域网**，能互相 ping 通。

- [ ] **445 端口放行**。验证：

  ```powershell
  Test-NetConnection LAPTOP-VS5F7HF4 -Port 445
  ```

  **预期结果**：`TcpTestSucceeded : True`。是 `False` 就找组长放行。

## 二、测试机

- [ ] Windows **10 / 11 64 位**（32 位不行）。
- [ ] **有管理员权限的账号**（步骤七起要用）。
- [ ] **真人登录进桌面，全程不锁屏、不息屏、不断 RDP**。
- [ ] 系统盘（或你要放安装包那个盘）**可用空间 ≥ 3 GB**。

## 三、控制机（= 包源机 `LAPTOP-VS5F7HF4`）

- [ ] **全程开机、不休眠**（所有机器都从它取安装包，没有兜底）。
- [ ] **共享已建好**。验证：

  ```powershell
  net share
  ```

  **预期结果**：能看到 `hall-packages` 和 `hall-farm`。
  没有就让负责人在包源机跑一次 `tools\provision_share.ps1`（**只做一次，全部机器共用**）。

## 四、找组长要齐 4 样

| 东西 | 什么时候用 |
| --- | --- |
| 管理员账号 | 步骤七起 |
| 共享盘账号 + 密码（账号通常叫 `hallshare`） | 步骤二起 |
| 华硕大厅测试账号 + 密码 | 步骤十一 |
| 微软邮箱（可选，只跑 SSO 免密用例才要） | 步骤十一 |

**不需要 GitHub 账号、不需要令牌、外网也不是必需的** —— 代码从共享盘拿，装 Git 也在共享盘上。
唯一要你自己装的是 **Python**（脚本本身是 Python 写的，绕不开）。

---

# 步骤一 · 装 Python 3.12

**机器**：测试机 ｜ **终端**：新开（普通） ｜ **权限**：普通

**做法**

1. 打开 `https://www.python.org/downloads/release/python-31210/`，下 **Windows installer (64-bit)**
   （想省这次下载：先做完步骤二连上共享盘，直接用共享盘那份
   `\\LAPTOP-VS5F7HF4\hall-packages\wheelhouse\python-3.12.10-amd64.exe`，和官网同一个包）
2. 运行安装包，**第一屏最下面勾上 `Add Python to PATH`**，再点 `Install Now`
3. 装完**新开一个 PowerShell 窗口**，敲：

   ```powershell
   python --version
   ```

**预期结果**

```
Python 3.12.x
```

⚠️ **必须是 3.12，不是"3.x 随便"**：共享盘的离线依赖包是给 3.12 打的（`cp312` 装不进 3.13）。
打出 `3.13.x` / `3.14.x` → **卸掉重装 3.12**，别抱侥幸往下走。
⚠️ 弹出微软商店 → 设置 → 应用 → 高级应用设置 → 应用执行别名，关掉 `python.exe` / `python3.exe`，回来重装。
⚠️ 报 `not recognized` → 勾 PATH 那步漏了，回第 2 条重装。

---

# 步骤二 · 连共享盘

**机器**：测试机 ｜ **终端**：继续用当前窗口 ｜ **权限**：普通

**做法**

```powershell
net use \\LAPTOP-VS5F7HF4\hall-packages /user:hallshare "<共享盘密码>" /persistent:yes
dir \\LAPTOP-VS5F7HF4\hall-packages\repo
```

**预期结果**

```
命令成功完成。
```

第二行列出：`hall-auto.git`、`Git-2.55.0.5-64-bit.exe`、`install_git.ps1`

**对不上就停**：

- `系统错误 53` / 找不到网络路径 → **代理没关（最常见）**。回前提条件一。
- `系统错误 1219` → 这台机器上已有一条到包源机的连接。**管理员**窗口跑
  `Restart-Service LanmanWorkstation -Force`，再重跑本步。
- `系统错误 1909` → `hallshare` 被锁了。**等 10 分钟**，别连着试。
- `系统错误 1326` / 拒绝访问 → 密码不对，或用的不是 `hallshare`。找组长要当前密码。
- 其他错误码 → 《测试机操作手册.md》第一部分 1。

---

# 步骤三 · 装 Git

**机器**：测试机 ｜ **终端**：继续用当前窗口 ｜ **权限**：普通

**做法**

```powershell
& "\\LAPTOP-VS5F7HF4\hall-packages\repo\install_git.ps1"
```

**预期结果**（结尾三行）

```
[git-setup] 完成：git version 2.55.0.5.windows.5
[git-setup] 当前窗口还没刷新 PATH，先这样用：& "C:\Program Files\Git\cmd\git.exe" --version
[git-setup] **新开一个 PowerShell 窗口**，git 才直接在 PATH 上。
```

⚠️ 看到结尾那句就**新开一个窗口**再做下一步。不新开的话，下一步会报「git 不是内部或外部命令」。

---

# 步骤四 · 放行共享盘上的裸仓库

**机器**：测试机 ｜ **终端**：新开（普通） ｜ **权限**：普通

**做法**

```powershell
git config --global --add safe.directory '\\LAPTOP-VS5F7HF4\hall-packages\repo\hall-auto.git'
```

**预期结果**

没有输出就是成功。只做一次，以后 `git pull` 也受用。

---

# 步骤五 · 把代码拉到桌面

**机器**：测试机 ｜ **终端**：继续用当前窗口 ｜ **权限**：普通

**做法**

```powershell
cd C:\Users\你的用户名\Desktop
git clone "\\LAPTOP-VS5F7HF4\hall-packages\repo\hall-auto.git" Huashuo-Dating-Automation
cd Huashuo-Dating-Automation
dir
```

**预期结果**

```
Cloning into 'Huashuo-Dating-Automation'...
```

文件数跑到 100%；`dir` 能看到 `AGENTS.md`、`tools`、`tests`、`hall_auto`、`config.yaml`

⚠️ **末尾那个 `Huashuo-Dating-Automation` 不能省。** 不给它，git 按仓库名落地成 `hall-auto`，
后面所有 `cd Huashuo-Dating-Automation` 全部报"路径不存在"。
⚠️ 报 `dubious ownership` → **步骤四没做**，照报错里给的那行跑一遍再重试。
⚠️ 报 `Permission denied`（路径指向**桌面上的文件**）→ **不是本机的问题**，是共享盘权限缺 `hallshare`。
把整段报错发给负责人，让他在共享宿主上跑 `tools\check_share_acl.ps1`，看到「读不到的：0」之后再重试。
⚠️ 其他报错 → 《测试机操作手册.md》步骤 1 的对照表。

---

# 步骤六 · 兜底：从 GitHub 下代码

> **只在步骤二过不去、而这台机器能上外网时才做这一步。** 做完直接跳到步骤七。

**机器**：测试机 ｜ **终端**：新开（普通） ｜ **权限**：普通

## 六 A · 浏览器下载（不用令牌，最省事）

**做法**

1. 在这台测试机的浏览器里打开 `https://github.com/Alvenovo/Huashuo-Dating-Automation`
2. 没登录就先用**有仓库权限的账号**登录（看不到仓库 = 账号没被拉进来，回前提条件四）
3. 点右上方的绿色 `Code` → 弹出框最下面点 `Download ZIP`
4. 右键那个 zip → 全部解压缩
5. **把解压出来的文件夹改名，去掉后面的 `-main`** → `Huashuo-Dating-Automation`
6. 把它放到 `C:\Users\你的用户名\Desktop\`

**预期结果**

```
C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation\
```

## 六 B · 命令行下载（要一个令牌，只在不能开浏览器时用）

**做法**

1. 用**有仓库权限的账号**登录 GitHub，打开
   `https://github.com/settings/personal-access-tokens/new?name=hall-auto-download&contents=read`
2. **Repository access** 选 `Only select repositories` → 勾上 `Huashuo-Dating-Automation`
3. **Permissions** → **Repository permissions** → **Contents** 选 **Read-only**
   —— **只要这一个权限就够了**，其他全部留 `No access`
4. 拉到底点 `Generate token`；页面顶部出现一串 `github_pat_` 开头的字符串
5. 敲：

   ```powershell
   $ProgressPreference = "SilentlyContinue"
   [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
   cd C:\Users\你的用户名\Desktop

   $tok = Read-Host "粘贴令牌"          # 粘贴后回车，屏幕上不会显示

   Invoke-WebRequest -Headers @{ Authorization = "Bearer $tok" } `
     -Uri "https://api.github.com/repos/Alvenovo/Huashuo-Dating-Automation/zipball/main" `
     -OutFile repo.zip -MaximumRedirection 5

   Expand-Archive repo.zip -DestinationPath _tmp_repo
   $inner = Get-ChildItem _tmp_repo | Select-Object -First 1
   Move-Item $inner.FullName Huashuo-Dating-Automation
   Remove-Item _tmp_repo -Recurse -Force
   Remove-Item repo.zip -Force

   # 清理：别把令牌留在窗口变量和命令历史里
   Remove-Variable tok
   Clear-History
   Remove-Item (Get-PSReadlineOption).HistorySavePath -ErrorAction SilentlyContinue
   ```

**预期结果**

```
C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation\
```

**对不上就停**：

- `401 Unauthorized` → 令牌复制不完整，或粘贴时带了空格/换行。重新生成一个。
- `404 Not Found` → **权限不够时 GitHub 会假装"仓库不存在"**。回第 2 条确认勾上了本仓库。
- `403 Forbidden` → 令牌过期，或 **Contents 不是 Read-only**。回第 3 条。
- 卡住不动 / 重定向出错 → 改用这行（`curl.exe` 是系统自带的）：

  ```powershell
  curl.exe -L -H "Authorization: Bearer $tok" -o repo.zip "https://api.github.com/repos/Alvenovo/Huashuo-Dating-Automation/zipball/main"
  ```

⚠️ **令牌是密码级的凭据**：不能写进任何文件、不能贴进聊天或群里。上面最后三行就是清理命令，别省。

## 两条路都要做的检查

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
dir
```

**预期结果**：`AGENTS.md`、`tools`、`tests`、`hall_auto`、`config.yaml`

---

# 步骤七 · 开管理员窗口，设 3 个变量

**机器**：测试机 ｜ **终端**：新开（管理员） ｜ **权限**：管理员

> 怎么开：开始菜单 → 右键 → **终端(管理员)** / **Windows PowerShell(管理员)**

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
$env:HALL_SHARE_USER = "hallshare"
$env:HALL_SHARE_PASSWORD = "<共享盘密码>"
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"
```

**预期结果**

```powershell
pwd                          # → C:\Users\...\Desktop\Huashuo-Dating-Automation
echo $env:HALL_FARM_ROOT     # → \\LAPTOP-VS5F7HF4\hall-farm
```

⚠️ **`cd` 那行不能省。** 新开的管理员窗口当前目录是 `C:\WINDOWS\system32`（Windows 的规定，
**不继承**你刚才那个窗口），而后面几步用的是相对路径。
不 `cd` 的话，下一步报的是 `can't open file 'C:\WINDOWS\system32\tools\probe_av_quarantine.py'` ——
读起来像「探针被杀了」，其实只是当前目录不对。
⚠️ **变量必须在"这个管理员窗口里"设**。在普通窗口设完再"以管理员身份运行"，三个变量一个都带不过去。
⚠️ 第二行看不到 → 你是在普通窗口设的，关掉重开管理员窗口，回到本步开头。

---

# 步骤八 · 杀软探针

**机器**：测试机 ｜ **终端**：继续用当前窗口（管理员） ｜ **权限**：管理员

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
python -X utf8 tools\probe_av_quarantine.py
```

⚠️ 用**系统 `python`**，不是 `.venv\Scripts\python.exe` —— `.venv` 要到步骤十才建出来。探针只用标准库。

**预期结果**

全是 `0(OK)@0.0s`

- **本机不中** → 这一步到此为止，**跳过步骤九**，直接进步骤十。
- 出现 `225(ERROR_VIRUS_INFECTED)@0.0s` → **先更新病毒库再复探**
  （设置 → Windows 安全中心 → 病毒和威胁防护 → 检查更新）。更新后探针干净就照样往下走；
  **还是 225 才做步骤九**。

---

# 步骤九 · 关实时保护

> **只在步骤八复探后仍报 225 时才做这一步。** 探针干净就别动。

**机器**：测试机 ｜ **终端**：继续用当前窗口（管理员） ｜ **权限**：管理员

**做法**

方式 A（推荐，最省事）：设置 → 隐私和安全性 → Windows 安全中心 → 病毒和威胁防护
→「病毒和威胁防护设置」下的「管理设置」→ 关掉「实时保护」

方式 B（被「篡改保护」挡住就用方式 A）：

```powershell
Set-MpPreference -DisableRealtimeMonitoring $true
```

**预期结果**（验证）

```powershell
(Get-MpComputerStatus).RealTimeProtectionEnabled
```

```
False
```

看到 `True` 就是没关掉，回去用方式 A。

⚠️ **重启后会被自动打开** —— 跑批中途重启过机器，回来重做这一步。
⚠️ **跑完记得开回来**（步骤十四最后一条）。
⚠️ 不想动整机安全设置？仓库里有更小代价的做法：`tools\add_av_exclusions.ps1 -DryRun` 先看它要改什么，
它只把**仓库目录、安装包缓存、python.exe** 排除掉。**这是安全策略变更，先问负责人再执行。**

---

# 步骤十 · 跑铺环境脚本

**机器**：测试机 ｜ **终端**：继续用当前窗口（管理员） ｜ **权限**：管理员

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1
```

**耗时 5-15 分钟，中途别关窗口。**

**预期结果**（结尾长这样）

```
[OK] 环境自检
[OK] 虚拟环境与依赖
...
[OK] 提权计划任务
[OK] 自检 tests/unit
[OK] 屏幕常亮（永不睡眠/不关屏）
结果：全部通过，本机已可跑批
```

无外网的机器，第 2 步应该看到「**离线 wheelhouse**」字样（关键是"离线"两个字）。

⚠️ **它会打印两遍「机器铺设」，这是正常的。** 第一遍只建虚拟环境 + 装依赖，
然后自动换成虚拟环境的解释器把整套重跑一遍；第二遍开头有一句「依赖已就绪。换用 venv 的解释器重跑整套」。
⚠️ 节点名要用 `R01` 这种编号：加 `-NodeId R01`。**这个名字必须和控制机 `dispatch --nodes` 写的一模一样。**
⚠️ 系统盘紧张：加 `-InstallerDir "D:\Test\大厅"`。
⚠️ 第 13 步 `[FAIL] 屏幕常亮` → 用**管理员**窗口重跑一次 bootstrap（前面 12 步会全跳过，十几秒）。
**别跳过**：不做这步，机器无人值守时会自己睡过去，控制机看到的是「投了任务没反应」。
⚠️ 第 12 步 `[FAIL] 自检 tests/unit` → 看日志 `reports\bootstrap\selftest_*.log`；
若是"共享盘拷贝"那几条报 `[Errno 22] Invalid argument`，是**杀软把刚写的临时文件判毒**，回步骤八。
⚠️ 报告路径：`reports\bootstrap\bootstrap_<机器名>_<时间>.json`

---

# 步骤十一 · 填凭据文件

**机器**：测试机 ｜ **终端**：不用终端（用记事本/编辑器改文件） ｜ **权限**：—

**做法**

打开仓库根目录的 `farm_node.env`，把等号后面填上真实值：

```ini
HALL_TEST_USER=大厅测试账号
HALL_TEST_PASSWORD=大厅测试密码
HALL_MS_USER=微软邮箱

HALL_SHARE_USER=hallshare
HALL_SHARE_PASSWORD=共享盘密码
```

**预期结果**

值不加引号；用不上的留空（对应套件会自动跳过，不影响其他套件）。

⚠️ **不需要重跑 bootstrap** —— 脚本每次取任务时会重新读这个文件。
⚠️ 不填的话登录套件会**静默跳过**：不报错，但一条都没真跑，报告上一片黄，你会以为"跑过了没问题"。

---

# 步骤十二 · 验证凭据读到了

**机器**：测试机 ｜ **终端**：新开（普通） ｜ **权限**：普通

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
.\.venv\Scripts\python.exe -X utf8 -c "from pathlib import Path; from hall_auto.env_pack import build_suite_env, credential_status; env,_=build_suite_env(node_env_path=Path('farm_node.env')); print(credential_status(env))"
```

**预期结果**

```
password_login: True
```

⚠️ `node_env_path=Path('farm_node.env')` 这段**不能省**。不传它，函数根本不会去读这个文件，
只认当前窗口里的环境变量 —— 你明明填对了，也会打印一片 `False`。

---

# 步骤十三 · 验证共享盘农场

**机器**：测试机 ｜ **终端**：继续用当前窗口 ｜ **权限**：普通

**做法**

```powershell
net use \\LAPTOP-VS5F7HF4\hall-farm /user:hallshare "<共享盘密码>" /persistent:yes
dir \\LAPTOP-VS5F7HF4\hall-farm
```

**预期结果**

```
tasks    done    results    logs
```

⚠️ 只看到部分文件夹 → 让负责人在包源机重跑一次 `tools\provision_share.ps1`。
⚠️ 报「拒绝访问」→ `hall-farm` 需要 **Change（可写）**权限，只给读会导致"看起来很成功但任务永远取不走"。

---

# 步骤十四 · 收工验收

**机器**：测试机 ｜ **终端**：— ｜ **权限**：—

逐条核对，全打勾才算这台机器铺好了：

- [ ] `python --version` 打出 **`Python 3.12.x`**
- [ ] `dir \\LAPTOP-VS5F7HF4\hall-packages\repo` 能列出文件
- [ ] 仓库目录里有 `AGENTS.md`、`tools`、`tests`、`hall_auto`
- [ ] bootstrap 结尾是「结果：全部通过，本机已可跑批」
- [ ] `farm_node.env` 已填，步骤十二打出了 `True`
- [ ] `dir \\LAPTOP-VS5F7HF4\hall-farm` 看到四个文件夹
- [ ] **桌面没锁屏、没息屏、没断 RDP**（跑批全程保持）
- [ ] bootstrap 第 13 步是 `[OK] 屏幕常亮（永不睡眠/不关屏）`
- [ ] 实时保护的状态心里有数：**探针干净、没关** = 合格，什么都不用做；
      **探针报 225、关掉了** = 合格，但跑批期间要一直保持关着，
      整台机器不再跑批了再开回来（设置 → Windows 安全中心 → 病毒和威胁防护 → 管理设置 → 打开「实时保护」）

---

# 步骤十五 · 控制机：确认农场目录已落盘

**机器**：控制机 ｜ **终端**：新开（普通） ｜ **权限**：普通

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py status
```

**预期结果**

```
待执行 0 个，执行中 0 个，已完成 0 个
```

⚠️ 报「未设 HALL_FARM_ROOT，config.local.yaml 里也没有 farm_root」→ 在 `config.local.yaml` 末尾补一行：

```yaml
farm_root: C:\hall-farm
```

**控制机自己就是共享宿主，写本地路径最省事** —— 少一层 SMB 认证，不会撞「拒绝访问」/「找不到网络路径」。
节点那边照旧用 UNC，两边指向同一个目录，互不影响。

---

# 步骤十六 · 测试机：启动 agent（**跑批的总开关**）

**机器**：测试机 ｜ **终端**：新开（普通） ｜ **权限**：普通

⚠️ **这一步决定"机器会不会自己跑测试"。**
敲下去之后，**这个窗口一直到整批跑完都不能关**。关掉它 = 控制机后面投的任务全躺在
`tasks\` 里没人取，控制机那边只看到「待执行 N 个」永远不动，而节点这边**一点报错都没有**。

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --loop
```

**预期结果**（每 30 秒多一行）

```
节点 DESKTOP-DOHED68 农场目录 \\LAPTOP-VS5F7HF4\hall-farm
等待任务文件 \\LAPTOP-VS5F7HF4\hall-farm\tasks\DESKTOP-DOHED68.json
[10:32:07] 轮询 #1：无任务，30 秒后再查（等 \\LAPTOP-VS5F7HF4\hall-farm\tasks\DESKTOP-DOHED68.json）
```

⚠️ **打出上面这行心跳 = 这一步做完了。窗口放着别动，去控制机做步骤十七。**
⚠️ **一行心跳都没有 = 真出问题**，不是"卡住"。先验共享盘：`dir \\LAPTOP-VS5F7HF4\hall-farm\tasks`。
⚠️ 心跳里那个名字，要和控制机 `dispatch --nodes` 写的**完全一致** —— 对不上就永远取不到任务。
⚠️ 心跳换成 `⚠ 节点进程活着，但一个任务都取不到` → **不是"没任务"，是真取不到**，按它下面那三条查。

**投完任务回来看**（步骤十七做完后），这个窗口会接着打：

```
取到任务 task_20260923_103241
  跑 launch（分片 1/1）
  跑 apps-detect（分片 1/1）
  证据已回传 \\LAPTOP-VS5F7HF4\hall-farm\results\DESKTOP-DOHED68\DESKTOP-DOHED68_2026-09-23_1040
任务完成，结果 \\LAPTOP-VS5F7HF4\hall-farm\done\DESKTOP-DOHED68_task_20260923_103241.json
```

⚠️ **跑批期间这个窗口一直开着，别关。**
⚠️ **别用 `--once` 跑批。** 它的语义是"去看一眼，有任务就跑、**没任务就退出**" ——
清单这步要的是**常驻**。`--once` 只在"只想确认链路通不通、不打算跑批"时用，见《测试机操作手册.md》。

---

# 步骤十七 · 控制机：先冒烟，再跑全量档

**机器**：控制机 ｜ **终端**：继续用当前窗口 ｜ **权限**：普通

**① 冒烟** —— 一个套件、几十秒，只验「节点真的会自己取任务跑起来」

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
.\.venv\Scripts\python.exe -X utf8 tools\run_farm.py --nodes DESKTOP-DOHED68 --suites launch
```

**预期结果**（`task_…` 和时刻是跑那一刻的值，只有这两处对不上不算错）

```
=== 跑批 task_20260923_103241 ===
节点：DESKTOP-DOHED68
套件：launch

[1/3] 投任务
DESKTOP-DOHED68: 1 个套件 -> DESKTOP-DOHED68.json
任务 task_20260923_103241 已投给 1 台节点（共 1 台）。
并发预算：
  launch           全并行（不碰服务端）

[2/3] 等结果（每 30 秒查一次，Ctrl+C 可中断）
[10:32:41] 待执行 1 / 执行中 0 / 已完成 0
  DESKTOP-DOHED68      还没被取走
[10:33:11] 待执行 0 / 执行中 1 / 已完成 0
  DESKTOP-DOHED68      已取走，正在跑
[10:34:02] 待执行 0 / 执行中 0 / 已完成 1
  DESKTOP-DOHED68      已交回执

全部节点已交回执。

[3/3] 出报告
汇总报告：\\LAPTOP-VS5F7HF4\hall-farm\aggregate_report.html
覆盖节点 1 台，用例 N 条
```

⚠️ **必须看到「已交回执」才往下走。** 没看到就别投全量档 —— 链路没通，投了只会白等一小时。
⚠️ `用例 N 条` 是汇总矩阵里的**累计**去重用例数，会随跑的轮次增加，不是这一轮的数字。

**② 全量档** —— 冒烟过了再投；7 个套件，几十分钟

```powershell
.\.venv\Scripts\python.exe -X utf8 tools\run_farm.py --nodes DESKTOP-DOHED68 --suites all --timeout 14400
```

**预期结果**：和上面同形，只是开头多一行全量档展开，耗时长得多：

```
--suites all → 全量档 7 个：unit, launch, apps-detect, login, settings, security, wb
```

⚠️ **`--timeout` 一定要放大**：默认 1 小时不够跑全量档，上面给的是 4 小时。
⚠️ **它会自己盯「投了没人取」**：任务投出去 90 秒还没被取走，它直接把排查清单打出来
（节点 agent 没跑 / 连不到农场目录 / 名字对不上）—— **不用你自己盯 `status` 猜**。
⚠️ **中途 Ctrl+C 不会丢任务**：节点会继续跑完并写回执。想随时看进度，另开一个窗口敲
`.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py status`。
⚠️ 只想投、不想在这儿等：加 `--no-wait`（等价于原来的 `farm_control.py dispatch`）。
⚠️ `--nodes` 写**节点的名字**（默认是节点主机名，也可能是 `-NodeId` 指定的 `R01`），
**必须和步骤十六心跳那行完全一致**。
⚠️ **全量档不含大厅装卸 / 夹具装卸 / 大厅里点更新** —— 那三样要管理员权限，农场节点是普通权限、
投了必挂。它们单独在**步骤十八**跑，**跑完步骤十八才算跑全**。
⚠️ `login-manual` 投不出去是**设计如此**（要真人守着收码），要跑就在那台机器的终端里手工跑。
⚠️ 套件清单、并发预算、每个套件的前置条件 → 《测试机操作手册.md》3.1。

---

# 步骤十八 · 测试机：补跑装卸 + 更新（**唯一要管理员的一段**）

**机器**：测试机 ｜ **终端**：新开（**管理员**） ｜ **权限**：**管理员**

全量档跑完还差三块。它们**必须提权**，而农场节点是普通权限、投了必挂，所以单独在这儿跑：

- **大厅自己**的卸载 + 重装 → `run_p0_install.ps1`
- **夹具**真装真卸（7-Zip 那类）→ `run_p1_apps.ps1`
- **在大厅里点「更新」**把夹具升到目标版本 → `run_p1_apps.ps1`（同一条命令）

**做法**

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
powershell -NoProfile -ExecutionPolicy Bypass -File run_p1_apps.ps1
```

**预期结果**

```
wrapper start admin=True time=2026-09-23T11:40:00.0000000+08:00
Done. Log: C:\Users\admin\Desktop\Huashuo-Dating-Automation\reports\p1_apps_run.log
```

大厅自身的装卸另跑一次（**它会把大厅卸了重装**，跑之前确认这台机器上没有别的东西在跑）：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File run_p0_install.ps1
```

⚠️ **必须是管理员窗口。** 非管理员跑 `run_p1_apps.ps1` 会打 `NOT ADMIN: ...` 然后退出码 2 ——
它不会静默降级，但你也别把它当成"跑过了"。
⚠️ `run_p1_apps.ps1` 会自己先跑 `tools\reset_fixture.py` 把夹具压回起点 ——
上一次装剩的会让用例**直接 skip**（不报错），这一步不能省。
⚠️ **这两条只能投给测试机**，绝不能在办公机上跑。
⚠️ **更新用例还有额外前置**：`config.local.yaml` 里要配 `update_fixture` 块，没配会 skip 而不是失败。
⚠️ **这一段的结果不进农场汇总报告**，在 `reports\p1_apps_run.log` 里。

**到这儿才算跑全。** 最终交付物 = 步骤十七那份农场矩阵报告 + 这一步的 `p1_apps_run.log`。

---

# 跑批期间不许做的事

- **别锁屏、别合盖、别关显示器、别断 RDP**（锁屏后截图全黑、点击没落点，整批用例假失败）。
- **别在测试机上手动操作**（脚本在点鼠标键盘，你一动就互相干扰）。
- **别关测试机的 agent 窗口**（关了就不取任务了）。
- **绝对不要在大厅界面上点「全部安装」** —— 列表里全是 QQ / 微信 / WPS 这类日常软件，点下去就真装一机器。
- **绝对不要把 `install` / `apps-lifecycle` 投给办公机**，也别手工设 `HALL_ALLOW_INSTALL=1` 去"试试看"。
