> 最后更新：2026-09-21
> 状态：**已落地**（2026-09-21 搭好并验证 clone；`install_git.ps1` 尚未在真无 Git 机器上跑过，见「未验项」）
> 来源：本机实测（共享盘实建、clone 实跑、SHA-256 实校）
> 维护人：wwd（测试开发）

# 多机代码分发（共享盘裸仓库）

## 一句话

新测试机从**共享盘的裸仓库** `git clone` 拿代码 ——
**不需要外网、不需要 GitHub 令牌、不需要手工排除任何文件**。

## 共享盘现状（已搭好）

```
C:\hall-packages\repo\
  hall-auto.git                    裸仓库（HEAD -> refs/heads/main）
  Git-2.55.0.5-64-bit.exe          65343712 字节，钉住版本
  install_git.ps1                  6411 字节，UTF-8 with BOM
```

对外的 UNC 路径：`\\LAPTOP-VS5F7HF4\hall-packages\repo\`

- **钉住的 Git 版本**：`2.55.0.5`（官方 release 资产 `Git-2.55.0.5-64-bit.exe`）
- **SHA-256**：`D065A4E23C3D9A6B5073D609B5BE0830227EC3CA053C083BA385061DDFAF94C6`
  （取自官方 release 说明 `github.com/git-for-windows/git/releases/tag/v2.55.0.windows.5`，
  本机下载后已实测校验通过）
- 钉版本的理由同 7-Zip 夹具：**无外网机器只能靠共享盘这一份，必须可复现、可校验**。

## 为什么放在 `hall-packages` 里

- `hall-packages` 对节点是 **READ-only** —— 节点只需要 `clone` / `pull`（读），权限刚好对得上。
  节点**不该** push 代码，只读共享正好把这件事从"靠自觉"变成"靠权限"。
- **不用再提权**。建新共享要重跑 `tools\provision_share.ps1`（唯一的提权动作，交接里写明"已用完"）。
- 本机是共享宿主，push 走**本地路径** `C:/hall-packages/repo/hall-auto.git`，不受 SMB 只读限制。

## 新测试机操作（替换手册步骤 1）

**前提：先认证共享盘。** 共享的 NTFS 只授权 `hallshare`，**新机器默认连不上** ——
不先 `net use`，下面 ① ② 都会报「找不到网络路径」/「拒绝访问」，而报错看着像网络坏了。

```powershell
# ⓪ 先连共享盘（普通窗口即可，/persistent:yes 重启后自动重连）
net use \\LAPTOP-VS5F7HF4\hall-packages /user:hallshare "<共享盘密码>" /persistent:yes
dir \\LAPTOP-VS5F7HF4\hall-packages\repo        # 能看到 hall-auto.git / install_git.ps1 才算过

# ① 装 Git（一行，零依赖，无外网可用）
& "\\LAPTOP-VS5F7HF4\hall-packages\repo\install_git.ps1"

# ② 放行共享盘上的裸仓库（Git 把 UNC 当"非本地目录"，不放行 clone 直接失败）
git config --global --add safe.directory '\\LAPTOP-VS5F7HF4\hall-packages\repo\hall-auto.git'

# ③ 拉代码（走局域网；**必须显式给目标目录名**，否则落地成 hall-auto）
cd C:\Users\你的用户名\Desktop
git clone "\\LAPTOP-VS5F7HF4\hall-packages\repo\hall-auto.git" Huashuo-Dating-Automation
cd Huashuo-Dating-Automation

# ④ 之后照旧跑 bootstrap（管理员窗口）
```

> ⚠️ **装完 Git 要新开一个 PowerShell 窗口** —— 安装程序改了系统 PATH，但**当前窗口不会自动刷新**。
> `install_git.ps1` 最后会提醒这一句。
>
> UNC 路径如果 Git 不认，先映射盘符再 clone（**别忘了末尾的目标目录名**）：
> `net use Z: "\\LAPTOP-VS5F7HF4\hall-packages"` → `git clone Z:\repo\hall-auto.git Huashuo-Dating-Automation`。
> 节点侧本来就在用 `//LAPTOP-VS5F7HF4/hall-packages`（见 `hall_auto/fetch.py`），网络通路是现成的。

## `install_git.ps1` 为什么存在（引导悖论）

bootstrap 要先有代码才能跑，而按本方案拿代码要先有 Git —— 死锁。
所以装 Git **不能只做在 bootstrap 里**，必须有一个**不依赖仓库、不依赖 Python、不依赖外网**的入口。
它就是那个入口：只用系统自带 PowerShell（5.1 兼容），从同目录取安装包静默装。

行为：

| 情况 | 结果 |
| --- | --- |
| `Get-Command git` 能找到 | 打印版本，跳过（rc=0）。要重装加 `-Force` |
| `git` 不在 PATH，但 `C:\Program Files\Git\cmd\git.exe` 在 | 提示"新开窗口再试"，跳过（rc=0）—— 避免白装一遍 |
| 找不到安装包 | rc=2，提示确认共享盘连通性 |
| SHA-256 对不上 | rc=3，**拒绝执行** |
| 安装程序起不来 / 非 0 退出 | rc=4 |
| 退出码 0 但 `git.exe` 不在预期位置 | rc=5 |

实现上两个刻意选择：

1. **先拷到 `%TEMP%` 再装**。`Start-Process` 的 UNC 路径有坑（工作目录不能是 UNC），
   而且从网络位置跑安装程序还会撞 SmartScreen / 执行策略。拷一次，麻烦全没。
2. **不写 `/COMPONENTS`**。默认组件已含「Git from the command line and also from 3rd-party
   software」（即写进 PATH），正是我们要的。少写一个开关就少一个踩坑点。

> **源文件在仓库 `tools/install_git.ps1`，共享盘那份是拷贝。**
> 改完必须重新拷过去，否则一线拿到的是旧版：
> `Copy-Item tools\install_git.ps1 C:\hall-packages\repo\ -Force`
> 拷完确认 BOM 还在（PS 5.1 会把无 BOM 的 UTF-8 当 ANSI 读，中文报错变乱码）。

## 更新流程

```bash
# 本机（唯一 push 方）
git push share main

# 各测试机
git pull
```

## 口径（必须定死，否则两边分叉）

- **只有本机能 push。** 本机同时推 `origin`（GitHub）和 `share`（共享盘）。
- **测试机只读不 push。** 由 `hall-packages` 的 READ-only 权限兜底。
- **共享盘裸仓库不是备份。** GitHub 仍是异地副本。但本机 `git fetch origin` 拿不到
  `origin/main` 远端跟踪引用（环境层，见《会话交接.md》）—— 所以**别用 `git status` 判断同步状态，
  权威口径是 `git ls-remote origin`**。

## 取代关系

| 原路线 | 处置 |
| --- | --- |
| A（GitHub clone） | **改**：clone 地址换成共享盘裸仓库。不再要外网、不再要令牌、不再弹 GitHub 登录 |
| B（下 zip） | **保留**，降级为兜底：共享盘也连不上时才用 |
| C（整包拷贝） | **删掉**。它存在的唯一理由是"无外网"，而共享盘 clone 同样无外网可用；它那三个手工排除的坑（`config.local.yaml` / `farm_node.env` / `reports/`）随之消失（`.gitignore` 由机器执行） |

## 第一台真机实测（2026-09-21 15:10，用户当场跑出来）

照《新机操作清单》步骤 2A 走，**卡在 `git clone`**，暴露两个文档缺陷（都已修进上面那段）：

| 现象 | 根因 | 处置 |
| --- | --- | --- |
| `fatal: detected dubious ownership in repository at '\\...\hall-auto.git'`<br>`'...' may refer to a non-local directory` | **Git 把 UNC 路径当"非本地目录"**。**与文件属主无关** —— 共享宿主本机走 UNC 也照样复现（已实测）。所以别按"远端 owner SID 不匹配"去查 | 加 `git config --global --add safe.directory '<UNC 裸仓库>'`（文档补 ② / A-2） |
| `cd Huashuo-Dating-Automation` 报"路径不存在" | 裸仓库叫 `hall-auto.git`，`git clone` 按**仓库名**落地成 `hall-auto` | clone **显式给目标目录名**（文档补 ③ / A-3） |

**同轮实测对照（本机，git 2.55.0.windows.3）**

| 做法 | 结果 |
| --- | --- |
| `git clone '\\LAPTOP-VS5F7HF4\...\hall-auto.git'` | ❌ dubious ownership |
| 先 `safe.directory` 再 clone | ✅ 通。`safe.directory` 的路径**斜杠方向不敏感**（正/反斜杠、配置与 URL 混用都匹配） |
| `git clone 'file:////LAPTOP-VS5F7HF4/hall-packages/repo/hall-auto.git'` | ✅ 通，**零配置**；origin 记成 `file://`，`git pull` 也通 —— 备用方案 |
| `net use Z:` 后 `git clone Z:\repo\hall-auto.git` | ✅ 通（盘符路径不触发 UNC 判定）；但 origin 会记成 `Z:\...`，**跨会话/计划任务失效**，只当兜底 |
| `git pull`（origin 是 UNC，无 `safe.directory`） | ✅ 通 —— **只有 `clone` 撞 ownership 检查**，fetch 不撞 |
| `git clone <url> Huashuo-Dating-Automation` | ✅ 目录名正确 |

⚠️ **顺带查出：共享盘裸仓库落后。** 实测 clone 下来的是 `4b0e0f6`，而本地/`origin` 已经在 `3878dbf`
（差 2 个 doc commit：`9f15336`、`3878dbf`）。**发新机之前先 `git push share main`**，
否则新机拿到的是**旧文档 + 旧代码**，而这类落后不会报错。

## 已验 / 未验

**已验（2026-09-21，本机）**

- 裸仓库建好并推送：`git ls-remote C:/hall-packages/repo/hall-auto.git` → `refs/heads/main = 4b0e0f6`，
  HEAD → `refs/heads/main`（不指 `master`，clone 不会报 "remote HEAD refers to nonexistent ref"）。
- **从共享盘 clone 实跑通过**：135 个文件落地，`AGENTS.md` / `tools` / `hall_auto` / `tests` /
  `pytest.ini` / `config.yaml` / `machines.yaml` 全在；
  `config.local.yaml` / `farm_node.env` / `.venv` / `reports` / `wheelhouse` **全部不在**
  —— 证明 `.gitignore` 的排除是自动的，不需要人工剔。
- `install_git.ps1` 语法解析 0 错误、BOM 实测 `EF BB BF`。
- Git 安装包 SHA-256 与官方一致。
- **`install_git.ps1` 三条分支沙箱实测通过**（做法：`scriptblock` 内联执行 + 把 `exit N` 换成 `throw`，
  控制流与打印不变；`$PSScriptRoot` / `$DefaultGitExe` 钉到沙箱。**不真装 Git**）：

  | 分支 | 期望 | 实测 |
  | --- | --- | --- |
  | 沙箱里没有安装包 | rc=2 | `EXITCODE=2` ✅ 走的是"找不到安装包"那条 |
  | 安装包内容是垃圾 | rc=3 | `EXITCODE=3` ✅ **校验失败即拒绝执行**，没往下走 |
  | 装了 Git 但当前窗口 PATH 里没有 | rc=0 | `EXITCODE=0` ✅ 走的是"新开窗口再试"那条 |

- **交付前验收第 6 条（换裸解释器）通过**：系统 Python 3.12.10 **没有 PyYAML**，
  但 `bootstrap_machine.py --help` 正常输出（rc=0）—— 自举链没断。
- **`tests/unit` 跑过**：376 passed / 4 failed。4 条失败**全部**是《会话交接.md》记录的
  已知 EINVAL flake（`test_fetch_policy.py` 3 条 + `test_bootstrap_policy.py::test_seven_zip_fixture_comes_from_share_when_offline`，
  报 `[Errno 22] Invalid argument`）。**drift 用例全绿** —— 本轮手册/清单的改动与代码一致。
- **`install_git.ps1` 的 rc=2 报错串改完重验**（2026-09-21，补「先 net use 认证」指路 +
  兜住 `Test-Path` 在未认证时抛的异常）：语法解析 **0 错误**；rc=2 / rc=3 两条分支
  用 `scriptblock` 沙箱（`$PSScriptRoot` 钉到沙箱、`exit N` → `throw`）重跑通过；
  共享盘副本 md5 与仓库一致、BOM `EF BB BF` 在。
- **新增守卫用例**：`test_sync_handbook.py::test_both_docs_authenticate_the_share_before_cloning` ——
  锁「两份对外文档都必须先 `net use` 认证共享盘，且排在 `git clone` 之前」。
  **负路径探针实测能红**（缺命令 / 顺序反，两种都报得出原因），正确顺序能绿。

**沙箱验收查出的真缺陷（已修）**

- `$DefaultGitExe` 原来用 `${env:ProgramFiles}` 拼路径。**该环境变量在某些受限会话里是空的**，
  于是 `Join-Path` 抛「无法将参数绑定到参数"Path"，因为该参数是空值」——
  完全看不出根因，而且发生在**做任何事之前**（脚本第一行就死）。
  **修**：改用 `[Environment]::GetFolderPath([Environment+SpecialFolder]::ProgramFiles)`。
  这个缺陷只会在"环境变量不全"的会话里出现 —— **正是本机跑不出来的那一类**。

**未验（下次别重查，也别当已验）**

- ⚠️ **「没认证共享盘时到底报什么」没在真 SMB 客户端上验过**（2026-09-21 补）——
  本机是共享宿主，客户端侧的失败码（53 / 67 / 1326 / 拒绝访问 / 1219）是按 Windows 语义写的，
  没在真新机上复现过。第一台真机上线时核对 A-0 那张表，实际报错补记回来。
- ⚠️ **`install_git.ps1` 的安装程序本身从未被执行过** —— 上面验的是三条**分支**，
  真跑一遍安装会在这台机器上**装一份 Git for Windows**（本机 git 是 `/mingw64/bin/git`
  工具链自带可移植版，`C:\Program Files\Git` 不存在），所以没跑。
  → 第一台真测试机上线时**盯着这一步**，跑完把结果补记回本节。
- ⚠️ **`Get-Command git` 命中的那条分支未单独触发** —— 本机 PowerShell 会话里改了 PATH 之后
  `Get-Command git` 仍查不到（会话限制：同一路径 `Test-Path` 是 True）。
  逻辑只有 3 行、且相邻的"PATH 没刷新"分支已实测，风险低 —— **但没验就是没验**。
- ✅ **UNC 路径 clone 已在真 SMB 客户端上验过（2026-09-21 15:18，测试机报回）** ——
  测试机照**旧版**文档走，**原样复现**上面那两个错误（dubious ownership + `cd` 路径不存在），
  结论与共享宿主本机一致：**UNC 裸仓库必须显式 `safe.directory`，clone 必须给目标目录名**。
  按本页「新测试机操作」②③ 走即可，不需要映射盘符。
  ⚠️ **但**：测试机拿到的文档是共享盘裸仓库里的**旧版**（`4b0e0f6`）——本页与两份对外文档的改动
  **当时还没 commit、没 `push share`**。**发新机前先 `git push share main`，否则一线永远拿到没修的那版。**

## 相关文件

- `tools/install_git.ps1` —— 装 Git 的零依赖入口（源）
- `C:\hall-packages\repo\` —— 共享盘上的分发目录（产物）
- 《测试机操作手册.md》步骤 1、《新机操作清单.md》第 2 节 —— 已按本方案改写
- `tools/sync_handbook.py` 的 `DOCS` 列表 —— 改完手册记得同步桌面副本
