# 测试机操作手册（照着做就行）

> 最后更新：2026-09-21
> 面向：拿到一台新测试机的测试人员
> 一句话：**你要做的就是「跑 3 条命令 + 填 1 个文件 + 按住屏幕别锁」**，其他全是脚本自己干。
>
> 📋 **只想照着敲、不想看原因** → 用《新机操作清单.md》（纯步骤，不带解释，8 步）。
> 本手册是它的完整版：每个动作的**为什么**、报错了往哪查，都在这儿。

---

## 第 0 步：先搞清楚这套东西是什么

三台角色的机器，别搞混：

| 角色 | 谁 | 干什么 |
| --- | --- | --- |
| **包源机** | 现在这台笔记本 `LAPTOP-VS5F7HF4` | 存安装包 + 当"任务中转站"。**必须一直开着** |
| **控制机** | 随便一台，通常是包源机或你的办公机 | 负责投任务、看进度、出报告 |
| **测试机（节点机）** | 你要操作的新机器，可能 5 台 | 真正跑用例的那台 |

**跑批的物理过程长这样**：

```
控制机投任务 → 写到共享盘 tasks/R01.json
                      ↓
              测试机每 30 秒去共享盘看一眼
                      ↓
           有任务就取走（改名叫 .running）→ 跑用例
                      ↓
      跑完写回执 done/R01_task_xxx.json + 把证据目录拷到 results/R01/
                      ↓
              控制机汇总 → 出一张「哪台机器挂在哪条用例」的矩阵表
```

记住这个流程，后面所有"为什么要这样做"都能自己想明白。

---

## ⚠️ 开工前必做（**只做一次，做完全部机器共用**）

**在包源机上，用管理员 PowerShell 跑一次**（完整说明见第七部分）：

```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
$env:HALL_SHARE_PASSWORD = "<共享账号密码>"      # 密码只走环境变量，别写进文件
powershell -NoProfile -ExecutionPolicy Bypass -File tools\provision_share.ps1
```
（也可以写成 `... provision_share.ps1 -SharePassword "<密码>"`，两种都行。
**推荐环境变量**：`HALL_SHARE_PASSWORD` 是节点机 `net use` 用的同一个变量，设一次就够。）

⚠️ **三行要一起执行，`cd` 不能省。** 不在仓库根目录跑会报
`-File 形式参数的实际参数"tools\provision_share.ps1"不存在` —— 这个报错读起来像"脚本没了"，
实际只是当前目录不对，不是文件缺失。

**为什么这件事排在最前面**：它负责在包源机上建出 `hall-farm` 共享（任务中转站）。
**没做这一步，后面所有测试机都连不上农场、取不到任务** —— 而且报错是"找不到网络路径"，
看起来像网络问题，实际是共享没建。**先做完这一步，再往下走。**

（如果你确认这个共享已经建好了 —— 在包源机上敲 `net share` 能看到 `hall-farm` —— 就跳过这里。）

### 全流程一览（照着这个顺序走，别跳）

| # | 在哪台机器 | 做什么 | 详细位置 |
| --- | --- | --- | --- |
| 1 | 包源机（**管理员**） | 建共享 `provision_share.ps1` | 上面这一节 / 第七部分 |
| 2 | 测试机 | 拉代码 `git clone` | 第二部分 步骤 1 |
| 3 | 测试机（**管理员**） | 铺环境 `bootstrap_machine.ps1` | 第二部分 步骤 2 |
| 4 | 测试机 | 填 `farm_node.env` 凭据 | 第二部分 步骤 3 |
| 5 | 测试机 | 验共享盘能连上 | 第二部分 步骤 4 |
| 6 | 测试机 | 采环境画像 `collect_machine.py` | 第二部分 步骤 5 |
| 7 | 控制机 | 设 `HALL_FARM_ROOT` + 投任务 `dispatch` | 第三部分 3.1 |
| 8 | 每台测试机 | 设 `HALL_FARM_ROOT` + 跑 `farm_agent` | 第三部分 3.2 |
| 9 | 控制机 | 看进度 `status` → 出报告 `aggregate` | 第三部分 3.3 |

**依赖关系**：1 必须在最前（没共享后面全白干）；3 必须在 7 之前（没铺环境取不到包）；
8 必须在 7 之后（任务得先投出去）。

---

## 第一部分：开工前的前置条件（缺一个都别开始）

### 1.0 先看清楚：哪些已经自动化了，哪些要你动手

**别把"被测的软件"和"跑测试要用的环境"搞混**，这是最容易误会的点：

| 软件 | 角色 | 自动化程度 |
| --- | --- | --- |
| **华硕大厅**（含各版本安装包） | **被测对象** | ✅ **全自动**。从共享盘取包 → 静默装 → 静默卸，P0 层全自动 |
| **网易云音乐**（P1-A 夹具） | 测试用的道具 | ✅ **全自动**。脚本驱动大厅去装/卸 |
| **7-Zip**（P1-A 更新夹具） | 测试用的道具 | ✅ **全自动**。`reset_fixture.py` 每轮自动压回钉住版本 |
| **7-Zip**（P2 解包工具） | 环境工具 | ✅ **已自动**。bootstrap 检测到没装就静默装（2026-09-18 起） |
| **内部安全工具**（P2 的 CheckAppV 等） | 环境工具 | ✅ **已自动**。bootstrap 从共享盘拷到本机（2026-09-18 起）。**它不进公开仓库，但可以放共享盘** —— 共享盘不是 git |
| **Python 3.x 64 位** | 环境工具 | ❌ **必须人工**（见下面的解释） |
| **Git** | 环境工具 | ⚠️ **可以不下**。最省事是浏览器点 `Download ZIP`（**连令牌都不用**）；命令行下载见步骤 1 路线 B |
| **磁盘可用空间 ≥ 3 GB** | 环境工具 | ✅ **已自动检查**。bootstrap 第 1 步会报剩余空间，低于 3 GB 明确警告 |
| **pip 源 / 外网** | 环境工具 | ⚠️ **半自动**。**只在第 2 步用一次**：bootstrap 会先探一次 pip 源，不通就几秒内报出来并给出两条绕法（**不拦路**，仍会尝试装）。**铺完之后跑批完全不需要外网** |

**为什么 Python 和 Git 自动化不了？** —— 这叫**引导悖论**：自动化脚本本身就是 Python 写的、
代码本身就是 Git 管的。没有 Python 就转不起来；Git 是用来"把代码搬过来"的，
但**搬运工不止它一个** —— PowerShell 自己就能下 zip 包（步骤 1 路线 B），
U 盘也能。**Python 才是真正绕不开的那一层**，其余都该由脚本负责。

**所以你要动手的事，一共就这 4 件**（其余全部自动，包括装 7-Zip、装依赖、取包、建计划任务）：

| # | 你要做的事 | 能省掉吗 |
| --- | --- | --- |
| 1 | 装 **Python 3.x 64 位**，安装时**勾上 "Add Python to PATH"** | ❌ **省不掉** —— 唯一的硬门槛，脚本本身是 Python 写的 |
| 2 | 装 **Git for Windows** | ✅ **能省**：浏览器 `Download ZIP`，或命令行下 zip（步骤 1 路线 B），**什么都不用装** |
| 3 | 找组长/负责人要 **3 样凭据**（共享盘账号密码、大厅测试账号密码、微软邮箱〔可选〕） | ❌ 省不掉 —— 脚本**故意不代劳**，密码是红线 |
| 4 | 开一个**管理员** PowerShell，跑一次 bootstrap | ❌ 省不掉 —— 第 4 步装 7-Zip、第 11 步建计划任务都要管理员 |

**"配合输入命令行"这个说法要修正一下**：真正要敲的命令只有**两条**
（设 3 个环境变量 → 跑 bootstrap，见步骤 2），而且**必须先开管理员窗口、再在窗口里设变量**。
另外还要**手工编辑一次 `farm_node.env`**（步骤 3）—— 那不是敲命令，是填文件。

⚠️ **特别注意 7-Zip 出现在两个角色里**（既是测试道具、又是环境工具），
这正是容易搞混的地方。记住结论：**7-Zip 你完全不用管。**

### 1. 网络前置条件（**最容易漏，漏了就白干**）

- [ ] **测试机和控制机（包源机）在同一个局域网里**，能互相 ping 通。
  - 自测方法：在测试机上开 PowerShell，敲 `ping LAPTOP-VS5F7HF4`，**必须看到有回复**。
  - 不通的话：可能不在同一网段、或者连的是访客 Wi-Fi（AP 隔离）。**这条路直接断了**，找 IT。

- [ ] **测试机和包源机的中断把 SMB 放行**（TCP 445 端口）。
  - 自测方法：在测试机上敲 `Test-NetConnection LAPTOP-VS5F7HF4 -Port 445`，看 `TcpTestSucceeded` 是 `True`。
  - 是 `False`：包源机那边的防火墙规则没生效，或者中间有防火墙挡着。

- [ ] **包源机能被访问到网络共享**。
  - 自测方法：在测试机上敲 `net use \\LAPTOP-VS5F7HF4\hall-packages /user:hallshare "<密码>"`，回 `命令成功完成`。
  - 报「找不到网络路径」= 防火墙问题；报「输入的凭据无效」= 账号或密码错了；报「系统错误 53」= 网络不通。

- [ ] **这台测试机能访问 pip 源**（**只在第 2 步用一次**）。
  - 自测方法：在测试机上敲 `Test-NetConnection pypi.org -Port 443`，看 `TcpTestSucceeded` 是 `True`。
  - **不通也不一定卡死**，但要提前知道。两条绕法（bootstrap 报错时也会打印这两条）：
    1. 用公司内网镜像：`pip config set global.index-url <镜像地址>`，然后重跑 bootstrap。
    2. 从一台**已经铺好**的机器把整个 `.venv` 目录拷过来，然后加 `--skip-venv` 重跑（完全不碰网络）。
  - 铺完之后**跑批不再需要外网**：安装包走共享盘，7-Zip 夹具包共享盘上也有。

⚠️ **重要前提：包源机必须一直开机、不休眠。** 它一关，所有测试机拿不到安装包 —— 因为官方没有下载地址，共享盘是唯一的包来源，没有兜底。

### 2. 测试机自己的前置条件

- [ ] **必须是 Windows 10/11 64 位**（32 位不行，pywinauto 访问不了 64 位进程）。
- [ ] **必须用真人登录进桌面**，**不能锁屏、不能息屏、不能断 RDP**。
  - 这是硬要求。锁屏后脚本截图全是黑屏、点击没有落点，整批用例会全部假失败。
  - 脚本会自己保持屏幕常亮（不让它睡），但**它管不了你手动按 Win+L 锁屏**，也管不了 RDP 断开。
- [ ] **7-Zip**：**不用管，脚本会自己装**（缺了 bootstrap 自动静默装钉住的 26.02）。
  - 为什么需要它：P2 安全验证要用 `7z` 解包待检安装包。
  - 只有两种情况才需要你动手：①**你没用管理员跑 bootstrap**（装到 `C:\Program Files` 需要管理员，脚本会跳过并提示）②**这台机器只跑 P2 且装失败了**。两种都会在 bootstrap 输出里明确告诉你。
- [ ] **Python 3.x 64 位** 已安装（能跑 `python --version` 就行，脚本会自己建虚拟环境）。
  - **这个没法自动化** —— 脚本本身是 Python 写的，没有 Python 就转不起来。属于"第 0 层"依赖，只能人工准备。
    （它是**唯一**真正绕不开的：Git 可以不下，用步骤 1 路线 B 下 zip 包即可。）
  - ⚠️ **必须能在命令行里敲 `python` 就出来**（也就是在 PATH 里）。装了但 PATH 没配的话，
    新机器第一次跑 bootstrap 会直接失败 —— 因为它要先用系统 Python 建虚拟环境。
    自测：开个新 PowerShell 敲 `python --version`，能打印版本号才算过。装的时候记得勾
    "Add Python to PATH"。
  - **bootstrap 会给你中文报错，不用猜**。第 1 步之前它会先找 Python，三种情况都有明确提示：
    | 情况 | 你会看到 |
    | --- | --- |
    | 没装 / 没勾 PATH | `[FAIL] 这台机器上找不到可用的 Python` + 安装地址 + "务必勾 Add Python to PATH" |
    | PATH 上只有 `py` 启动器 | `[提醒] python 不在 PATH 里，本次改用 py 启动器顶上`（**能跑**，但手册的自检会失败，建议补上 PATH） |
    | 敲 `python` 弹微软商店 | `[FAIL]` + 点明这是「应用执行别名」桩，并给出关掉它的路径 |
  - ⚠️ **「应用执行别名」是个真陷阱**：Windows 自带一个假的 `python.exe` 桩（在
    `...\WindowsApps\` 下），敲 `python` 会弹微软商店。**它看着像装了 Python，其实没装。**
    自测时如果弹出商店，去 设置 → 应用 → 高级应用设置 → 应用执行别名，把
    `python.exe` / `python3.exe` 关掉，再装真 Python。
- [ ] **有管理员权限的账号**（至少一开始要，用来建计划任务、装 7-Zip）。
- [ ] **测试机上没装华硕大厅**，或者装了也行（脚本会先卸再装）。
- [ ] **屏幕缩放不用管**，150% / 125% / 100% 都不影响正确性。**千万不要手动改成 100%**，改了反而没用（脚本走物理像素）。
- [ ] **系统盘（或你指定的安装盘）至少留 3 GB 可用空间**。**脚本会自己检查并告诉你**（bootstrap 第 1 步）。
  - 为什么：安装包缓存约 800MB、装上去的大厅本体几百 MB、加上证据截图。
  - **盘满了的报错长得像"共享盘坏了"**（拷到一半失败），会把人往错的方向带 —— 所以第 1 步就先把剩余空间报出来。
  - 空间不够：先清盘，或用 `-InstallerDir "D:\Test\大厅"` 指到别的盘。

### 3. 要提前拿到的三样东西（找组长/负责人要）

| 东西 | 说明 | 放哪 |
| --- | --- | --- |
| **共享盘账号密码** | 账号通常叫 `hallshare` | 用的时候临时设成环境变量，**别写进任何文件** |
| **测试账号 + 密码** | 华硕大厅的登录账号，只有 1 个，多机同时登录不会互踢 | 填到测试机本地的 `farm_node.env` |
| **微软邮箱**（可选） | 如果这台机器要跑微软 SSO 免密登录用例 | 同上 |

⚠️ **密码规则（红线，别踩）**：
- 密码**只走环境变量或本机 `farm_node.env`**，那个文件已经在 `.gitignore` 里。
- **绝对不许**把密码写进共享盘任务文件、不许写进 git、不许贴到聊天里。
- 任务文件里如果混进了密码，脚本会**直接拒绝这个任务**，不会偷偷忽略。

---

## 第二部分：新测试机操作步骤（按顺序做，别跳）

### 步骤 1：把代码搞到测试机上

**Python 一定要先装好**（脚本自己装不了，见 1.0）：
在 PowerShell 里敲 `python --version`，**能打印版本号才算过**
（装了但 PATH 没配也算没过 —— 见 1.2 那一条）。

**Git 可以不下。** 下面三条路线任选一条，区别只在"用什么把代码搬过来"：

#### 路线 A：有 Git（最常规）

```powershell
cd C:\Users\你的用户名\Desktop
git clone https://github.com/Alvenovo/Huashuo-Dating-Automation
cd Huashuo-Dating-Automation
```

**⚠️ 仓库是 private，第一次 clone 会弹 GitHub 登录**（浏览器授权或填 token）。
在这台机器上 `git config --global credential.helper manager`，然后按弹窗登录一次。

#### 路线 B：**不下 Git**，用系统自带的东西把代码弄下来（推荐）

**B-1 浏览器下载（不用令牌，最省事）**：浏览器打开仓库页 → 绿色 `Code` 按钮 →
`Download ZIP` → 解压 → 把文件夹名后面的 `-main` 去掉 → 放到桌面。
前提是浏览器里登录了**有仓库权限的账号**。完整步骤见《新机操作清单.md》步骤 1B-1。

**B-2 命令行下载（要一个令牌）**：适合这台机器不方便开浏览器的场合。

```powershell
$ProgressPreference = "SilentlyContinue"
[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
cd C:\Users\你的用户名\Desktop
$tok = Read-Host "粘贴令牌"

Invoke-WebRequest -Headers @{ Authorization = "Bearer $tok" } `
  -Uri "https://api.github.com/repos/Alvenovo/Huashuo-Dating-Automation/zipball/main" `
  -OutFile repo.zip -MaximumRedirection 5

Expand-Archive repo.zip -DestinationPath _tmp_repo
$inner = Get-ChildItem _tmp_repo | Select-Object -First 1
Move-Item $inner.FullName Huashuo-Dating-Automation
Remove-Item _tmp_repo, repo.zip -Recurse -Force
Remove-Variable tok
Clear-History
Remove-Item (Get-PSReadlineOption).HistorySavePath -ErrorAction SilentlyContinue
```

**为什么用 `api.github.com/.../zipball/main` 而不是 `github.com/.../archive/refs/heads/main.zip`**：
后者是网页端按钮用的地址，**GitHub 的 API 文档里没有它**；前者是官方文档记载的
「Download a repository archive (zip)」端点，对细粒度令牌要求 **`Contents: Read-only`**
（来源：GitHub REST API 文档 "REST API endpoints for repository contents"）。
私有仓库要走有文档保证的那条。

**令牌怎么申请**：细粒度令牌（`github_pat_` 开头），**只给 `Contents: Read-only` 一个权限**，
`Repository access` 勾上本仓库。有预填好权限的直达链接和逐步截图式说明，
见《新机操作清单.md》步骤 1B-2 —— **令牌只显示一次，生成后立刻复制走**。

**报错对照**：`401` 令牌复制不全 / `404` 权限不够（GitHub 会假装"仓库不存在"）/
`403` 令牌过期或 Contents 没给 Read-only。表在《新机操作清单.md》步骤 1B-2。

**这条路线的三个好处**（也正是路线 C 的三个坑，见下）：
- GitHub 打的 zip **不含 `.gitignore` 里的东西** → 不会带上老机器的 `config.local.yaml`、
  不会带上 `farm_node.env`（明文密码）、不会带上 2.8 万个文件的 `reports/`；
- 包体小、解压快；
- 全程只用系统自带组件。

**代价**：拿到的是快照、**没有 `.git`**，以后不能 `git pull` 更新 —— 要更新就重下一次。

⚠️ **令牌是密码级的凭据**：不能写进任何文件、不能贴进聊天或群里。用完清掉命令行历史
（上面最后两行就是干这个的）。

#### 路线 C：整包拷贝（**无网、无令牌时**才用）

让有权限的人在**已经铺好**的机器上打包整个目录拷过去，然后用 `-SkipVenv` 跑 bootstrap
（`.venv` 带着就不用联网装依赖）。

**⚠️ 但必须从压缩包里排除这三样**，否则会把老机器的状态一起搬过来：

| 排除 | 为什么 |
| --- | --- |
| `config.local.yaml` | 它被 gitignore，**所以 clone 拿不到、拷贝却拿得到**。bootstrap 取 `installer_dir` 的优先级是 `--installer-dir` > **已有 config** > 默认值 —— 于是新机器会继承老机器的 `C:/Users/<老用户名>/...`，取包必然失败，**而报错长得像"共享盘坏了"** |
| `farm_node.env` | 里面是**明文密码**。拷来拷去等于让密码到处旅行 |
| `reports/` | 2.8 万个文件、体积巨大，且是本机证据，对新机器毫无用处 |

另外 `.venv` 跨机器**可能起不来**：`pyvenv.cfg` 里的 `home` 指向老机器的 Python 安装路径，
两台机器用户名或安装位置不同就会失败。**真拷了就先试**
`.venv\Scripts\python.exe --version`，报错就删掉 `.venv` 重跑（让它自己建）。

bootstrap 第 1 步会**主动检查**这些痕迹：发现 `installer_dir` 指向别的用户目录、
或 `config.local.yaml` 里的 `node_id` 与本机不符，会明确报警并告诉你怎么处理。

**预期结果**：目录里能看到 `AGENTS.md`、`tools\`、`tests\`、`hall_auto\` 这些文件夹。

**如果报找不到 python**：装 Python 3.x 64 位，**安装时务必勾 "Add Python to PATH"**。

---

### 步骤 2：跑铺环境脚本（**核心步骤，只做一次**）

**第 1 步：先开管理员 PowerShell**（开始菜单右键 → 终端(管理员) / Windows PowerShell(管理员)），
**就在这个窗口里**设共享盘凭据 + 农场目录：
```powershell
$env:HALL_SHARE_USER = "hallshare"
$env:HALL_SHARE_PASSWORD = "<共享盘密码>"
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"
```

⚠️ **顺序不能反：先开管理员窗口，再在里面设变量。** 管理员窗口是**全新窗口**，环境变量在里面是空的 ——
你在普通窗口设完再"以管理员身份运行"，那三个变量**一个都不会带过去**。
症状是第 7 步提示「未设 HALL_SHARE_USER/PASSWORD，无法自动连接」、第 10 步取包失败、
`farm_root` 也没落盘。自测：在管理员窗口里敲 `echo $env:HALL_FARM_ROOT`，能打出路径才算设上了。

⚠️ **`HALL_FARM_ROOT` 必须在这一步就设，不是可选项。** bootstrap 第 5 步**只在它存在时**才把农场目录
写进 `config.local.yaml`。这里不设，后面第三部分那句"跑过 bootstrap 就不用重设农场目录"就不成立 ——
测试机跑 `farm_agent` 会直接打印「未设 HALL_FARM_ROOT，config.local.yaml 里也没有 farm_root」然后退出。
（`HALL_SHARE_*` 不设的后果轻一些：bootstrap 第 7 步连不上共享盘，但**它不算失败**，会一直走到取包那步才暴露。）

**第 2 步：同一个窗口里接着跑**：
```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1
```

**（可选）如果这台机器在农场里的名字要用 `R01` 这种编号**，加 `-NodeId`：
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1 -NodeId R01
```
不指定的话节点名就是**主机名**。这个名字**必须和控制机 `dispatch --nodes` 里写的完全一致**，
否则节点一直报「无任务，退出」—— 看着像控制机没投任务，实际是名字对不上。

**这一步要跑多久？** 第一次大概 5-15 分钟（要建虚拟环境、装依赖、从共享盘拷 400MB 的安装包）。**中途别关窗口**。

**脚本会依次做这 12 件事**（它会自己判断，已经有跳过的，不会重复做）：

| # | 它做什么 | 你要看什么 |
| --- | --- | --- |
| 1 | 环境自检 | 检查系统版本、是不是 64 位 Python、有没有 7-Zip、是不是解锁桌面、**磁盘还剩多少** |
| 2 | 建虚拟环境 + 装依赖 | 会**先探一次 pip 源**再装（探不到会立刻提示 + 给绕法，但仍会尝试装）；第一次会 pip 装 5 个包，慢是正常的 |
| 3 | 校验夹具文件完整性 | 检查 7-Zip 夹具包的 sha256 对不对 |
| 4 | **7-Zip**（确保夹具包 + 缺了自动装） | ①夹具包不在就从共享盘/官方源取一份 ②本机没装 7-Zip 就静默装钉住版 26.02 ③已装就一个安装动作都不做 |
| 5 | 生成这台机器的配置 `config.local.yaml` | 落盘安装包目录、目标版 / 基线版两个包名、农场目录。**已有值不会被覆盖**（老机器手工配过的保留） |
| 6 | **生成凭据文件模板 `farm_node.env`** | 只生成空模板！**值要你自己填**（见步骤 3） |
| 7 | 连共享盘（**安装包共享 + 农场共享都连**） | 用你刚才设的环境变量去连；已可达就跳过 |
| 8 | **备内部安全工具**（P2 用） | 从共享盘把 `security_tools/` 拷到本机。共享盘上没有就只提示，不判失败 |
| 9 | 准备农场目录 `tasks/ done/ results/ logs/` | |
| 10 | 准备安装包 | 本地有就用本地的，没有就从共享盘拷 |
| 11 | **建提权计划任务 `HallAutoP1`** | 这一步必须管理员，否则会 FAIL |
| 12 | 跑单元测试自检（**全量 `tests/unit`**，约 1-2 分钟） | 全绿说明环境 OK |

**安装包会落到哪？** 新机器上 bootstrap 默认把 `installer_dir` 设成
`C:\Users\Public\Desktop\Test\华硕大厅`（想换位置加 `-InstallerDir "D:\Test\大厅"`）。
从共享盘取回的包落在它下面的 `_cache\` 子目录里，两个包合计约 **800MB**，**别当垃圾删掉** ——
删了下一次还要从共享盘重拷一遍。想清缓存请确认不再跑批了再动手。

**预期结果**（结尾长这样，注意方括号里是 `OK` 不带多余空格）：
```
[OK] 环境自检
[OK] 虚拟环境与依赖
...
[OK] 提权计划任务
[OK] 自检 tests/unit
结果：全部通过，本机已可跑批
```

脚本最后会打印一个报告文件的路径：`reports\bootstrap\bootstrap_<机器名>_<时间>.json`。

**❗ 几种可能的报错，这样处理**：

| 报错 | 什么意思 | 怎么办 |
| --- | --- | --- |
| **脚本还没开始就** `[FAIL] 这台机器上找不到可用的 Python` | 这台机器没装 Python，或者装了但**没勾 "Add Python to PATH"**。这是**唯一需要人工装**的东西 | 按报错里给的地址装 Python 3.x **64 位**，安装向导第一屏**务必勾 "Add Python to PATH"**；装完**开一个新窗口**敲 `python --version` 验证，再重跑。见 1.2 |
| **脚本还没开始就** `[FAIL]` 并提到「应用执行别名」 | 你敲的 `python` 是 Windows 自带的**假桩**（会弹微软商店），不是真 Python。**看着像装了，其实没装** | 设置 → 应用 → 高级应用设置 → 应用执行别名，关掉 `python.exe` / `python3.exe`，然后装真 Python |
| **脚本还没开始就** `[提醒] python 不在 PATH 里，本次改用 py 启动器顶上` | 不是错误 —— 靠 `py` 启动器能跑起来。但手册步骤 1 的 `python --version` 自检会失败，`run_p0_*.ps1` 的兜底路也会找不到 | 能跑就先跑；有空把 PATH 补上（重装 Python 并勾 "Add Python to PATH"，或手动把 Python 目录加进 PATH） |
| 第 11 步 FAIL「非管理员无法建计划任务」 | 你没用管理员身份跑 | 关掉窗口，重新用**管理员** PowerShell 跑一遍。幂等的，前面做过的会跳过 |
| 第 2 步打印「⚠️ 探不到 pip 源：pypi.org:443 连不上」 | 测试机连不上 pip 源（内网机常见）。**脚本几秒内就报出来，不会让你等 pip 重试** | 这条只是**提示不拦路**，它仍会尝试装。装失败时按报错里的两条绕法：① `pip config set global.index-url <内网镜像>` 后重跑；② 从已铺好的机器拷 `.venv` 过来 + `-SkipVenv` 重跑 |
| 第 2 步 FAIL「pip install 失败」 | 探得到源但装不下来（镜像缺包、版本冲突） | 看报错里那 400 字符的 stderr；确认源上有这 5 个包，或换源 |
| 第 1 步提示「⚠️ 磁盘剩余 X GB，低于建议的 3 GB」 | 盘不够，**不是错误但会中途失败** | 先清盘，或用 `-InstallerDir "D:\Test\大厅"` 指到别的盘。**别忽略**：盘满的报错长得像"共享盘坏了" |
| 第 1 步 FAIL「非交互式会话」 | 屏幕锁了 / RDP 断了 | 把屏幕解锁，重跑 |
| 第 1 步提示「⚠️ installer_dir 指向别的用户目录（xxx）」 | 你是用**整包拷贝**（步骤 1 路线 C）把代码搬过来的，老机器的 `config.local.yaml` 也被带过来了，于是 `installer_dir` 指向 `C:/Users/xxx/...`，而本机没这个用户。**不改的话取包会在第 10 步失败，报错长得像"共享盘坏了"** | 加 `-InstallerDir "C:/Users/Public/Desktop/Test/华硕大厅"` 重跑；或删掉 `config.local.yaml` 再跑。**根治办法是改用路线 B（下 zip）** |
| 第 1 步提示「⚠️ config.local.yaml 里的 node_id 是 xxx」 | 同上，整包拷贝的痕迹 | 目前没有代码读这个字段，**不影响跑批**。想干净就删掉 `config.local.yaml` 里那一行；如果这个值是 `-NodeId` 故意指定的，忽略即可 |
| 第 1 步只是提示「未装 7-Zip」 | 不是错误 | 下一步会自动装。若下一步也没装成功，会告诉你原因（多半是没用管理员） |
| 第 4 步「未装 7-Zip 且当前非管理员」 | 装到 `C:\Program Files` 要管理员 | 用管理员重跑一遍即可。**不影响其他套件**，只影响 P2 |
| 第 3 步只是提示「暂无 7-Zip 更新夹具包」 | 不是错误 | 下一步会按 **缓存 → 共享盘 → 官网** 顺序取。**测试机没有外网也没关系**：共享盘 `fixtures\7z2602-x64.exe` 有就行 |
| 第 7 步提示「连接失败：系统错误 67 找不到网络名」 | ①**共享还没建**（最常见）②地址被写成了 IP 且已过期 | ①回开头「⚠️ 开工前必做」，在包源机上跑 `provision_share.ps1`；②配置里要写机器名 `LAPTOP-VS5F7HF4`，不要写 IP |
| 第 7 步打印 `\\LAPTOP-VS5F7HF4\hall-farm: 连接失败 rc=2 ...` | 共享没建，或凭据不对 | 回开头「⚠️ 开工前必做」那一节，在包源机上跑 `provision_share.ps1` |
| 第 7 步打印「...且未设 HALL_SHARE_USER/PASSWORD，无法自动连接」 | 你在开管理员窗口**之前**设的环境变量 | 回步骤 2 的顺序提示：先开管理员窗口，再在**这个窗口里**设变量 |
| 第 8 步只是提示「共享盘上没有 fixtures/security_tools」 | 不是错误 | 只影响 P2 安全套件（会 skip）。补救见第八部分 |

**为什么要跑这一步？** 因为仓库里默认的配置写的是占位假路径 `C:/Users/ASUS/Desktop/华硕大厅`。不跑 bootstrap 的话，脚本会去这个不存在的路径找安装包 → 找不到 → 想下载 → 但没有下载地址 → **直接失败**。

**这一步可以重复跑吗？** 可以。脚本是幂等的，第二次跑会全部跳过，只花十几秒。

---

### 步骤 3：填凭据文件（**这一步必须人工，脚本故意不代劳**）

打开测试机上的 `farm_node.env`（就在项目根目录里），长这样：

```ini
# --- 登录套件（P1-B）---
HALL_TEST_USER=
HALL_TEST_PASSWORD=
# 微软 SSO 免密用例
HALL_MS_USER=
# 改密码往返用例（只在人在环时用）
HALL_TEST_NEW_PASSWORD=

# --- 共享盘取包 ---
HALL_SHARE_USER=
HALL_SHARE_PASSWORD=
```

**你要做的**：把等号后面填上真实值。比如：
```ini
HALL_TEST_USER=hall_test_001
HALL_TEST_PASSWORD=xxxxxx
HALL_MS_USER=someone@outlook.com
HALL_SHARE_USER=hallshare
HALL_SHARE_PASSWORD=xxxxxx
```

**规则**：
- 值**不要加引号**。
- 用不上的留空就行，对应套件会自动跳过，**不影响其他套件**。
- **不需要重跑 bootstrap**。脚本每次取任务时会重新读这个文件。

**❗ 为什么不填就出事**：
登录套件（`login`）跑起来会**静默跳过** —— 不报错，但一条都没真正跑。报告上看起来是黄色（跳过），如果不看凭据列，你会以为"跑过了没问题"，其实是**什么都没验证**。

**预期结果**：文件保存后，可以敲这行验证一下能不能读到（**在仓库根目录敲**）：
```powershell
.\.venv\Scripts\python.exe -X utf8 -c "from pathlib import Path; from hall_auto.env_pack import build_suite_env, credential_status; env,_=build_suite_env(node_env_path=Path('farm_node.env')); print(credential_status(env))"
```
应该看到 `password_login: True` 之类（填了就是 True，没填是 False）。

⚠️ **`node_env_path=Path('farm_node.env')` 这段不能省。** 不传它，函数**根本不会去读这个文件**，
只认当前窗口里的环境变量 —— 你明明填对了，也会打印一片 `False`，白折腾半天。

---

### 步骤 4：手工验证一下能连上共享盘农场

```powershell
net use \\LAPTOP-VS5F7HF4\hall-farm /user:hallshare "<共享盘密码>" /persistent:yes
dir \\LAPTOP-VS5F7HF4\hall-farm
```

**预期结果**：能看到 `tasks`、`done`、`results`、`logs` 四个文件夹。

**看不到怎么办**：
- 报「找不到网络路径」→ 两种情况：①**共享还没建**（回开头「⚠️ 开工前必做」那一节）②防火墙/网络不通（回第 1 部分检查）。
- 只看到部分文件夹 → 共享建了但农场目录没建全，在包源机上重跑一次 `provision_share.ps1`。
- 报「拒绝访问」→ 共享权限没给够。`hall-farm` 需要 **Change（可写）**权限，只给读会导致"看起来很成功但任务永远取不走"。

**为什么要手工验一次**：bootstrap 第 7 步会自动连，但连不上时**它不算失败**（设计如此——可能这台机器有本地包）。
所以手工确认一次，比事后从一堆黄色用例里倒推原因省事得多。

---

### 步骤 5：确认这台机器已经准备好

```powershell
.\.venv\Scripts\python.exe -X utf8 tools\collect_machine.py
```

**预期结果**：打印出这台机器的环境画像（系统版本、分辨率、缩放、WebView2 有没有装、大厅版本等）。

把这些信息填到控制机的 `machines.yaml` 台账里（人工维护，方便后面按机型分组看结论）。

---

## 第三部分：控制机（你）要做的三件事

**先设农场目录**（**控制机和每台测试机都要**）：
```powershell
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"
```

⚠️ **好消息：跑过一次 bootstrap 的机器不用每次重设了。**
bootstrap 第 5 步会把 `HALL_FARM_ROOT` 的值写进 `config.local.yaml` 的 `farm_root`，
之后 `farm_agent` / `farm_control` 会**自动从配置读**，环境变量只是临时覆盖（比如临时切到别的农场）。

所以优先级是：**环境变量 > `config.local.yaml`**。
两边都没有才会报错退出 —— 控制机如果没跑过 bootstrap，就还得手工设一次这行。

### 3.1 投任务

```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py dispatch --nodes R01,R02,R03,R04,R05 --suites launch,apps-detect
```

**参数说明**：
- `--nodes`：测试机的节点名字，逗号分隔。**要和测试机的 `node_id` 对得上**（默认是主机名，也可以用 bootstrap 的 `--node-id` 指定）。
- `--suites`：要跑哪些套件，逗号分隔。**可用套件名单**：

| 套件名 | 跑什么 | 需要什么前提 | 大概多久 |
| --- | --- | --- | --- |
| `unit` | 单元测试（纯逻辑，不碰界面） | 无 | 1 分钟 |
| `install` | 安装/卸载华硕大厅 | **管理员**；卸载闸由 `dispatch` 自动开（见下方警告） | 15-20 分钟 |
| `launch` | 启动冒烟 + 搜索（P0） | 已装大厅 | 3-5 分钟 |
| `apps-detect` | 应用列表检测（只读，不装东西） | 已装大厅 | 3 分钟 |
| `apps-lifecycle` | 真装真卸第三方应用 | **管理员**；卸载闸由 `dispatch` 自动开 | 10-15 分钟 |
| `login` | 登录 + 注册（P1-B） | **需要测试账号** | 5 分钟 |
| `login-manual` | 短信登录 / 改密码（**人在环**） | 要真人守着。**`dispatch` 会直接拒绝投它** | 5 分钟 |
| `settings` | 主题切换（P1-C） | 已装大厅 | 2 分钟 |
| `security` | 安装包签名验证（P2） | 内部安全工具在（bootstrap 第 8 步自动从共享盘备） | 1 分钟 |
| `wb` | WorkBuddy 入口（P2） | WB 后台开关已开、WB 未安装 | 1-2 分钟 |

⚠️ **投 `install` / `apps-lifecycle` 等于给那批机器开了"真装真卸"的闸**：
`dispatch` 会自动往任务文件里带 `HALL_ALLOW_INSTALL=1`，节点收到就真卸真装，
**没有第二次确认**。所以：

- 别在办公机上投这两个套件，也别拿它们试探；
- 投之前先确认 `--nodes` 里每台都是**专门用来跑批的测试机**；
- 只想看环境、不装东西，就投 `launch` / `apps-detect` / `settings`。

⚠️ **`login-manual` 投不出去是设计如此，不是脚本坏了**。它要真人在那台机器上收短信、
改密码，投进无人值守农场只会让 agent 卡在等输入、那台机器再也取不到新任务。
要跑它，去**那台机器的交互式终端**里手工执行：
```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --local login-manual
```

**预期结果**：
```
R01: 2 个套件 -> R01.json
R02: 2 个套件 -> R02.json
...
任务 task_20260918_180000 已投给 5 台节点（共 5 台）。

并发预算：
  launch 全并行（不碰服务端）
  apps-detect 全并行（不碰服务端）
```

⚠️ **那句"并发预算"别忽略**，它告诉你哪些套件被限流了。

---

### 3.2 叫测试机开跑

**在每台测试机上开跑**（跑过 bootstrap 的机器直接敲命令就行）：
```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"   # 只在没跑过 bootstrap 时才需要
```
⚠️ **跑过 bootstrap 的话，上面那行不用敲** —— 第 5 步已经把农场目录写进
`config.local.yaml` 的 `farm_root`，`farm_agent` 自己会读。只有临时想切到别的农场时才用环境变量覆盖。

**方式 A（推荐，一次性）**：
```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --once
```
取一次任务，跑完就退出。

**方式 B（常驻，无人值守）**：
```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --loop
```
每 30 秒去共享盘看一眼，有任务就跑。**跑批期间让这个窗口一直开着**。

**测试机那边的预期输出**：
```
节点 R01 农场目录 \\LAPTOP-VS5F7HF4\hall-farm
取到任务 task_20260918_180000
  跑 launch（分片 1/1）
  跑 apps-detect（分片 1/1）
  证据已回传 \\LAPTOP-VS5F7HF4\hall-farm\results\R01\R01_2026-09-18_1800
任务完成，结果 \\LAPTOP-VS5F7HF4\hall-farm\done\R01_task_20260918_180000.json
```

**出问题时对照**：

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| 打印「未设 HALL_FARM_ROOT，config.local.yaml 里也没有 farm_root」后立刻退出 | 环境变量没设，而且这台机器没跑过 bootstrap | 跑一次 bootstrap（会自动落盘），或者临时设一次环境变量 |
| 打印「系统错误 67 找不到网络名」 | 农场目录写的是**过期 IP** | 改用机器名：`\\LAPTOP-VS5F7HF4\hall-farm` |
| 打印「无任务，退出」 | 控制机还没投任务，或者**节点名对不上** | 检查 `node_id` 是否等于 `dispatch --nodes` 里写的名字 |
| 报「拒收任务」 | 同一个 `task_id` 已经跑过了（防重复执行） | 控制机换个 `--task-id` 重投 |
| 报「找不到网络路径」 | 共享没建 / 没认证 | 回开头「⚠️ 开工前必做」那一节，或手工 `net use`（见第二部分步骤 4） |

---

### 3.3 看进度 + 出报告

**看进度**（随时可以敲）：
```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py status
```
**预期结果**：
```
待执行 0 个，执行中 2 个，已完成 3 个
  执行中: R01.json.running
  执行中: R02.json.running
  已完成: R03_task_20260918_180000.json
  ...
```

**出报告**（全部跑完后）：
```powershell
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py aggregate
```
**预期结果**：
```
汇总报告：\\LAPTOP-VS5F7HF4\hall-farm\aggregate_report.html
覆盖节点 5 台，用例 157 条
```

打开那个 HTML，就是最终交付物：一张「节点 × 用例」矩阵表，一眼看出哪台机器挂在哪条用例。

---

## 第四部分：跑批过程中，测试人员要盯着什么

### 必须做的

| 项目 | 为什么 |
| --- | --- |
| **别锁屏、别合盖、别关显示器** | 锁屏后截图全黑、点击没落点，整批用例假失败。脚本只能保屏幕常亮，管不了你手动锁 |
| **别断 RDP** | RDP 一断，Windows 会话变非交互态，脚本会直接报错退出（这是故意的，比产出一堆假数据好） |
| **别在测试机上手动操作** | 脚本在点鼠标键盘，你一动就会互相干扰 |
| **别关测试机的 agent 窗口**（如果用 `--loop`） | 关了就不取任务了 |
| **确保包源机不关机** | 它一关，正在跑的机器取不到包 |

### 会看到什么现象（正常，别慌）

| 现象 | 正常吗 | 说明 |
| --- | --- | --- |
| 屏幕忽明忽暗 | ✅ 正常 | `settings` 套件在切主题 |
| 大厅自己启动又自己关 | ✅ 正常 | 各套件在跑 |
| 有几个软件被装上又卸掉 | ✅ 正常 | `apps-lifecycle` 在真装真卸夹具应用 |
| 报告里一片黄（skip） | ⚠️ 要看原因 | 看报告「节点环境」表里的**凭据列**：✗ = 这台机器没配凭据（去填 `farm_node.env`）；✓ = 凭据配了，是用例本身在跳过 |
| 汇总时打印「⚠ 这些节点没拿到凭据状态」 | ⚠️ 要留意 | 说明这批结果里凭据列不可信（节点是旧版本、或证据没回传全）。**别拿那一列的 ✗ 当结论**，直接去那台机器看 `farm_node.env` |
| 整批突然停在某个点 | ❌ 要查 | agent 可能挂了。看测试机的 `farm_root\logs\<节点>_<任务>.log` |

### 绝对不能做的（**点了就出事**）

- **绝对不要在大厅界面上点「全部安装」**。列表里全是 QQ / 微信 / WPS 这类日常软件，点下去就真装一机器。
- **绝对不要把 `install` / `apps-lifecycle` 投给办公机或不归你管的机器**。
  这两个套件一旦被投出去，`dispatch` 会自动带 `HALL_ALLOW_INSTALL=1`，节点会**真卸真装**，
  中间没有第二次确认。
- **绝对不要在办公机上手工设 `HALL_ALLOW_INSTALL=1`**，也不要拿它去"试试看"。
- **别想着把 `login-manual` 硬投出去**（加 `--allow-manual` 能绕过，但除非你正盯着那台机器的屏幕，否则别用）——
  它会卡住那台节点，后面所有任务都进不来。

---

## 第五部分：跑完之后

1. **收报告**：把 `aggregate_report.html` 发给组长。这是单文件 HTML，不需要别的依赖，双击就能看。

2. **看证据**：每条用例的截图在共享盘 `results\<节点>\<运行目录>\` 里，有 `final.png`（终态截图）和 `result.json`。

3. **关防火墙**（如果跑批期间为了让共享盘通而临时开的 445）：
   ```powershell
   Remove-NetFirewallRule -DisplayName "HallShare-SMB"
   ```
   ⚠️ 但**如果还要继续跑批就先别关**，一关共享盘就断了。

4. **清下载缓存**：`C:\AsusMCenterDownload` 会堆积安装包（每个几百 MB），定期清理。

5. **别再动测试机**：跑完停在什么状态就是什么状态，下一轮脚本会自己复位。

---

## 第六部分：出问题怎么查（对照表）

### 测试机侧

| 现象 | 大概率原因 | 怎么解 |
| --- | --- | --- |
| agent 一直说「无任务」，但控制机明明投了 | ①节点名对不上 ②上一轮进程被杀留下 `.running` 占位挡住了 | ①确认 `node_id` 和 `--nodes` 一致 ②agent 启动时会自动清残留，重跑一次；还不行就看 `tasks\` 下有没有 `.running` 文件 |
| 控制机说投给 5 台，只有 3 台取到 | 另 2 台网络不通或 agent 没起 | 去那 2 台检查 `net use` 和 agent 进程 |
| 用例大批失败且截图全黑 | **屏幕锁了 / RDP 断了** | 解锁屏幕重跑。这是最常见的原因 |
| 登录用例全 skip | `farm_node.env` 没填凭据 | 填上（见步骤 3） |
| 报「找不到安装包」 | 没跑 bootstrap，还在读假路径 `C:/Users/ASUS/...` | 跑 bootstrap |
| 报「拿不到包，已阻止卸载」 | 共享盘不通 + 没有下载地址 | 检查包源机是否开着、网络是否通 |
| 装到一半失败 | 权限不够 | `install` 套件要管理员 + 计划任务 `HallAutoP1` |
| 找不到 `HallAutoP1` 计划任务 | bootstrap 第 11 步失败 | 用管理员重跑 bootstrap |

### 共享盘侧

| 现象 | 原因 | 怎么解 |
| --- | --- | --- |
| `dir` 没任何输出也不报错 | **目录是空的**，不是失败 | 放个文件再试 |
| 「找不到网络路径」 | 防火墙挡了 445 | 包源机放行 445 |
| 「输入的凭据无效」 | 用了没密码的账号 | 必须用带密码的 `hallshare` 账号 |
| 「拒绝访问」 | 共享权限没给对 | `hall-farm` 要 **Change**，`hall-packages` 要 **Read** |
| `ping` 都不通 | AP 隔离 / 不同网段 | 这条路断了，找 IT |

---

## 第七部分：一次性准备工作（只做一次，做完全部机器共用）

⚠️ **这一步要人工，而且必须用管理员身份。** 原因是：建 SMB 共享是内核级的特权操作，非管理员进程会报「系统错误 5 拒绝访问」，而且 UAC 那个「是」的确认框画在安全桌面上，非管理员进程根本碰不到 —— 这是唯一绕不过去的硬墙。**但只需按这一次**，5 台机器共用，不用每台都提权。

**在包源机上，用管理员 PowerShell 跑**：
```powershell
cd C:\Users\你的用户名\Desktop\Huashuo-Dating-Automation
$env:HALL_SHARE_PASSWORD = "<共享账号密码>"      # 密码只走环境变量，别写进文件
powershell -NoProfile -ExecutionPolicy Bypass -File tools\provision_share.ps1
```
（也可以写成 `... provision_share.ps1 -SharePassword "<密码>"`，两种都行。
**推荐环境变量**：`HALL_SHARE_PASSWORD` 是节点机 `net use` 用的同一个变量，设一次就够。）

⚠️ **密码必须是 `hallshare` 账号的当前真实密码。** 账号已存在时本脚本只做
「`already exists, left untouched`」——**不会改密码**。随手设一个新密码跑这里，
脚本照样报 `[OK] share account hallshare`，但账号真实密码没变；接着节点机拿这个新密码
`net use` 会报「输入的凭据无效」，全批连不上。忘了原密码就先重置：
`net user hallshare "<新密码>"`，再把 `HALL_SHARE_PASSWORD` 设成同一个值。
**这个密码要一致地用在两处**：包源机跑 provision、每台节点机跑 bootstrap 之前。

⚠️ **管理员窗口是全新窗口**，环境变量在里面是空的，要**在新窗口里重设一遍**再跑。

⚠️ **三行要一起执行，`cd` 不能省。** 不在仓库根目录跑会报
`-File 形式参数的实际参数"tools\provision_share.ps1"不存在` —— 读起来像"脚本没了"，
实际只是当前目录不对。

⚠️ **`hallshare` 账号密码有到期日**（默认 42 天）。到期后所有节点 `net use` 会突然失败，
报错很难认。跑批周期跨月的话，先设成永不过期：
`Set-LocalUser -Name hallshare -PasswordNeverExpires $true`。

**它会做 6 件事**：
1. 确保 `hallshare` 账号存在（不存在就建）
2. 建 `hall-packages` 共享（**Read** 权限）+ `hall-farm` 共享（**Change** 权限）
3. 配 NTFS 权限
4. 放行防火墙 TCP 445
5. 建农场四个目录 `tasks/ done/ results/ logs/`
6. 最后自测一遍网络路径能不能通

**预期结果**（一共 11 行 `[OK  ]`，逐行核对）：
```
[OK  ] share account hallshare
[OK  ] share hall-packages
[OK  ] hall-packages -> hallshare = Read
[OK  ] NTFS C:\hall-packages -> hallshare = Read
[OK  ] share hall-farm
[OK  ] hall-farm -> hallshare = Change
[OK  ] NTFS C:\hall-farm -> hallshare = Modify
[OK  ] farm subdirectories
[OK  ] firewall HallShare-SMB
=== verification (network path, as a node sees it) ===
[OK  ] reachable \\LAPTOP-VS5F7HF4\hall-packages
[OK  ] reachable \\LAPTOP-VS5F7HF4\hall-farm
RESULT: OK
Shares are ready.
```
⚠️ 如果这台机器上共享**早就建好了**，`share hall-packages` / `share hall-farm` 那两行会变成
`already exists -> ... (left as is)`，`firewall` 那行会变成 `already present (left as is)` ——
**都是正常的**，脚本是幂等的。只要末尾是 `RESULT: OK` 就算过。

**退出码**：`0` = 成功 / `2` = 不是管理员 / `1` = 有失败项。

**❗ 报错这样处理**：

| 报错 | 怎么办 |
| --- | --- |
| `RESULT: NOT_ELEVATED`，退出码 2 | 你没用管理员。重新开管理员 PowerShell |
| `RESULT: NO_SHARE_ACCOUNT` | `hallshare` 账号不存在而且你没给密码。设 `$env:HALL_SHARE_PASSWORD` 或加 `-SharePassword "<密码>"` 后重跑 |
| `[FAIL] reachable \\...\hall-farm` | 共享建好了但本机访问不到，通常是防火墙。检查 `Get-NetFirewallRule -DisplayName "HallShare-SMB"` |
| 末尾不是 `RESULT: OK` | 上面一定有 `[FAIL]` 行，按那行的提示处理 |

⚠️ **`hall-farm` 为什么必须给 Change 而不是 Read**：节点要改任务文件名（取走任务）、要写回执、要推证据目录。给 Read 的话这些操作会**静默失败** —— 不报错，但任务永远取不走，你会查到怀疑人生。

---

## 第八部分：新机器上跑不了的套件（**提前知道，别白折腾**）

新机器铺完环境后，**不是所有套件都能跑**。下面这些要么要额外准备，要么条件不具备：

| 套件 | 新机器上会怎样 | 要什么才能跑 |
| --- | --- | --- |
| `launch` / `apps-detect` / `settings` | ✅ 能跑 | 只要装了大厅（bootstrap + P0 装完后就有） |
| `install` / `apps-lifecycle` | ✅ 能跑 | 管理员 + `HallAutoP1` 计划任务（bootstrap 建好） |
| `unit` | ✅ 能跑 | 无 |
| `login` | ⚠️ 会**静默跳过** | 该机 `farm_node.env` 填了 `HALL_TEST_USER/PASSWORD` |
| `security`（P2） | ✅ 能跑（2026-09-18 起） | 内部安全工具由 bootstrap 从共享盘自动拷；待检安装包按 `installer_dir\<latest_setup>` 找，找不到会回退到 `_cache\`（bootstrap 取回的包就在那）。**若共享盘上没有 `fixtures/security_tools/`，就会 skip** |
| `wb`（WorkBuddy） | ⚠️ 会跳过 | 该机 WB 后台开关已开、且 WB 未安装 |
| `login-manual` | ❌ 永不进农场 | 人在环，只能你在自己终端手动跑 |

**`security` 这条特别说明**：内部工具（CheckAppV / SignAppsV / signcheck_v2）按红线
**不进公开仓库**，但**可以放共享盘**（共享盘不是 git 仓库）。
现在包源机的 `C:\hall-packages\fixtures\security_tools\` 已经放了一份，
bootstrap 第 8 步会自动拷到每台机器的 `<installer_dir>\fixtures\security_tools\`。

所以**新机器上 P2 现在能真跑**，不再是必然黄。只有当共享盘上那份被删了、
或者第 8 步提示「共享盘上没有」时才会 skip —— 那时按提示在包源机补一份即可。

⚠️ 如果你不想让内部工具走共享盘（hallshare 账号能读到），
把 `C:\hall-packages\fixtures\security_tools` 删掉即可，脚本会退化成提示、不判失败，
P2 回到"需要手工拷"的状态。

---

## 附录 A：命令速查

```powershell
# --- 测试机 ---
$env:HALL_SHARE_USER="hallshare"; $env:HALL_SHARE_PASSWORD="<密码>"   # 跑 bootstrap 前要设
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"   # 只有没跑过 bootstrap 才需要（跑过就落进 config.local.yaml 了）

powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1   # 铺环境（管理员）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1 -InstallerDir "D:\Test\大厅"   # 系统盘紧张时指到别的盘
.\.venv\Scripts\python.exe -X utf8 tools\collect_machine.py                       # 采环境画像
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --once                     # 取一次任务
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --loop                     # 常驻取任务
.\.venv\Scripts\python.exe -X utf8 tools\farm_agent.py --local launch             # 不走共享盘，本机单跑

# --- 测试机：没有外网 / pip 源不通时（两条绕法，任选一条）---
pip config set global.index-url <内网镜像地址>    # ① 换镜像，然后重跑 bootstrap
# ② 从一台已铺好的机器整份拷 .venv 过来，然后：
powershell -NoProfile -ExecutionPolicy Bypass -File tools\bootstrap_machine.ps1 -SkipVenv

# --- 控制机 ---
$env:HALL_FARM_ROOT = "\\LAPTOP-VS5F7HF4\hall-farm"   # 没跑过 bootstrap 才需要
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py dispatch --nodes R01,R02 --suites launch
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py dispatch --nodes R01,R02 --suites launch --task-id task_002   # 重投同一批
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py status
.\.venv\Scripts\python.exe -X utf8 tools\farm_control.py aggregate
# 人在环套件（login-manual）会被 dispatch 拒绝；确要投才加 --allow-manual

# --- 包源机一次性（管理员）---
powershell -NoProfile -ExecutionPolicy Bypass -File tools\provision_share.ps1   # 密码走 $env:HALL_SHARE_PASSWORD
net share                                        # 确认 hall-packages 和 hall-farm 都在
```

## 附录 B：文件与目录在哪

| 东西 | 位置 |
| --- | --- |
| 铺环境报告 | `reports\bootstrap\bootstrap_<节点>_<时间>.json` |
| 采集的环境画像 | `reports\machine_<节点>_<时间>.json`（或 `.csv`） |
| 测试机本机凭据 | 项目根目录 `farm_node.env`（**已 gitignore，不进 git**） |
| 测试机本机配置 | 项目根目录 `config.local.yaml`（**已 gitignore**） |
| 节点执行日志 | 共享盘 `hall-farm\logs\<节点>_<任务>.log` |
| 任务回执 | 共享盘 `hall-farm\done\<节点>_<任务>.json` |
| 用例证据 | 共享盘 `hall-farm\results\<节点>\<运行目录>\` |
| 最终报告 | 共享盘 `hall-farm\aggregate_report.html` |
| 单机跑的证据报告 | 测试机本地 `reports\evidence\<节点>_<时间>\report.html` |

## 附录 C：几个必须知道的"为什么"

**为什么新机器第一步永远是 bootstrap？**
仓库里默认配置写的是占位路径 `C:/Users/ASUS/Desktop/华硕大厅`，实际不存在的路径。bootstrap 才会把这个路径改成这台机器的真实路径。不跑就找不到包。

**为什么凭据要手填、不自动导出？**
红线是「密码不落盘、不进 git」。自动从环境变量导出到文件，等于把本来是临时的密码写死在磁盘上了。所以脚本只生成一个空模板，落盘这个动作由人显式决定。

**为什么不用映射盘符（`Z:`）？**
映射盘符是跟登录会话绑的。计划任务和服务跑在另一个会话里，看不到你映射的 `Z:`。UNC 路径（`\\机器名\共享名`）没有这个问题。所以配置里一律写 UNC。

**为什么共享盘地址写机器名、不写 IP？**
因为 IP 是 DHCP 发的，**会变**。2026-09-18 就真踩过：`config.yaml` 里配的是
`//192.168.0.4/hall-packages`，这台笔记本重启后 IP 变成 `192.168.0.8`，
所有节点立刻报「系统错误 67 找不到网络名」—— 报错看着像"共享没建"，实际是 IP 过期了。
机器名靠 NetBIOS/LLMNR 解析，同一局域网里是稳的（`nslookup` 查不到不代表不能用）。

**为什么内部安全工具能放共享盘，却不能进仓库？**
红线是「不进**公开仓库**」。共享盘在局域网内、要 `hallshare` 账号才能读，
和安装包（未发布的内部版本）在同一个信任边界里。所以放共享盘合规，
bootstrap 就能自动把它备到每台机器 —— 这是把「N 台拷 N 次」压成「1 台拷 1 次」。

**为什么用机器名而不是 IP？**
DHCP 分配的 IP 重启会变，用机器名稳一些（前提是局域网名字解析正常）。
上面「为什么共享盘地址写机器名、不写 IP」那条讲的是同一件事的实例。

**为什么 `login-manual` 套件永远不进农场？**
因为它要真人守着（短信验证码、改密码），无人值守跑不了，硬跑只会卡在 `input()` 上 ——
而且卡住的不只是这一条用例，是**整台节点**（agent 不返回，后面投的任务都取不到）。
所以 `dispatch` 现在**直接拒绝**投它，而不是只打一句警告。要跑就在那台机器上
`farm_agent.py --local login-manual`。

**为什么登录用例没凭据是"静默跳过"而不是"报错"？**
因为设计上允许一台机器只跑不需要凭据的套件。但代价是容易误判 —— 所以报告里专门有「凭据」列来区分「没配凭据」和「用例本身在跳过」，看报告时记得看那列。
