.venv/
venv/
__pycache__/
.pytest_cache/
*.pyc
*.pyo
.idea/
.vscode/
reports/
config.local.yaml
# 节点本地凭据文件（密码/测试号/共享盘账号）—— 每台机器自己一份，绝不进 git
farm_node.env
tools/farm_node.env
交接归档/
*.log
myappstore_*_Setup.exe
# WorkBuddy 工具目录（会话记忆等），属本机状态，不进仓库
.workbuddy-ai/
# Obsidian 工作区状态（打开的标签/光标位置），纯本机 UI 状态，每次开都变
**/.obsidian/workspace.json
**/.obsidian/workspace-mobile.json
# 学习库手机版：由 .workbuddy-ai/tools/build_mobile_html.py 生成，属可重建产物
学习库-手机版.html
# 离线依赖包（tools/fetch_wheelhouse.py 的默认落点，十几~上百 MB 的 .whl）。
# **必须忽略**：无外网机器靠它装依赖，但它跟 Python 版本绑死、且体积大，
# 属于"按需重新产出"的产物，不是源码 —— 提交进去等于给每个 clone 塞一份二进制。
wheelhouse/
