[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    unit: 不改机器状态
    install: P0 安装 / 升级
    destructive: 会卸载或安装华硕大厅，需 --allow-install 或 HALL_ALLOW_INSTALL=1
filterwarnings =
    ignore::DeprecationWarning
